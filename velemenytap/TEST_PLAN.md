# Test Plan

Round-4 finding R4-08: this file previously hardcoded test counts (82 unit / 35 e2e) that had already drifted from reality by two rounds' worth of changes. Exact counts are not repeated here going forward — run `npm run test` / `npm run test:e2e` (or `npx playwright test --list`) for the current numbers rather than trusting a number in prose that nothing keeps in sync automatically.

## Automated: Vitest (`npm run test`)

Pure logic only — nothing that needs a database, a browser, or Next.js's server runtime:

- **`lib/safe-redirect.test.ts`** — the shared open-redirect guard (`SECURITY.md` § Redirect safety): valid internal paths, absolute external URLs, protocol-relative URLs, the backslash bypass, the round-3 dot-segment/double-slash-pathname bypass (R2-01 round 2), control characters, encoded variants, malformed input.
- **`lib/sentry-redact.test.ts`** — the `beforeSend` hook stripping `feedback_text`/`internal_note` from every Sentry event before it's sent. Canary-based: asserts against the fully serialized event, including circular-reference and JSON-stringified-content cases.
- **`features/feedback/schema.test.ts`** — the public submission zod schema, including the **review-gating regression test at the schema level**: `it.each([1,2,3,4,5])` asserts the validator has no rating-dependent branching.
- **`features/analytics/parse-snapshots.test.ts`** — parses `get_feedback_overview_snapshot`/`get_feedback_period_analytics`'s `jsonb` RPC results into typed shapes, discriminating a query error/null result into `{ unavailable: true }` (round-2 R2-04) rather than a silently-empty stats page.
- **`e2e/support/env.test.ts`** — e2e credential resolution (round-2 R2-06, round-3 R3-01): fail-closed on missing config, single-source resolution (never a file/process-env mix), the approved-project allowlist, force-disabled email/telemetry keys.
- **`e2e/support/db-connection.test.ts`** — round-4 R4-04: `SUPABASE_DB_URL`'s CI-mandatory/local-optional split, the approved-project-ref check, unreachable-in-CI failing loud.
- **`app/api/health/route.test.ts`** — round-4 R4-01: `/api/health` reports `ok:false`/503 when release metadata (`VERCEL_GIT_COMMIT_SHA`) is missing in a production/preview environment, `ok:true` locally and whenever a commit SHA is present.
- **`features/onboarding-tour/actions.test.ts`** — found during an independent review that no e2e test could deterministically exercise `setOnboardingTourStatusAction`'s own zero-affected-row disambiguation (the UPDATE and its follow-up read are two calls inside one server-side function, with no black-box hook to steer what either returns mid-flight). Mocks `@/lib/supabase/server` and `@/features/organizations/current` (`vi.hoisted` + `vi.mock`, a chainable mock query builder standing in for the real Supabase client's fluent API) and drives every branch directly: one-row success, each zero-row outcome (confirmed-completed success; readable-but-not-completed error; unreadable error; a read-back database error), the initial UPDATE erroring, and the invalid-status guard. First Server-Action unit test in this codebase — see its own file comment for why this specific function is the exception to the rule below.
- **`features/billing/status.test.ts`** (32 tests) — `isBillingActive()` and `hasLiveSubscription()`, the two halves of the billing state machine (`DECISIONS.md` has the full transition table). `isBillingActive`: no billing row, a real active/trialing Stripe subscription, the no-card trial (not-yet-expired, expired, missing `trial_ends_at`), a genuine Stripe-side trial, every real-Stripe-subscription status that HAS activated before (blocked), and — found during an independent review — the same statuses when the organization has **never** activated (`incomplete`/`incomplete_expired` fall back to grandfathering or the trial instead of blocking outright, since real billing never actually started), plus grandfathering's own transitions (never expires, stops applying once activation has genuinely happened). `hasLiveSubscription`: the set Checkout's own guard uses to distinguish a live/recoverable subscription from a terminal one (`canceled`/`incomplete_expired` must allow a fresh Checkout attempt, not block it forever).
- **`features/billing/plans.test.ts`** — the billing-interval allowlist (`isBillingInterval`) a crafted form submission must not bypass, and `stripePriceId`'s env-var resolution per interval.
- **`features/billing/actions.test.ts`** (37 tests) — found during an independent review, the same reasoning as `onboarding-tour/actions.test.ts` above: `getOrCreateStripeCustomerId`'s missing-row/error/concurrency handling and `claimAndCreateCheckoutSession`'s durable checkout-attempt model have no black-box e2e hook that can reliably force those exact states. Mocks the collaborators these actions call — `admin.rpc()` per-RPC-name queues for `claim_checkout_attempt`/`record_checkout_session`/`release_checkout_attempt`/`renew_checkout_attempt`, plus the same generic queue-based builder as before for `getOrCreateStripeCustomerId`'s own reads (see the file's own top comment for the exact call order each test's queue follows) — and drives each branch directly: a missing `organization_billing` row fails safe instead of creating an orphaned Stripe customer; **Finding 10** — `customers.search()` recovers an already-created Stripe customer before ever calling `create()`, independent of the idempotency key's own retention window, and the mutable `name` field is set via a separate `update()` call, never inside the idempotency-guarded `create()`; **Finding 1** — a reused session's plan is verified against its own actual Stripe-side line-item price, never this row's bookkeeping alone; **Finding 2** — a superseded still-`open` session is explicitly `expire()`d before a different session's URL is ever returned, with a race-safe re-check for the session completing during expiration; **Finding 8** — a `complete` session is only treated as paid when `payment_status` is `"paid"`/`"no_payment_required"`, and released (not trapped) otherwise; **Finding 7** — `release_checkout_attempt` reporting `false` is inspected, not ignored, and a reclaim after release fails closed if it still shows an existing session id rather than trusting whatever attempt id comes back; **Finding 9** — `renew_checkout_attempt` is called at the checkpoint right before Checkout Session creation, and a failed renewal aborts rather than proceeding; a barrier-controlled test proves two concurrent attempts for the same organization never both create a real Stripe session; `canManageBilling` is enforced server-side (manager/staff refused, owner/admin allowed) for both Checkout and the Billing Portal.
- **`features/billing/stripe-config.test.ts`** (25 tests) — third independent review, Finding 6; fourth independent review, Findings 6/13, plus the HUF `unit_amount` bug below: `assertStripeConfigurationValid()`'s own validation logic, mocking Stripe's Price API. Covers a fully consistent live/production and test/preview configuration; `resolveExpectedLivemode()` now failing CLOSED — a deployed runtime (`NODE_ENV` not `"development"`) with `APP_ENV` unset, empty, or misspelled throws rather than silently skipping enforcement, and only genuine local development (`NODE_ENV === "development"`) is exempt; a key/expected-livemode mismatch in both directions; a missing Price id env var; a Price's livemode/active/currency/type/recurring-interval/`interval_count` (Finding 13 — a Price billed every 3 months no longer passes as an ordinary monthly plan)/amount/tax_behavior each individually wrong; the monthly and yearly Prices resolving to different Stripe Products (and the expanded-product-object case working correctly); memoization of a successful validation (one Stripe call per interval, not per call); and a failed validation never being cached (a subsequent call re-attempts it automatically). **Two of these 25 tests were added directly to reproduce and prove the fix for a genuine, previously-undiscovered bug found live during this round's own real Stripe test-mode verification** (not by any unit test, and not one of the fourteen findings a reviewer identified from reading the code): the amount check compared `price.unit_amount` (Stripe's own minor-unit amount — HUF is a normal two-decimal charging currency, so a real 5,990 Ft Price reports `599000`) against `plan.amountHuf` (a whole-Forint display value, `5990`) with no unit conversion, a 100x mismatch that made this check FAIL for every correctly-priced real Stripe Price — Checkout had been unconditionally blocked (fail-closed, by this function's own design) wherever the configured Price actually matched what the billing page promises. **The bug had gone completely undetected by the pre-existing tests because `price()`, this file's own test helper for constructing a mock Price, used the identical wrong `unit_amount` convention** (`5990` instead of `599000`) — the mock mirrored the implementation's bug rather than catching it, so every test comparing the (broken) implementation against the (equally broken) mock passed. Fixed by comparing against `plan.amountHuf * 100`, and by correcting the helper's own convention; see `DECISIONS.md`'s "Stripe configuration validated..." entry for the full incident and why this is the concrete, citable reason a real (non-mocked) Stripe round trip was required this round, not optional.
- **`scripts/rollout.test.ts`** (102 tests) — `scripts/rollout.mjs`'s own suite: `loadEnvironments`, `projectRefFromDbUrl`, `parseArgs`'s `prepare`/`finalize` subcommand split and phase-separation rejections, `validateMigrationPlan`/`validatePreparePlan`/`validateFinalizePlan`'s resumability semantics, credential redaction (round-7 R7-02), `pollHealth`'s redirect rejection (round-7 R7-04) and its own diagnostic-message surfacing, `runFinalize` actually polling health even when nothing is pending to apply, `pollHealth` aborting a hanging request within its own timeout budget (third independent review, Finding 8), `runPrepare`'s real temporary-workspace assembly (third independent review's own adversarial self-review — the workdir contains every `--expand` file and `config.toml` but excludes every `--enforce` file, `supabase db push` is pointed at the temp workdir, the real `supabase/migrations/` directory is provably untouched, cleanup after both success and a thrown push failure, `--dry-run`, the empty-`--expand` skip path), and — **fourth independent review, Finding 14** — a tighter `pollHealth` deadline test proving it never waits meaningfully past its own configured deadline even with well under a second remaining: verified both ways, against the pre-fix body (which floored any remaining-time computation at `Math.max(1000, ...)`, so a near-expired deadline could still wait a further full second past when it should have already given up) and against the current no-floor implementation.
- **`app/api/webhooks/stripe/route.test.ts`** (22 tests — down from 34; several of the removed tests are the ones the "Corrected record" paragraph in `DECISIONS.md` explains were never valid proof of anything) — fourth independent review, Finding 5: this route now delegates all actual reconciliation to `features/billing/reconcile.ts` (mocked here as a single collaborator), so this file's own job narrows to what the route itself is responsible for: missing/invalid signature rejected before touching the database; `resolveExpectedLivemode()` throwing, or returning a live/test-mode mismatch, both reject the event (Finding 6); a genuine event resolves the organization, verifies the Stripe Customer matches the organization's own persisted one, then delegates to the shared service and records the event; the event's own embedded object is never trusted for anything but its id; **Finding 11** — a Stripe-Customer mismatch is quarantined (never rebinding the organization) and recorded as a `"customer_mismatch"` anomaly via `recordAnomaly`; the shared service reporting `"error"` or `"deferred"` (lease contention) both ask Stripe to retry, with `"deferred"` explicitly NOT treated as a failure — the durable dirty-marking inside the database function is the real guarantee, this route's retry is only a fast-path; a duplicate delivery reprocesses idempotently; the `stripe_customer_id` fallback lookup when metadata is missing; an organization that can't be resolved at all asks Stripe to retry; and the `activated_at`/`invoice.paid` suite (Finding 5/11) — a non-`'paid'` invoice status, a non-subscription invoice, a customer mismatch, an unapproved-price subscription, the metadata-snapshot fallback lookup, and the activation service itself deferring. **What this file deliberately no longer tests**: the actual concurrency/locking behavior of the reconciliation lease — that needs a real Postgres connection to mean anything, and is proven instead by `e2e/billing-concurrency.spec.ts` against the real isolated project (see below and `DECISIONS.md`'s "Corrected record" for why this round's own adversarial self-review concluded a mocked concurrency test for this specific property could not be trusted going forward).
- **`features/billing/reconcile.test.ts`** (24 tests, NEW) — the shared reconciliation service every other billing entry point calls. `pickCurrentSubscription`: an active/trialing subscription always wins over a newer recoverable one and a recoverable one always wins over a terminal one (Finding 4); among multiple simultaneously active/trialing subscriptions, the most recent is picked and the rest reported as `duplicateActive`; a subscription whose price isn't approved is excluded from the pool entirely, even when nothing else exists (Finding 11); a subscription with no price on its first item is treated as unapproved rather than crashing. `customerIdMatches`: a null persisted id matches anything (first-ever subscription), a persisted id requires an exact match. `reconcileOrganizationBilling`: claims the lease before calling Stripe and conditions the write on still owning it (Finding 3); returns `"deferred"`, never an error, both when the lease can't be claimed and when the write loses it; releases the lease (and returns an error) when Stripe's `list()` call itself throws; clears the dirty flag via `write_reconciliation_result`, not `release_reconciliation_lease`, on a genuine `"no_subscriptions"` outcome. **Two of these tests reproduce bugs found during this round's own independent adversarial audit of its own new code, not one of the fourteen findings**: a previously-tracked subscription that has since moved to an unapproved price now correctly revokes entitlement instead of leaving `status: "active"` frozen forever from the last time it was approved; and `activateOrganizationBilling` now wraps `write_activation` in a `try`/`catch` that releases the lease and marks the organization dirty on a thrown error, closing a gap where an earlier version had no `try`/`catch` there at all (unlike `reconcileOrganizationBilling`), silently abandoning a claimed lease with `needs_reconciliation` never set on that specific failure path.
- **`features/billing/admin-actions.test.ts`** (8 tests, NEW) — the self-service admin resync action (Finding 5). Covers: no current organization; `canManageBilling` enforced (a `manager`/`staff` role is refused before ever touching the database); no `organization_billing` row yet, or no Stripe customer on it yet; and the shared service's `"reconciled"`/`"deferred"`/`"no_subscriptions"`/`"error"` outcomes each mapped to the right Hungarian success/error message, with `revalidatePath("/dashboard/billing")` called on every path.
- **`app/dashboard/billing/page.test.ts`** (16 tests, NEW) — `orgIdentifiersAgree` (Finding 12): both identifiers present and agreeing passes; both present but conflicting fails even though one of the two matches (the specific OR-to-AND fix); only one present must match on its own; neither present fails closed. `resolveCheckoutSuccessState`: no `session_id` or an unretrievable session both return `"invalid"`; a foreign session whose identifiers don't agree with the current organization returns `"invalid"` (Finding 12); an incomplete session, or a complete one with `payment_status: "unpaid"` (Finding 8), returns `"unpaid"`; `"no_payment_required"` is treated as paid; `"confirmed"` requires the EXACT session's own subscription to be locally reconciled as active/trialing — a different live subscription on the same organization does not count, and neither does a locally-recoverable (`incomplete`/`past_due`/`unpaid`/`paused`) status even when the subscription id matches (both Finding 12); when the local row hasn't caught up yet, this actively calls the shared reconciliation service itself rather than only waiting for a webhook (Finding 5), and reports `"pending"` (not an error) both when that reconciliation itself defers and when `billing` is `null` (a page-load race).

`npm run typecheck` and `npm run lint` run clean on every change — part of the same verification gate as the test suites, not test suites themselves.

### What's deliberately not covered by Vitest

Anything that needs a *real* database, RLS enforcement, cookies, or rendered UI needs a real browser and a real Supabase project, which Vitest (plain Node, no `react-server` condition) can't provide — that's what the e2e suite below is for. A Server Action's own internal logic is a narrower exception (`actions.test.ts` above): mocking its Supabase/lookup collaborators can exercise branches no e2e test can reach deterministically, but it proves nothing about RLS, real query behavior, or the action's actual database access — that half of the picture still needs e2e coverage alongside it, not instead of it.

## Automated: Playwright e2e (`npm run test:e2e`)

Against a **dedicated, isolated Supabase test project** (`.env.test.local`, gitignored) — never the production project. See `e2e/README.md` for setup and `DECISIONS.md` for why there's no Docker-based local stack instead. Every spec file carries its own doc comment explaining what it verifies and why; this is a map of what exists, not a restatement of each file's reasoning:

| Area | File(s) |
|---|---|
| Review-gating (Google Review CTA identical across ratings 1–5) | `review-gating.spec.ts` |
| Open-redirect fix, both call sites, both round-2/round-3 payloads | `redirect-safety.spec.ts` |
| Cross-tenant isolation (reads, writes, RPCs, dashboard rendering) | `tenant-isolation.spec.ts` |
| Feedback/card column-level immutability (RLS is row-level, not column-level) | `feedback-and-card-integrity.spec.ts` |
| Public submission safety: card-deactivation race, per-card rate limit | `public-submission-safety.spec.ts` |
| Location-deactivation race (round-2 R2-05) — real two-Postgres-connection concurrency | `location-deactivation-race.spec.ts` |
| Feedback inbox cursor pagination under tied timestamps | `feedback-pagination.spec.ts` |
| Analytics: row-count-ceiling removal, single-snapshot consistency under concurrent inserts | `analytics-aggregation.spec.ts` |
| Analytics period-day validation (round-3 R3-04) | `analytics-period-validation.spec.ts` |
| Organization onboarding: atomicity, idempotency, per-user serialization, slug collision | `organization-onboarding.spec.ts` |
| Negative-feedback alert abuse controls: server-owned cooldown, org-wide budget, real concurrency | `negative-feedback-alert-abuse.spec.ts` |
| Negative-feedback alert attempt/delivery status accounting | `negative-feedback-alert-finalize.spec.ts` |
| Card-editor location immutability at the UI/server-action/database layers | `nfc-card-location-lock.spec.ts` |
| Notification-email recipient confirmation flow, end-to-end including the real browser confirm-link round trip | `notification-email-verification.spec.ts` |
| RPC role-allowlist matrix (21 functions × anon/authenticated/service_role — fifteen after a third independent review's checkout/reconciliation-fencing additions, twenty-one after a fourth independent review dropped `claim_billing_sync` and added seven more), direct catalog introspection, plus a completeness test that fails if any function in the catalog has no matching row | `rpc-privilege-matrix.spec.ts` |
| Finding 1 rollout compatibility: a distinctly-named notification-email RPC never produces a `PGRST203` ambiguous-overload error at any point in the expand/deploy/enforce rollout sequence — reproduces all three real rollout states against the isolated project's actual PostgREST layer, plus a negative control proving the harness would catch the original bug | `rollout-compatibility-notification-email-rpc.spec.ts` |
| Dashboard nav accessibility (accessible names, `aria-current`, keyboard) and responsive behavior at 320/375/768/1280/1536px, including a real bounding-box overlap check at and above the 2xl breakpoint (found during an independent review: `scrollWidth`-only overflow checks cannot detect two elements visually overlapping without changing the page's scrollable width — see `dashboard-nav.tsx`'s own comment for the real measurements this caught) | `dashboard-nav-accessibility.spec.ts` |
| First-time dashboard onboarding tour: state model (`not_started`/`completed`/`skipped`), every dismissal path, `completed`'s terminal-state guarantee, reopened-tour promotion, backfilled-organization protection, nav highlight/responsive degradation, async close failure handling, CHECK-constraint enforcement independent of the Server Action's own validation | `onboarding-tour.spec.ts` (cross-tenant case in `tenant-isolation.spec.ts`) |
| Subscription paywall gates the dashboard (a lapsed org is redirected from any `/dashboard/*` route to `/dashboard/billing`, which itself never redirects) but never the public product: feedback submission through `/r/{publicId}` keeps working, unauthenticated, for an organization with an inactive subscription | `billing-paywall.spec.ts` |
| Checkout-attempt and reconciliation-lease RPCs under **real, genuine concurrency** against the actual isolated project (fourth independent review, migration `20260907210000`) — 12 tests: attempt rollover never lets a stale session id coexist with fresher attempt metadata (Finding 1); a genuinely concurrent second claim reuses the same live attempt; `record_checkout_session`/`renew_checkout_attempt` with the wrong id/attempt are real zero-row CAS failures (Finding 7/9); a genuinely concurrent lease claim is refused and durably marks the organization dirty without the winner ever releasing; a barrier-controlled test proves exactly one of two genuinely concurrent claims wins; a sequential claim-write-release-then-reclaim-write scenario proves the row ends up reflecting the LATER write, never the earlier one (the actual mutual-exclusion property the fencing-token design's own flawed test never established — see `DECISIONS.md`); `write_reconciliation_result` with the wrong owner is a zero-row CAS failure that re-marks the organization dirty; an expired lease can be reclaimed by a new caller; `release_reconciliation_lease` releases only on an owner match and always re-marks dirty regardless; **found by this same round's own three-independent-reviewer adversarial audit of the lease design just built** — a successful `claim_reconciliation_lease` call now marks the organization dirty immediately, before any write or release, proven by reading the row right after claiming (migration `20260907230000`, closing a genuine process-crash gap); `clear_reconciliation_dirty` releases the lease AND genuinely clears the dirty flag, unlike `release_reconciliation_lease` (migration `20260907240000`) | `billing-concurrency.spec.ts` |

**`rpc-privilege-matrix.spec.ts` and `location-deactivation-race.spec.ts` need a direct Postgres connection** (`SUPABASE_DB_URL`) — optional locally (skip gracefully without it), **mandatory in CI** as of round-4 R4-04 (`e2e/support/db-connection.ts` throws rather than skipping when CI is set and the connection is missing, invalid, or doesn't resolve to the approved isolated project). A skipped run of either is not equivalent to a passing one — check the run's own output for a `skipped` count, not just the absence of failures.

Runs locally against `npm run dev` (Playwright's `webServer` starts it automatically, isolated-project env injected — see `playwright.config.ts`) and in CI's `e2e` job (needs the four secrets in `DEPLOYMENT.md` § 4; skips the whole job, not individual tests, if any are missing).

## Review-Gating Regression Test (standing checklist)

Per the product skill, re-run this whenever the public rating/review flow changes:

- [ ] 1 star → Google Review CTA available, same placement/prominence as other ratings
- [ ] 2–5 stars → same
- [ ] No code path conditions CTA visibility on rating, sentiment, or AI analysis
- [ ] `features/feedback/schema.test.ts`'s `it.each([1,2,3,4,5])` still passes (schema-level guard)
- [ ] `e2e/review-gating.spec.ts` still passes (browser-level guard, automated in CI)

## CI

See `DEPLOYMENT.md` § 5 for the full job graph and gating model. In brief: `checks` (typecheck/lint/unit tests) → `e2e` (Playwright, needs all four secrets or skips) → `verify-production-deployment` (push to `master` only, confirms the live production `/api/health` reports the pushed commit — round-4 R4-01).

## Manual verification (this project's discipline, not a substitute for the above)

Whenever a change is only verifiable by actually using the app (a UI change, a flow that's awkward to script), it gets a real browser pass against the isolated test project before being called done — never against production, and never by asking the user to manually check something an automated test could instead cover permanently. See individual commit messages and `STATUS.md`'s round-by-round entries for what was manually verified when, rather than a separate manual checklist here that would just go stale the same way the old test counts did.

## Known gaps (tracked, not silently skipped)

- **No automated test that a real email is actually delivered via Resend.** The alert-cooldown/budget *claim mechanism* is tested directly (`negative-feedback-alert-abuse.spec.ts`, `negative-feedback-alert-finalize.spec.ts`); the isolated test project deliberately has no `RESEND_API_KEY` (`e2e/README.md`), so an actual Resend API call is never exercised by CI. Exercising it would need either a second, Resend-key-bearing test configuration or a mocked Resend client — not currently justified by risk (the claim/budget logic, which is where the actual abuse-prevention value is, is fully covered).
- **`verify-production-deployment` (round-4 R4-01) has not yet had a real failure to prove it actually catches the failure mode it's for** — it's designed and reasoned from the actual historical incident (`STATUS.md`), but its own alarm has not fired for real (by design, ideally never). Worth deliberately testing once, in a low-stakes way (e.g., temporarily pointing `PRODUCTION_HEALTH_URL` at a `--expected-sha` that will never match, on a non-`master` branch, to confirm the job actually goes red) rather than trusting the design alone forever.
- **No *permanent, CI-automated* e2e coverage of the actual Stripe Checkout/webhook flow — but this round performed the real thing manually, once, with concrete evidence, and that gap is narrower than it used to be.** `billing-paywall.spec.ts` still drives the paywall gate directly through `organization_billing`, never a real Stripe call, and the unit suites (`app/api/webhooks/stripe/route.test.ts`, `features/billing/actions.test.ts`/`reconcile.test.ts`/`admin-actions.test.ts`, `app/dashboard/billing/page.test.ts`) mock Stripe entirely — `e2e/README.md`'s own text ("There is currently no e2e coverage of the actual Stripe Checkout/webhook flow") is still accurate as a description of *automated, repeatable CI coverage*. **What changed this round: a real Stripe test-mode round trip was performed manually** — a seeded test organization, a real browser session, a real redirect to Stripe's hosted test-mode Checkout, payment with Stripe's standard test card, and 14 real signed webhook events (forwarded locally via `stripe listen`) processed with HTTP 200 and zero errors, ending in a verified database state (`status: active`, `activated_at` set, the reconciliation lease correctly acquired/used/released, `needs_reconciliation: false`) confirmed directly against the isolated project's REST API, not just the UI — see `STATUS.md` for the full run. This is real, mandatory verification, genuinely completed — but it was a one-time manual pass, not (yet) a committed Playwright spec that runs on every CI push, so a future regression in the real Stripe request/response shape, webhook signature handling, or event-delivery timing would not be automatically caught the way the mocked coverage's own branching logic is. A good candidate follow-up (not built this round, given the scope already covered): automate this using the Stripe CLI as a companion process in CI, a known, documented pattern.
- **Migration replay (a clean-database apply of all 31 migrations, and an upgrade-from-production's-actual-1–17 apply of 18–31) was attempted and NOT performed this round — a genuine, confirmed, unresolved gate, not a silently skipped step.** This environment has no Docker, no local Postgres client tools (`psql`/`pg_ctl`/`initdb`/`postgres` all confirmed absent), no `embedded-postgres`/`@electric-sql/pglite` npm packages installed, and no authenticated Supabase CLI/MCP account access to provision a fresh disposable cloud project (`supabase projects list` fails with a `LegacyPlatformAuthRequiredError` — no access token configured). Per this project's own standing discipline (never destructively reset the shared isolated Supabase project; use a genuinely disposable database or leave the gate open, rather than substitute a misleading approximation), this was correctly left undone: a WASM/embedded Postgres would not accurately replicate Supabase's own `auth` schema, extensions, and role setup these migrations depend on, so a "pass" there would not actually prove what real migration replay is supposed to prove. **If this gate is ever closed, it needs either Docker becoming available in this environment, or authenticated Supabase CLI/MCP access to provision a genuinely disposable cloud project — not a workaround inside the existing constraints.**
- **A dev-mode, high-parallelism e2e run producing failures is a known, understood, non-representative artifact — do not mistake it for suite flakiness.** An earlier full-suite run against `next dev` (not a production build) with unlimited local parallelism (6 workers) produced 50 failures, confirmed by an isolated stray-process-elimination test and by re-running against a production build (which produced zero such failures) to be pure dev-server compilation-latency contention under load — Turbopack's on-demand compilation genuinely slows down under many concurrent first-hits to routes it hasn't compiled yet, which looks like flakiness but isn't a rate-limit problem or a real application defect, and is irrelevant to CI (which already runs against `next start`, not `next dev` — see `playwright.config.ts`'s own comment on why). **The suite's own actual reliability was proven separately and cleanly**: the full 20-file, 169-test suite was run to completion against a real production build (`npm run build` + `npm run start`, matching CI's exact `workers: 1, retries: 1` configuration) with a real direct Postgres connection so nothing silently skipped — see `STATUS.md` for the exact pass/fail/skip counts and duration of that run. Two tests needed fixing first: `rpc-privilege-matrix.spec.ts`'s role-matrix test and its own completeness test both still referenced the now-dropped `claim_billing_sync` and were missing the seven functions this round added — fixed to match the current 21-function/21-row catalog (`DATABASE_SCHEMA.md`).
- **A standing, previously-undocumented e2e flakiness source — Supabase Auth's shared rate limiter under full parallel runs — is now closed with a shared retry helper, not by working around it per-file.** Every UI-driven sign-in across the suite now goes through `e2e/support/ui.ts`'s `signInViaUi`/`submitLoginForm` (rate-limit-aware retry with backoff) instead of each spec file duplicating its own fill-and-click block with no retry; `e2e/support/seed.ts`'s `userClient()` (the API-level sign-in helper used outside the UI) gained the same retry-with-backoff for rate-limit errors. This is the fix that made the full-suite production-build run above pass cleanly rather than intermittently.

## Real-PostgreSQL harness (`scripts/verify-local-database.mjs`)

Unit mocks are fine for isolated error branches; they are not evidence about locking, CAS semantics, migration replay, triggers, grants or RLS. `node scripts/verify-local-database.mjs` runs 25 checks against a real, disposable local PostgreSQL cluster — it creates uniquely named throwaway databases and never drops or resets an existing one. It deliberately does **not** claim to cover Supabase Auth or PostgREST; those need the isolated project and the Playwright suite.

What it proves that nothing else does:

- Every migration replays from an empty database, and an organization created at migration 17 (the last pre-billing state) survives the remaining migrations with its data untouched and a non-expiring grandfathered grant.
- The checkout attempt's identity, its recorded Session and its immutable request all survive operation-lease expiry, while a superseded owner can neither record nor release — the property the whole duplicate-Session defence rests on.
- An event arriving during an owned reconciliation leaves durable unfinished work after the holder's write, and the follow-up pass converges. This is executed as four real, ordered RPC calls, not asserted about a mock.
- A lease-timed write that waits past its own expiry behind a genuine competing row lock is rejected *after* acquiring that lock. This check found a real bug in the first draft of migrations 34/35 (see `DECISIONS.md`'s second postscript on the `now()`-after-lock class) — reading the SQL would not have.
- The candidate scan finds an organization by staleness alone, with nothing flagged and no history at all — the "webhook never delivered" case.
- Ratings 1–5 return the identical Google review opportunity through the real `submit_feedback_atomic`, including for an organization whose own subscription has lapsed.
- Every billing RPC denies `anon`/`authenticated` execute and allows only `service_role`, and an authenticated user sees only its own billing row and cannot grant itself a subscription.

Point it at a cluster with `LOCAL_AUDIT_DATABASE_URL`; it refuses to run against anything but a `127.0.0.1:55439` `audit` cluster, so it cannot be aimed at a real project by accident.

## Real Stripe test-mode lifecycle (2026-09-08, against the integrated flow)

Run against the **isolated** Supabase project with Stripe **test-mode** keys and `stripe listen` forwarding — never production, and never live keys. What it established that no mock can:

- A real Checkout Session, paid with Stripe's `4242` test card, produced **14 signed webhook events, all HTTP 200**, and converged to `status: active` with a real customer and subscription id, `activated_at` set, and `current_period_end` one month out.
- `billing_sync_requested` and `billing_sync_completed` both reached **3 and matched** — the generation pair converged rather than leaving phantom outstanding work, and `needs_reconciliation` ended `false` with the lease released and every `checkout_*` column cleared.
- Mid-flight, the row showed exactly the new design: `checkout_attempt_id` and `checkout_request` present with `checkout_owner_token` **null** — the operation had finished while the attempt deliberately outlived it.
- Stripe's hosted page rendered **HUF 5,990.00** for `unit_amount: 599000`, confirming against real Stripe data that HUF is a two-decimal charging currency and that `plan.amountHuf * 100` is correct.
- Exactly **one** Stripe Customer carried the organization's id afterwards, and exactly one subscription hung off it.

Three claims about Stripe's own behaviour — the ones the whole duplicate-prevention design rests on — were verified directly rather than assumed:

| Claim | Result |
|---|---|
| Replaying an attempt's idempotency key with identical parameters returns the SAME Session | confirmed, same `cs_test_…` id |
| Replaying it with DIFFERENT parameters is rejected, not deduplicated | confirmed, `StripeIdempotencyError` |
| A different attempt id yields a genuinely new Session | confirmed |

The middle row is why `checkout_request` has to be an immutable stored snapshot rather than rebuilt per attempt: a takeover that rebuilt the request would hit that error rather than recovering.

## Supabase Auth recovery — what is proven, and the one hop that is not

Recovery and resend-confirmation go through **Supabase Auth's own mailer** (`auth.resetPasswordForEmail`, `auth.resend`), which is an entirely separate path from this app's Resend integration. The verified `velemenytap.hu` Resend domain does nothing for them unless it is also configured as Supabase Auth's custom SMTP.

Verified end to end against the real isolated project: the form renders, the Server Action calls Auth with the correct `redirectTo`, Auth mints a valid recovery token honouring that target, `/auth/callback` completes it, and `/auth/reset-password` renders the set-new-password form with a real session cookie established. An invalid or already-used link degrades to `/auth/auth-code-error` rather than anywhere authenticated.

**Not verified: the SMTP hop itself.** Two sends against the isolated project returned `over_email_send_rate_limit`, which is the signature of Supabase's built-in mailer (a few messages per hour, deliverable only to project team members) rather than configured custom SMTP. Whether production has custom SMTP is unknown from here and needs the owner. Until it is confirmed, assume password reset and signup confirmation do not reach real customers.

## Subscription lifecycle beyond the initial purchase (Stripe test clocks)

The initial monthly purchase was verified by hand through the real UI. Everything *after* it is verified by a scripted test-clock lifecycle, because renewals and dunning cannot be driven by clicking. Each phase asserts what **this app** persisted, not what Stripe reports, since the app's own convergence is what is under test.

| Phase | Verified |
|---|---|
| Annual checkout | Real Checkout Session on the yearly price through the app's own Server Action, paid with `4242`; reconciled to `active` with `current_period_end` exactly **365 days** out, generations 5/5 |
| Renewal | Clock advanced one month; renewal charged, still `active`, `current_period_end` rolled forward |
| Failed payment | Default payment method swapped to a failing card, clock advanced; lands in `past_due` (a recoverable status, not a terminal one) |
| Recovery | Good card restored and the open invoice paid; returns to `active` |
| Cancellation | Subscription cancelled; entitlement revoked to `canceled` |
| Resubscription | New subscription created; entitlement restored and `stripe_subscription_id` points at the NEW subscription, not the cancelled one |
| Convergence | `billing_sync_requested == billing_sync_completed` and `needs_reconciliation = false` after the whole lifecycle |
| `activated_at` | Set once on first activation and never rewritten across renewal, failure, cancellation or resubscription |

**One property of the harness worth knowing before reading its output.** A webhook whose reconciliation defers (another writer holds the lease) returns HTTP 500 *on purpose*, so Stripe redelivers it — with the durable dirty flag and the scheduled sweep as the guarantee underneath. `stripe listen` does **not** redeliver on 5xx, so a local run silently loses exactly the retries production would make, and phase results vary run to run purely on which event lost a lease race. Driving the sweep from the harness restores the half of the production contract the CLI cannot model. Separately, `get_billing_reconciliation_candidates` backs off any organization attempted in the last minute, so a sweep fired immediately after a burst correctly reports nothing to do — the final convergence assertion has to wait that window out, which the 15-minute production schedule does by construction.

A related harness lesson: linking the organization to its Stripe customer *before* creating a subscription made the script emit `customer.created` / `payment_method.attached` / `customer.updated` against a subscription-less organization. A reconciler then correctly recorded "nothing to reconcile" and marked the generation satisfied — a true result for a state the real product never produces, since its Stripe customer is created inside checkout and a subscription always follows. The harness now links the organization only once a subscription exists.

## Supabase Auth email delivery — measured, not inferred

Against the **isolated** project, with a real readable mailbox (a disposable `mail.tm` address polled over its API):

- `auth.signUp` → **HTTP 429 `over_email_send_rate_limit`** (project-wide hourly cap).
- `auth.resend` → accepted, **no error** — and nothing arrived within 120 s.
- `auth.resetPasswordForEmail` → accepted, **no error** — and nothing arrived within 120 s.

"Accepted, then never delivered" to an ordinary external address is the signature of Supabase's built-in email service, which only delivers to project team members and caps sends at a few per hour; configured custom SMTP raises that cap substantially. Two independent signals therefore point the same way, and the practical conclusion does not depend on choosing between them: **a normal customer's confirmation and password-reset emails do not arrive on this configuration.**

What that does *not* establish is production's configuration, which is a separate project and cannot be read from here without a Management API token (`supabase projects list` fails with `LegacyPlatformAuthRequiredError`). Nor does an API acceptance response distinguish "queued and delivered" from "queued and dropped" — only a mailbox does, which is why this test uses one.

Everything either side of the SMTP hop is verified: the forms render, the Server Actions call Auth with the correct `redirectTo`, Auth mints valid tokens honouring those targets, `/auth/callback` completes both server-readable link shapes, `/auth/reset-password` renders with a real session, and an invalid or already-used link degrades to `/auth/auth-code-error`.

## Supabase Auth email — verified end to end (2026-09-09)

Superseding the measurement above, which established only that nothing arrived. With
the CLI authenticated, the actual configuration could be read rather than inferred,
and it showed two independent faults:

1. **No custom SMTP on either project** — the built-in mailer, capped at 2/hour and
   documented as team-members-only. Sends to an external mailbox were accepted with no
   error and never delivered.
2. **Templates used `{{ .ConfirmationURL }}`** — which redirects with the session in a
   URL fragment. A fragment never reaches the server, so `/auth/confirm` and
   `/auth/callback` both fell through to `/auth/auth-code-error` *even though the token
   was consumed correctly* (the account genuinely became confirmed). The app has always
   implemented the `token_hash` flow; the templates never matched it.

Fixing only the first would have produced emails that arrive and links that still fail.

Both fixed on the isolated project and verified with a real mailbox, receiving and
clicking each link:

| Flow | Result |
|---|---|
| Signup confirmation | email received from `alerts@velemenytap.hu` → clicked → **`/onboarding`** (200) → `email_confirmed_at` set |
| Resend confirmation | on a deliberately unconfirmed account: second email received → clicked → **`/onboarding`** → account confirmed |
| Password reset | email received → clicked → **`/auth/reset-password`** (200) with a real `sb-` session cookie |

Two harness notes: Supabase throttles per address (`max_frequency`, ~60s), so a rapid
second send returns 429 — that is a throttle, not a delivery failure. And a resend of
type `signup` for an **already-confirmed** account is accepted but sends nothing, which
is correct; proving resend requires an account left unconfirmed on purpose.
