# Status

Last updated: 2026-09-10, after an independent round-10 review found eight defects — including that **two of round 9's own fixes were wrong**. All eight are confirmed and fixed, with each regression mutation-tested against the pre-fix behaviour.

## Round 10: two of round 9's fixes were wrong (2026-09-10)

An independent reviewer was given `REVIEW_REQUEST_ROUND10.md` and the branch at `aa87587`, and returned **eight defects — two P1 and six P2 — every one confirmed and fixed.** The round-10 request asked specifically whether round 9's fixes were right, because several replaced one invariant with another. Two of them had not been.

The eight are not eight separate stories. They are four causes:

| Cause | Findings |
|---|---|
| A live Stripe **status** was treated as a historical **payment** | R10-01, and R10-03 as its consequence |
| A **terminal-looking state** was treated as one that cannot collect | R10-02 |
| An idempotency key's **age** was treated as proof it had been **sent** | R10-04, R10-05 |
| A **time-based decision** was made before a lock it waits on | R10-06, R10-07 |
| **Work not lost** was reported as **work done** | R10-08 |

Four forward migrations: `20260910100000`, `20260910110000`, `20260910120000`, `20260910130000`. Production untouched — it is still on migration 17 with no Stripe environment variables.

The requirements these now satisfy are written down once, in `BILLING_INVARIANTS.md`, so a future change can be checked against a requirement rather than against the current implementation. That file exists because the recurring failure mode across five rounds has been a comment stating a rule the code did not enforce.

### R10-01 (P1) — an `active` subscription permanently consumed prepayment grace

Round 9's R9-02 fix had `write_reconciliation_result` set `activated_at` whenever it wrote an `active` status, "from evidence it already holds". It holds no such evidence. Stripe documents a `collection_method=send_invoice` subscription as starting active with its first invoice unpaid, and a subscription can be created active out-of-band entirely.

`activated_at` is a one-way latch that permanently ends grandfathering. The reviewer reproduced a grandfathered organization given an approved-price invoiced subscription that was never paid, then canceled: dashboard access went **true → true → false**, with no activation ever requested and no invoice ever paid. Unrecoverable without manual repair.

**Fixed:** `activated_at` is now written *only* by `request_billing_activation`, which requires verified `invoice.paid` evidence and stores it in a new `activation_evidence` column. No reconciliation writer derives it from any status. The product's payment policy is unchanged and stated explicitly in `BILLING_INVARIANTS.md` § I1 — including that a **zero-total paid invoice still activates**, because the pre-round-9 path already accepted it and narrowing it would revoke access from organizations that legitimately have it.

### R10-03 (P2) — a verified activation could never finish after cancellation

The reason round 9 reached for that wrong rule was real. Activation was **two phases**: register the obligation, then discharge it under the reconciliation lease. A handler that verified a paid invoice and died in between left an obligation only another invoice event could satisfy — and if the invoice was never redelivered and the subscription was since canceled, every sweep could refresh the subscription and none could finish the activation. Reproduced over three successful canceled-state refreshes, each leaving the row exactly as dirty as it found it.

**Fixed by removing the gap rather than adding a recovery path.** Activation is now a single statement that writes the evidence and the latch together, so a pending activation cannot exist — strictly stronger than "a pending activation can be finished later". `write_activation` is dropped; it *was* the interruptible half. The lease was never needed: activation makes no Stripe call and `activated_at = coalesce(activated_at, paid_at)` is idempotent and order-independent.

### R10-02 (P1) — a completed Checkout with an unresolved payment authorized a replacement

Round 9's R9-01 fix released a checkout attempt once the Session left the `open` state, arguing `complete` is "terminal for payment purposes". Stripe reaches `complete` while a payment is still processing; `payment_status: unpaid` means unresolved, not dead. The reviewer drove the actual checkout coordinator through both paths — an initially complete/unpaid Session and one that becomes complete/unpaid during expiration — and both released the attempt and returned a replacement URL, while the original payment could still succeed.

**Fixed:** for a completed Session with an unresolved payment, the decision is made on **the subscription it created** — the object that would actually collect. Only a terminal subscription (`canceled`, `incomplete_expired`) releases the attempt. `incomplete`, `past_due`, `unpaid`, `active`, `trialing`, `paused`, an unreadable subscription, or no subscription at all all keep the obligation pending. `expired` remains a valid replacement case.

**Scope note the reviewer raised and I am not resolving by assumption:** `buildCheckoutRequest` specifies no `payment_method_types`, which is how Stripe enables Dashboard-managed dynamic payment methods. The claim that this app is "card only" is an account *setting*, not something this source establishes. The fix above does not depend on it — it is about Stripe's states, not this app's configuration — but the card-only claim itself remains unverified and is now listed as such.

### R10-04 (P2) — the corrective backfill overwrote valid modern identities

Round 9's R9-04 fix stamped `legacy-org-<id>` on **every** unresolved row. "Unresolved" does not imply "used the old organization-derived key": a deployment that had run migration 36 could already hold a valid random identity whose Customer existed but was never recorded locally. The backfill overwrote it, and for a recent organization the replacement looked retry-safe — so recovery was skipped and `create()` replayed a *different* key. The reviewer's ledger ended with **two Customers under two keys**.

Round 9 fixed one instance of a wrong inference by making the same inference in reverse. The real error, common to both, is that an identity's **age** was treated as proof it had been **sent**.

**Fixed:** `customer_creation_key_state` makes that an explicit, durable fact — `unused` (minted here, never sent; nothing can exist under it), `sent` (may have landed; replay inside retention, enumerate outside it), `unverified_legacy` (a migration supplied it; always enumerate). The application moves `unused → sent` immediately *before* the Stripe call, so a crash during the call still records that it may have landed. No migration can manufacture a false replay, and no valid identity needs overwriting — the identity's *value* is no longer what decides safety.

### R10-05 (P2) — an expired claimant could still create under a retired key

The creation lease excludes live contenders but cannot fence a worker that resumes after its lease expired: it can wake after a successor has enumerated, rotated and created, and create a second Customer under the retired key. The reviewer demonstrated it with a scaled one-second lease.

**Partially fixed, and the limit is stated rather than papered over.** `mark_stripe_customer_key_sent` is a fence taken immediately before the Stripe call: it re-checks the lease and requires enough of it to remain to cover the call's own bounded lifetime. That narrows the window to the gap between the check and Stripe receiving the request. **It does not close it, and no database lease can** — Stripe cannot be told to disregard a request already in flight. So the residual case is now *detected*: a Customer this request created that the row no longer points at is recorded as an `orphaned_customer` anomaly instead of being silently discarded, which is what made the race invisible rather than merely rare. `OPERATOR_RECOVERY.md` § 1 says what to do with one.

### R10-06 (P2) — two more clock-before-lock cases, both at implicit locks

The fifth round in which this class has been found. `reserve_notification_email_change` reads the clock after its advisory lock but then UPDATEs `organizations`, which waits; `claim_negative_alert_send` — fixed in round 9 for the card row — then INSERTs a log row whose **foreign key** takes `FOR KEY SHARE` on `organizations`, which waits. Both were observed backdating a reservation by ~1,520 ms. Round 9's own comment claimed "every lock this decision depends on is now held". A foreign key is a lock.

**Fixed:** both acquire the organization row explicitly, in the mode the implicit operation would have used, *before* reading the clock. A documented lock order (`nfc_cards → locations → organizations`) keeps making them explicit from introducing deadlocks.

### R10-07 (P2) — public feedback's rate window used transaction-start time

`submit_feedback_atomic` waits for the card and location locks and then counts the last five minutes with `now()`, which is transaction-start time. Seeding twenty submissions aged 299 seconds and blocking a new one behind the card lock for 1.5 s produced a **VT003 rejection while the true trailing-five-minute count was zero**.

Lower impact than the billing findings, and the one case where the person who pays is a customer standing at a counter with no account and no way to report it. **Fixed:** both the decision and the inserted `created_at` use one instant read after every lock is held. The limit itself is unchanged; `clock_timestamp()` only moves forward, so this makes the limit more accurate, not more permissive.

### R10-08 (P2) — successful pagination could repeatedly expire the lease, invisibly

R9-07 gave reconciliation up to twenty sequential Stripe calls but left the 45-second lease unrenewed and never called `renew_reconciliation_lease` at all — which `DECISIONS.md` had claimed it did. A healthy three-page scan at 16 s per page took 48 s, lost the lease, had its write correctly rejected, and returned `deferred`. Every retry reproduces it identically, and the sweep reported **HTTP 200**.

**Fixed in two parts.** The service now renews between pages and stops immediately if renewal fails. And because "no work lost" and "someone has been told" are different guarantees, `get_billing_reconciliation_backlog` surfaces organizations dirty for over an hour — four consecutive sweeps — as an operator-visible failure, separately from per-run errors. Ordinary `deferred` contention stays non-fatal, which round 9 was right about.

### What I did not change, and why

- **`clear_reconciliation_dirty` checking owner but not expiry.** The reviewer recorded this as a contract inconsistency rather than a ninth defect, and demonstrated no entitlement failure from it. I added the expiry check anyway — "still the owner" and "still holds the lease" should not be different tests in different functions — but it fixes no reproduced bug.
- **The 20-page caps.** Failing closed rather than deciding entitlement from a prefix is still correct. What was missing was the operator procedure, now in `OPERATOR_RECOVERY.md`.

### Verification

- `npm run test` — **578/578** across 25 files.
- `scripts/verify-local-database.mjs` — **41 checks** against real PostgreSQL 17, all **44 migrations**, including the migration-17 upgrade path.
- Isolated Playwright suite — **190/190 passed, zero failed, zero skipped.**
- `npm run typecheck` / `lint` / `build` — clean.
- **8/8 mutation tests caught.** Each of the eight defects was re-introduced into the migration that fixes it and the harness re-run; every one fails, naming the specific check. A regression that has only ever been green proves nothing about the bug it names.
- Two harness checks had to be **inverted**, both of which had encoded a round-9 defect: one asserted that an `active` subscription evidences payment, the other that a freshly minted creation identity is retry-safe. The browser suite's activation test — already inverted once in round 9 — was rewritten again, this time against the requirement rather than any implementation's shape.

### The browser suite caught a regression I introduced

The round’s sharpest lesson, and it is about my own work rather than the review’s. Migration 40 had to re-create `claim_reconciliation_lease`, because its return type changed. Re-creating it silently dropped three behaviours that earlier rounds had added to the version being replaced, none of them related to the change being made:

  1. the **crash-gap dirty marking** (migration 33) — a successful claim must mark the organization dirty in the same statement, or a worker that dies between claiming and writing leaves no durable evidence that reconciliation is owed, and nothing ever selects it again;
  2. the **refused contender’s dirty marking** — an event arriving while somebody else holds the lease would be lost when the holder’s write cleared the flag;
  3. the **300-second lease cap**.

Typecheck passed. Lint passed. 578 unit tests passed. The 41-check real-PostgreSQL harness passed — none of them exercised those guarantees. Three hosted browser tests failed immediately. Restored by migration `20260910140000`, which is why this round ships five migrations rather than four.

Re-creating a function is not a mechanical operation: it discards everything previous rounds added to the version being replaced, and only a test written against the **guarantee** rather than the **change** notices.

**A second thing the suite surfaced:** `SUPABASE_DB_URL` was never propagated into the test process — `loadEnvVars` forwards only the three required credential keys — so every test needing a direct Postgres connection was silently skipping. That is **45 tests, including the entire 32-case RPC privilege matrix**, which is precisely what checks that new RPCs are `service_role`-only and that no function in the public schema is unaccounted for. They pass once the variable is set. Any previous “zero skipped” claim depended on it happening to be exported in the shell, and this round’s 190/190 was run with it set explicitly.

**Not re-run this round:** the Stripe test-mode lifecycle against real Stripe test mode. Listed in `LAUNCH_CHECKLIST.md`.

## Round 9: an independent review found 7 defects — all 7 confirmed, all 7 fixed (2026-09-09)

> **Superseded in part by round 10.** All seven findings below were valid. Two of the *fixes* were not: R9-02's anti-deadlock half (an `active` status setting `activated_at`) and R9-01's "not open means not payable" were both reversed above. The remaining five stand.

> **Superseded in part by round 10.** All seven findings below were valid. Two of the *fixes* were not: R9-02's anti-deadlock half (an `active` status setting `activated_at`) and R9-01's "not open means not payable" were both reversed above. The remaining five stand.

An independent reviewer was given `REVIEW_REQUEST_ROUND9.md` and the branch at `53b86e8`, and returned two P1 billing defects plus five P2s, each with a reproduction. **Every one was confirmed against this branch and fixed.** Nothing was rejected — where I had a disagreement it was about scope, not validity, and is recorded per finding below.

Three forward migrations: `20260909100000`, `20260909110000`, `20260909120000`. Production untouched.

### R9-01 (P1) — a failed expiration left two payable Checkout Sessions

On a plan switch the code expires the old Session before returning a replacement. If `expire()` threw and the follow-up `retrieve()` returned a still-open Session — or failed as well — control fell straight through to `releaseAttempt`, and the caller created a second Session. Both URLs remained payable, so a customer holding both links could complete both. The comment above the call promised the guarantee; the code never checked for it.

**Fixed:** an attempt may only be released once the Session is confirmed to have LEFT the open state. `open` is the only payable state, so the test is simply whether we observed it change; anything still open, or unknown because it could not be read, fails closed and keeps the attempt for the next request to re-check.

### R9-02 (P1) — activation and subscription refresh consumed each other's generations

The generation pair introduced last round counts REQUESTS, not KINDS OF WORK, and two different obligations shared it. The reviewer demonstrated both directions against real PostgreSQL: an invoice activation writing only `activated_at` marked a pending subscription refresh complete and left the row clean with a stale `past_due`; and a subscription sweep marked a pending activation complete with `activated_at` still null, so a previously-paying organization could keep a grandfathered grant it should have lost.

**Fixed in two parts.** Activation gets its own counter pair, so a writer may only advance the counter for work it actually performed and the dirty flag is the disjunction. And — so that part one cannot deadlock, since the sweep has no way to satisfy an activation obligation on its own — a subscription refresh that writes an `active` status now sets `activated_at` from that evidence. An `active` subscription means its first invoice was paid, which is exactly migration `20260907180000`'s own definition of the field, so this is the documented semantic rather than a workaround, and it needs no extra Stripe call.

### R9-03 (P2) — customer-key rotation could race an in-flight creation

Two requests can sit on opposite sides of the 23-hour retry-safe boundary. One replays the frozen key; the other, seeing a genuinely accurate empty list, rotates the identity and creates its own. Both succeed, and the database only stops the loser from overwriting the winner *after* both external objects exist. Accuracy was never the problem — a fresh list cannot prove a concurrent create will not land a moment later.

**Fixed:** customer creation now has the same operation lease the checkout attempt already had. A second caller is told to stand down, and rotation — the step that declares the old key dead — is refused unless the caller holds the live lease.

### R9-04 (P2) — the legacy backfill invented a key and called it retry-safe

`20260908120000` gave interrupted legacy rows a RANDOM creation identity dated from `coalesce(checkout_created_at, created_at)`, which for a recent organization reads as retry-safe. But the previous billing version used a DETERMINISTIC key, `customer-create:org-ID` — so "replaying" the random replacement was not a replay at all, and Stripe would not deduplicate it. The backfill also skipped rows with no `checkout_attempt_id`, even though commit `241572f` resolved the customer *before* claiming an attempt, so an interruption there leaves exactly that shape.

**Fixed:** every unresolved row is stamped with the sentinel `legacy-org-<id>`, which the application maps back to the original org-derived key, and dated from the organization's own `created_at` — the earliest moment a Customer could have existed for it. Both branches are then sound: recent rows replay the real legacy key, older ones go through the canonical enumeration over an honestly-bounded window.

### R9-05 (P2) — two notification paths still read the clock before the last lock

`confirm_notification_email_change` compared against `now()` (transaction-start), and the reviewer accepted an expired token by holding the row lock until it expired. `claim_negative_alert_send` captured `clock_timestamp()` after its *advisory* lock but before the card ROW lock the UPDATE takes, and recorded a reservation 1,521 ms before the lock was released — backdating the cooldown.

**Fixed:** both now acquire every lock the decision depends on, then read the clock, then decide. This is the fifth instance of this class in this project, and each previous round's class-wide search missed the next one. The reason is visible here: neither case looks wrong at the statement that is wrong — one uses the correct function against the wrong instant, the other takes the deciding lock implicitly. The rule worth carrying forward is stated in the migration header: **a lock taken implicitly by the deciding statement is still a lock, and it is the easiest one to miss.**

### R9-06 (P2) — the monitored sweep reported success when every reconciliation failed

The route returned 200 unconditionally, and the workflow's `curl -fsS` reads only the status. A run in which Stripe was unreachable and every organization errored left the job green. No work was lost — the dirty flags are durable — but "no work lost" and "the operator has been told" are different guarantees, and only the first held.

**Fixed:** genuine errors return 500. `deferred` deliberately does not: lease contention is normal and self-correcting, so ordinary concurrency cannot turn into a page.

### R9-07 (P2) — reconciliation treated the first 100 subscriptions as the whole history

`subscriptions.list` was called once with `limit: 100` and `has_more` was ignored, so an older but still ACTIVE subscription behind 100 newer terminal ones would never be seen and entitlement would be written as canceled.

**Fixed:** the history is paged through to completion. Exhausting a 20-page cap is NOT treated as "this is the whole set" — it fails the reconciliation, leaving the organization dirty for the sweep rather than deciding entitlement from a prefix.

### On the reviewer's documentation ledger

Several entries are fair and are now corrected in place: the `getOrCreateStripeCustomerId` comment claiming Search is the source of truth (it is not, and has not been since the enumeration landed), the "registered before any Stripe call" wording (the webhook's identity-resolution retrieval precedes the shared reconciler), and the claim that unrecorded Sessions have expired after 23 hours (that conflates attempt age with Session expiry — Stripe's default is 24 hours from Session creation, and this app sets no explicit `expires_at`).

The ledger's broader point stands and is worth keeping: **this repository can verify its own consistency, not the state of external accounts.** Where a claim is about a Vercel, Supabase or Stripe setting, it is reported evidence unless someone re-read it that day.

### Verification

- `npm run test` — **572/572** across 26 files.
- `scripts/verify-local-database.mjs` — **34 checks** against real PostgreSQL 17, all 39 migrations.
- `npm run typecheck` / `lint` / `build` — clean.
- Full isolated browser suite — **187/187 passed, zero failed, zero skipped**. One pre-existing test in that suite had to be inverted: it asserted that an activation write advanced `billing_sync_completed`, i.e. it encoded the very coupling R9-02 identified as the defect. It now asserts the opposite.
- **Every one of the seven fixes is mutation-tested**: each regression was run against the pre-fix behaviour and fails there, then passes against the fix. A test that has only ever been green proves nothing about the bug it names.

## Fifth round, part three: a second, stricter pass on the same reasoning, and the rest of the lifecycle (2026-09-08)

### Verification for this part

- `npm run test` — **557/557** across 24 files (+4: the empty-search, failed-search, found-by-enumeration, incomplete-enumeration, window-bounding and no-recorded-time cases).
- `npm run typecheck` / `npm run lint` / `npm run build` — clean.
- `node scripts/verify-local-database.mjs` — **29 checks** against real PostgreSQL 17.
- Full isolated browser suite — **186/186, zero failed, zero skipped**.
- Both new regressions mutation-tested: reverting to "empty search authorizes creation" fails 5 tests; treating an incomplete enumeration as absent fails the page-cap test.


### The customer-recovery fix was still resting on an inference

Part two replaced "trust the search" with "trust the search unless the attempt is old" — and that second version still authorized creating a chargeable Stripe object on the strength of an empty search result. The justification was that an existing Customer "would have been indexed by now," from Stripe's statement that propagation runs "up to an hour behind **during outages**." That is a description of typical behaviour with no stated upper bound, not a guarantee, and it was being used to license exactly the irreversible action it could not license.

**The asymmetry that does hold.** Search's documented weakness is staleness, so it can only produce FALSE NEGATIVES — it may miss a Customer that exists, but will not invent one that doesn't. So a positive search result is proof; an empty one proves nothing at any age. `customers.list` is the canonical read: no consistency caveat, a `created` interval filter, and pagination. A COMPLETED enumeration of the bounded window in which an attempt could have created anything is therefore a negative that means something.

**Now:** creation is authorized only by that completed enumeration. Anything that prevents it finishing — an API error, or more pages than the cap — is `unknown`, and the attempt is left **pending**, with its identity and recorded timestamp untouched, for a later request to re-check. Nothing is created and nothing is rotated on an ambiguous outcome. A failed search no longer blocks a decidable case either, because the enumeration decides.

**Verified against real Stripe, not argued:** a Customer created and then immediately searched for returned **zero results**, while `list({created:{gte}})` found it on the first page — precisely the false negative the previous version would have turned into a duplicate. Both regressions were mutation-tested: reverting to "empty search authorizes creation" fails 5 tests, and treating an incomplete enumeration as absent fails the page-cap test.

### The rest of the payment lifecycle

The initial monthly purchase (part two) was one point on a curve. Everything after it is now verified too, via Stripe test clocks — renewals and dunning cannot be driven by clicking:

| Phase | Result |
|---|---|
| **Annual checkout** | Real Session on the yearly price through the app's own Server Action; `active`, `current_period_end` exactly **365 days** out, generations 5/5 |
| **Renewal** | Clock +1 month; charged, still `active`, period rolled forward |
| **Failed payment** | Failing card + clock advance; lands in `past_due` — recoverable, not terminal |
| **Recovery** | Good card restored, open invoice paid; back to `active` |
| **Cancellation** | Entitlement revoked to `canceled` |
| **Resubscription** | Restored, and pointed at the NEW subscription id |
| **Convergence** | `requested == completed`, `needs_reconciliation = false` |
| **`activated_at`** | Set once, never rewritten across any of the above |

**Two harness properties that produced misleading runs before being understood**, both worth recording because they will mislead the next person too:

1. A webhook whose reconciliation defers returns **HTTP 500 on purpose**, so Stripe redelivers it. `stripe listen` does **not** redeliver on 5xx — so a local run loses exactly the retries production makes, and phases pass or fail run to run purely on which event lost a lease race. Early runs scored 6/8, 7/10, 10/10, 8/10 for this reason alone. Driving the sweep from the harness restores the half of the contract the CLI cannot model.
2. Linking the organization to its Stripe customer *before* creating a subscription made the script emit `customer.created` / `payment_method.attached` / `customer.updated` against a subscription-less organization. A reconciler then correctly recorded "nothing to reconcile" and marked the generation satisfied — a true answer about a state the real product never produces, since its customer is created inside checkout with a subscription following immediately.

Neither was an application defect, and chasing the first as though it were would have been wasted work.

### Auth email delivery, now measured against a real mailbox

Against the isolated project, with a disposable `mail.tm` address polled over its API:

- `auth.signUp` → **429 `over_email_send_rate_limit`**
- `auth.resend` → accepted, no error, **nothing arrived** in 120 s
- `auth.resetPasswordForEmail` → accepted, no error, **nothing arrived** in 120 s

"Accepted then never delivered" to an ordinary external address is the signature of Supabase's built-in mailer, which delivers only to project team members and caps sends at a few per hour. Two independent signals agree, and the practical conclusion does not depend on choosing between them: **on this configuration a normal customer's confirmation and reset emails do not arrive.** An API acceptance response cannot distinguish queued-and-delivered from queued-and-dropped — only a mailbox can, which is why this test now uses one.

Production's own configuration is a separate project and still cannot be read from here: `supabase projects list` fails with `LegacyPlatformAuthRequiredError`, and no `SUPABASE_ACCESS_TOKEN` is set in this environment.

## Fifth round, part two: verifying the integration against Stripe's actual documented behaviour, and the real test-mode lifecycle (2026-09-08)

### A claim this project made about Stripe was wrong, and it was hiding a real duplicate

The integration entry below records a decision not to take the parallel implementation's frozen customer-creation identity, on the grounds that this repo's Stripe metadata search was "authoritative even after the key has expired" and therefore "strictly stronger." Checked against Stripe's own reference, both halves of that are false:

- *"Don't use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages."* — `docs.stripe.com/api/customers/search`
- *"You can remove keys from the system automatically after they're at least 24 hours old. We generate a new request if a key is reused after the original is pruned."* — `docs.stripe.com/api/idempotent_requests`

The search was being used in precisely the read-after-write flow Stripe rules out, and the key genuinely does expire. Worse, `getOrCreateStripeCustomerId` caught a **failed** search, logged "proceeding to create", and created — treating no-information identically to a negative result. The concrete duplicate:

1. `customers.create()` succeeds at Stripe; the response, or the database write that would record it, is lost. Nothing local records that `cus_X` exists.
2. 25 hours later the owner retries. The search fails (an incident, a timeout, a rate limit) — or is still lagging.
3. The code creates again. The key was pruned at 24 hours, so Stripe generates a new request: `cus_Y`.

Reconciliation only ever lists subscriptions for the customer persisted locally, so a customer who then pays on the other one is **charged and stays locked out of the dashboard** — the worst failure this product has.

**Fixed** by migration `20260908120000`, which restores the mechanism this round had rejected, on the merits: the two are complementary, not redundant. The frozen key is authoritative from 0 to ~24 hours (where search may lag); search is authoritative from ~1 hour onward (where the key may be pruned); and knowing which regime you are in requires a durable record of *when* the creation was attempted. Inside the retry-safe window the code now replays the frozen key and does not search at all — removing the read-after-write use of search entirely rather than making it more careful. Outside it, the search must SUCCEED; a failure fails the checkout closed. An empty result is trusted only because the recorded attempt is by then older than both the key lifetime and Stripe's documented worst-case propagation delay, with ~22 hours of margin.

**Regression test, verified both ways** (`features/billing/actions.test.ts`): the new test asserting that a failed search must not license a create fails against the old swallow-and-create body and passes against the fix. Four more cover the retry-safe replay, the successful-empty rotation, a lost rotation race, and a superseded record. Four further checks in `scripts/verify-local-database.mjs` prove the identity semantics against real Postgres.

### Real Stripe test-mode lifecycle, against the newly integrated flow

Isolated Supabase project, Stripe test-mode keys, `stripe listen` forwarding. A seeded organization, a real browser session, a real redirect to Stripe's hosted Checkout, payment with `4242 4242 4242 4242`:

- **14 signed webhook events, all HTTP 200.** Final state: `status: active`, real customer and subscription ids, `activated_at` set, `current_period_end` one month out, lease released, `needs_reconciliation: false`, every `checkout_*` column cleared.
- **`billing_sync_requested` = `billing_sync_completed` = 3** — the new generation pair converged rather than leaving phantom outstanding work.
- Mid-flight the row showed exactly the intended new shape: `checkout_attempt_id` and `checkout_request` present, `checkout_owner_token` **null** — the operation had finished while the attempt deliberately outlived it.
- Stripe's hosted page rendered **HUF 5,990.00** for `unit_amount: 599000`, confirming the `plan.amountHuf * 100` fix against real Stripe data.
- Exactly **one** Stripe Customer carried the organization's id afterwards, with exactly one subscription.

The three claims about Stripe's own behaviour that the duplicate-prevention design rests on were verified directly, not assumed: replaying an attempt's key with identical parameters returns the **same** Session; replaying it with **different** parameters is rejected (`StripeIdempotencyError`, which is exactly why `checkout_request` must be a stored snapshot rather than rebuilt); a different attempt id yields a new Session. All test data was cleaned up afterwards (subscription cancelled, customers deleted, organization and users removed).

One user-visible bug found by actually looking at the result: the billing page rendered "2026. október 8.." — a Hungarian formatted date already ends in a period, and the sentence added another. Fixed.

### Supabase Auth recovery: proven except the one hop that matters most

Recovery and resend-confirmation go through **Supabase Auth's own mailer**, not this app's Resend integration — a distinction that is easy to miss and that makes the verified `velemenytap.hu` Resend domain irrelevant to them unless it is also wired in as Auth's custom SMTP.

Verified end to end against the real isolated project: the form renders, the Server Action calls Auth with the correct `redirectTo`, Auth mints a valid recovery token honouring that target, `/auth/callback` completes it, and `/auth/reset-password` renders the set-new-password form with a real session cookie established. An invalid or already-used link degrades to `/auth/auth-code-error`, not to anywhere authenticated.

**A real fragility found doing this.** The first recovery link generated came back in the *implicit* shape (`#access_token=…`), and a fragment is never sent to the server, so `/auth/callback` — which read only `?code=` — dead-ended on the error page. Which shape Supabase sends depends on the project's email templates and on whether the generating request registered a PKCE challenge, neither of which this app controls; and the app already had a *second* route (`/auth/confirm`) handling the `token_hash` shape, which recovery did not point at. `/auth/callback` now accepts both server-readable shapes, verified against a real Supabase-minted `token_hash` link (307 → `/auth/reset-password`, session cookie set) and pinned by six unit tests in `app/auth/callback/route.test.ts`.

**Not verified: the SMTP hop.** Two sends against the isolated project returned `over_email_send_rate_limit` — the signature of Supabase's built-in mailer (a few messages per hour, deliverable only to project team members) rather than configured custom SMTP. There is no management token here to read the projects' Auth settings, and no mailbox to receive a real message.

### Verification for this part

- `npm run test` — **553/553** across 24 files (a new `app/auth/callback/route.test.ts`, plus five new customer-creation tests replacing three that encoded the old behaviour — one of which had been asserting the bug).
- `npm run typecheck` / `npm run lint` / `npm run build` — clean.
- `node scripts/verify-local-database.mjs` — **29 checks** against real PostgreSQL 17 (up from 25; four new ones prove the creation-identity semantics: concurrent claims share one frozen identity, rotation is refused inside the retry-safe window, a retired identity cannot record, a resolved organization hands out no further identity and cannot be repointed at a second customer).
- Full isolated browser suite — **186/186 passed, zero failed, zero skipped** (7.2 min, `--workers=1`, `SUPABASE_DB_URL` exported).
- Migration `20260908120000` applied to the isolated project; 36 local migrations match 36 remote, zero drift.

### A scare worth recording: eight failures that were not a regression

A full suite run came back with 8 failures including **all five `review-gating` tests** -- nominally the product's single most important invariant breaking. It was not a regression. Playwright's `reuseExistingServer` had adopted a leftover dev server on port 3000 whose compiler worker had died, so every page returned a 500; the port check only asks whether *something* answers, not whether it is healthy. Killing the stale process and re-running the same specs passed 12/12 in 37 seconds.

Recorded because the failure mode is genuinely misleading -- a broad, alarming failure set across unrelated public-flow specs, with no error in the app's own code -- and because the instinct to start patching the feedback flow would have been entirely wrong. `e2e/README.md` now carries the check to run first.

### Launch blockers

The actionable form of this list, with who has to do each step, is `LAUNCH_CHECKLIST.md`; the non-engineering decisions are in `BUSINESS_DECISIONS.md`. Both are kept short on purpose. What follows is the reasoning behind them.

Ordered by what stops a paying customer from succeeding.

1. **Supabase Auth email — TWO faults, both now fixed and verified on the isolated project, neither yet applied to production.** With the CLI authenticated the real configuration could finally be read instead of inferred.

   **Fault A: no custom SMTP on either project.** The built-in mailer, capped at 2 emails/hour and documented as delivering only to project team members. External sends were accepted with no error and never arrived.

   **Fault B: the email templates.** Both projects use `{{ .ConfirmationURL }}`, which redirects with the session in a URL **fragment** — never sent to the server, so `/auth/confirm` and `/auth/callback` both fell through to `/auth/auth-code-error` **even though the token was consumed correctly and the account genuinely became confirmed**. This app has always implemented the `token_hash` flow; the templates never matched the app written for them. Fixing only Fault A would have produced emails that arrive and links that still fail — which is exactly what the first verification run showed.

   Both fixed on the isolated project and verified with a real mailbox, receiving and clicking every link: signup → `/onboarding`, resend (on a deliberately unconfirmed account) → `/onboarding`, password reset → `/auth/reset-password` with a real session cookie. `LAUNCH_CHECKLIST.md` § 1 carries the exact production settings and template strings.

   Superseded note, kept for the record — the earlier framing of this blocker:

   > **Supabase Auth custom SMTP.** Owner action, and no longer a suspicion: against the isolated project, `resend` and `resetPasswordForEmail` were both **accepted with no error while nothing arrived** at a real external mailbox, and `signUp` hit the project-wide hourly cap. That is the built-in mailer, which delivers only to project team members. Production is a separate project whose setting still cannot be read from here (`supabase projects list` → `LegacyPlatformAuthRequiredError`, no `SUPABASE_ACCESS_TOKEN` set), so it must be checked, and configured if unset. Resend is already an account here with a verified `velemenytap.hu` domain, so this is configuration, not vendor selection. **Verification once configured:** one real reset email to a mailbox you control, clicked — it should land on `/auth/reset-password`, not `/auth/auth-code-error`. That single click also settles the link-shape question above.
2. **Stripe production configuration.** Live-mode Product and Prices, the webhook endpoint and its signing secret, and `RECONCILE_SWEEP_SECRET` plus the matching GitHub Actions secret and `PRODUCTION_RECONCILE_SWEEP_URL` for the sweep. None are set. `assertStripeConfigurationValid` fails closed, so checkout stays unavailable rather than misbehaving until they are.
3. **The billing migration set has never been applied to production.** Production is still on migration 17. The rollout manifest in `DEPLOYMENT.md` § 7 lists all of `20260907150000`–`20260908120000` as `--expand`; the reasoning that makes the signature-changing ones safe depends on none of them having been deployed yet, and expires the moment any of them is.
4. **A real live-mode transaction has never been made.** Test mode is now genuinely proven end to end, but live mode has its own Product, Prices, keys and webhook endpoint, and the `livemode` guard is deliberately strict.

5. **`RECONCILE_SWEEP_SECRET` is load-bearing, not optional.** The lifecycle work made this concrete rather than theoretical. A webhook whose reconciliation defers returns HTTP 500 so Stripe redelivers it, and the durable dirty flag plus the scheduled sweep are the guarantee underneath — but with the secret unset the endpoint answers **503** and that backstop does not exist. A burst of renewal or resubscription events can then leave entitlement stale with no page load to self-heal it, since renewals involve no customer visit. Set the Vercel env var, the matching GitHub Actions secret, and `PRODUCTION_RECONCILE_SWEEP_URL` together, and confirm one run returns 200.

Pending owner confirmation, deliberately not decided here:

- **Invoicing.** Stripe issues receipts, not Hungarian-compliant invoices. Whether that needs a Számlázz.hu/Billingo integration, and on what legal basis, is a business decision, not a code one.
- **The physical NFC card price.** The parallel implementation's marketing copy stated 6,990 Ft. That number has no source anywhere in this repo and the card is sold through a separate storefront, so the pricing section was ported without it. The homepage renders subscription prices from `PLAN_PRICING` and says the card is ordered separately; it states no card price at all until you confirm one.

Also still open and unchanged: the business, legal and support pages (imprint, terms, privacy policy, support contact) that a Hungarian SaaS needs before taking real money.

## Fifth round: independent review of a parallel implementation, then integration of its checkout-identity and reconciliation-completeness models (2026-09-08, on `feature/billing-subscriptions`)

A second agent built its own product-fix pass against a copy of this repo. This round (a) reviewed that work independently rather than trusting its report, and (b) integrated the parts of it that were genuinely better than what is here, onto this repo's own design, via new forward migrations — never by reverting or replacing this implementation wholesale.

### Step 1 — what the independent review actually established

**Re-ran their verification rather than reading their logs for it.** Their claimed 609 automated tests, clean TypeScript and clean lint all reproduced exactly against their working copy. Their 20 real-PostgreSQL checks were **not** re-run at review time (their cluster was stopped and no PostgreSQL was installed here yet) — their log was read instead, and that distinction was stated rather than glossed. It stopped mattering later in this round: this repo now has its own adapted harness and its own 25 checks, run for real (below).

**Proved the migration incompatibility instead of asserting it.** Their two billing migrations cannot be applied on top of this repo's history, and the reason is concrete, not stylistic: this repo's `20260907210000` drops `billing_sync_seq` at line 58, and their `20260907220000` references that column four times. Their `drop function` statements also omit `if exists` (they would error against this schema's state), and their `claim_checkout_attempt` changes signature from `(bigint,text,text,text,int)` to `(bigint,text,text,jsonb)`. So "apply their patch" was never an option; only integration was.

**Corrected one of my own suspicions rather than reporting it as a finding.** Their `claim_checkout_attempt` appeared to return the OLD interval and price on takeover, ignoring a newly-requested plan. Tracing their `checkout.ts` (lines 299-310) showed the application detects that mismatch, expires the old Stripe Session (verifying `expired.status === "expired"`), releases the attempt and retries. Not a bug; reported as not-a-bug.

**Confirmed independent agreement on the HUF bug.** Both implementations arrived separately at `plan.amountHuf * 100` — two independent derivations of the same fix is meaningfully stronger evidence about Stripe's HUF minor-unit convention than either one alone.

**Flagged an unverifiable business claim, and did not publish it.** Their marketing copy states a 6,990 Ft price for the physical NFC card. That number has no source anywhere in this repo — the card is sold on a separate, password-protected storefront. The pricing section was ported; the number was not. `PRODUCT_SPEC.md` now records why.

**Their own remaining gate #3 ("configure a more frequent supported scheduler") was already closed here.** Their daily `0 3 * * *` Vercel cron was a workaround for the Hobby plan's once-a-day cap; this repo's GitHub Actions sweep is not subject to that cap and already runs every 15 minutes.

### Step 2a — the product fixes brought over (15 new files, plus their integration points)

Complete features, not file lists: self-service account recovery (`features/auth/recovery-actions.ts`/`recovery-form.tsx`/`resend-confirmation-form.tsx` + `app/auth/forgot-password`/`reset-password`/`callback`, wired into the login form, the check-email screen and the auth-error page), `lib/google-review-url.ts` (applied at all four places a stored Google URL is read), `features/feedback/filter-params.ts` (replacing the feedback page's own inline cursor parser and now shared with the filter bar), error boundaries (`app/error.tsx`, `app/dashboard/error.tsx`, `components/page-error.tsx`), a status toggle with a `NEXT_REDIRECT` rethrow guard, a copy-URL fallback for browsers that refuse the clipboard API, read-error handling on every dashboard page that previously rendered an empty state on a failed query, a first-run setup guide on the dashboard overview, and homepage/pricing copy. Their tests came with them (`recovery-actions.test.ts`, `google-review-url.test.ts`, `filter-params.test.ts`, `status-actions.test.ts`, `current.test.ts`), normalized to this repo's semicolon style rather than importing a second formatting convention. The settings form's `logo_url` field was removed — nothing in the product ever rendered a logo, so it was an input promising branding the product does not deliver; stored values were left untouched, not deleted.

### Step 2b — the two billing mechanisms integrated, via forward migrations only

Migrations `20260908100000_checkout_request_identity.sql` and `20260908110000_billing_reconciliation_generations.sql`. Nothing was reverted, no database was reset, and this repo's exclusive-lease concurrency model, GitHub Actions scheduler and test infrastructure were all kept. Both mechanisms address real defects that this implementation genuinely had:

1. **Checkout attempt identity was destroyed by lease expiry.** `checkout_attempt_expires_at` meant both "this request owns the slot" and "this attempt exists," so a request that died after `sessions.create()` returned but before `record_checkout_session` landed lost the attempt id — and the next request minted a fresh one, hence a fresh idempotency key, and Stripe created a genuine SECOND Checkout Session. Split into a durable attempt (`checkout_attempt_id`) and a short operation lease over it (`checkout_owner_token`), with `finish_checkout_operation` ending the operation while keeping the attempt. Plus `checkout_request` (the immutable snapshot replayed verbatim, since Stripe rejects rather than deduplicates a retry whose parameters differ under the same key) and `checkout_created_at` (backing a `retry_safe` result, since Stripe's key retention is roughly 24 hours, not permanent). `record_checkout_session` is now called before the Session is validated at all.
2. **An event arriving DURING a reconciliation was silently lost.** The lease gave real mutual exclusion and, since `20260907230000`, durable recovery of an abandoned reconciliation — but a *completed* one cleared `needs_reconciliation` unconditionally, including the flag a newer webhook had set moments earlier after failing to claim the lease. Fixed with a monotonic generation pair (`billing_sync_requested`/`billing_sync_completed`): every entry point commits its request before any Stripe call, the claim reports the generation it observed, and a completing write may clear the flag only if nothing newer has been requested since. Additionally, `get_billing_reconciliation_candidates` gives the sweep a staleness trigger that does not depend on this app ever having been told anything — the only mechanism that recovers a webhook which was never delivered at all.

**Deliberately not taken:** their `claim_stripe_customer`/`record_stripe_customer` pair. This repo already searches Stripe itself for a customer carrying the organization's id in its metadata, which is authoritative even after an idempotency key has expired — strictly stronger than freezing a creation key, and adding both would mean two mechanisms for one invariant. Also not taken: their daily Vercel cron (see step 1).

### A real bug found by the new harness, not by reading the code

The first draft of both migrations had a genuine defect, and so did the already-applied `20260907210000` it was modelled on. `write_reconciliation_result`'s `UPDATE ... WHERE reconciliation_lease_expires_at > clock_timestamp()` looks immune to clock problems — but Postgres evaluates that qual during the scan, i.e. *before* waiting behind another transaction's row lock, and re-checks it afterwards only if the blocking transaction actually modified the row. A plain `SELECT ... FOR UPDATE` holder leaves the tuple unchanged, so nothing is re-evaluated and the write lands with a lease that expired while it waited. Fixed by taking the row lock explicitly before every lease-timed `UPDATE` (`write_reconciliation_result`, `write_activation`, `clear_reconciliation_dirty`, `fail_billing_reconciliation`, `renew_reconciliation_lease`, `record_checkout_session`, `renew_checkout_attempt`).

This is a fourth instance of the `now()`-after-lock class this project has now hit four times (R6-04, R7-05, R7-05's own postscript, and this) — and the first one that source-level searching could not have found, because the SQL was already correct and only its *execution order* was wrong. See `DECISIONS.md`'s second postscript on that entry.

### Verification — what passed, and what is still unverified

**Passed here, run for real:**

- `npm run test` — **543/543** across 23 files (up from 459/18): 10 new billing tests covering request replay on takeover, refusal of a stale stored request, release-and-replace outside the retention window, record-before-validate, finish-not-release on success, request-before-claim ordering, and the observed generation reaching every write path.
- `npm run typecheck` and `npm run lint` — clean.
- `node scripts/verify-local-database.mjs` — **25 checks** against a real, disposable local PostgreSQL 17 cluster, including all 35 migrations replaying from empty, an organization created at migration 17 surviving the rest with its grandfathering intact, the identity/immutable-request/generation guarantees exercised through genuinely concurrent connections, and the lock-wait check that found the bug above. The harness is adapted from the parallel implementation's own, re-pointed at this repo's migration history and RPC names.
- The full isolated browser suite — **183/183 passed, zero failed, zero skipped** (20 spec files, 7.0 min, `--workers=1`), with `SUPABASE_DB_URL` exported so `rpc-privilege-matrix.spec.ts` and `location-deactivation-race.spec.ts` genuinely ran rather than silently skipping. `billing-concurrency.spec.ts` was rewritten for the new contracts and grown to 15 tests; `rpc-privilege-matrix.spec.ts` was updated for eight changed and four new RPC signatures. Both migrations applied to the isolated project with `supabase db push`; `supabase migration list` shows 35 local matching 35 remote, zero drift.
- `npm run build` — clean, 24 routes (`/auth/forgot-password`, `/auth/reset-password`, `/auth/callback` new).

**One thing worth knowing about that suite run:** at Playwright's default parallelism it does NOT come back clean, and the failures are misleading. The isolated project's Supabase Auth endpoint rate-limits partway through, and 15-24 specs fail with `AuthApiError: Request rate limit reached` on a rotating set that changes between runs. Re-running those same specs serially passes them (66/66, then 183/183 for the whole suite). This is an environment limit, not a regression — but a shifting failure set is exactly the shape a real intermittent bug has, so `e2e/README.md` now documents the distinction: rotating set + that specific error means re-run serially; the same spec twice, or any other error, is real.

**Still needs external verification, unchanged from the previous round and not claimed here:**

- A real Stripe test-mode lifecycle against the *new* checkout flow. The previous round's manual pass is still the only real Stripe evidence, and it exercised the pre-integration code. The mechanisms changed underneath it; the unit and PostgreSQL evidence is genuine but is not a substitute for a real Session created, taken over and completed.
- Email delivery (Resend) for the newly ported recovery and resend-confirmation flows — the code paths are tested, the actual deliverability is not.
- Invoicing, and the business/legal/support details the parallel implementation also left open.
- The 6,990 Ft physical-card price, deliberately unpublished until it has a source.

**Production is untouched.** No deploy, no production migration. Production still runs migration 17 with no Stripe environment variables at all, which is also why the two new migrations carry no coordinated-switch risk there: the interfaces they replace have never been deployed.

Previously, and still true of everything below: a FOURTH independent review found the third round's own 9-finding response ("Third independent review of the integration round," below) still had 14 further defects, including one — the third round's own `billing_sync_seq` fencing-token design — that this round's own adversarial self-review found was invalid in principle, not merely improvable, with its own "proof" (a barrier-controlled test) never actually exercising the scenario it claimed to. Branch `feature/billing-subscriptions`, PR #4 (`github.com/lbenyok/velemenytap/pull/4`), still open, still not merged to `master`.

## Fourth independent review of the integration round: 14 findings, plus a genuine bug found only by actually completing the mandated real Stripe verification (2026-09-08, on `feature/billing-subscriptions`, not yet merged to `master`)

A fourth independent review found 14 further defects in the third round's own response — 4 HIGH, the rest MEDIUM/LOW — including, unusually for this project's history so far, a finding that the third round's own *verification* of one of its central claims was itself invalid, not just its design. All 14 fixed and tested; a further, genuinely new, previously-undiscovered bug was also found and fixed while completing this round's own mandatory real Stripe test-mode verification step (documented separately below, since it was not one of the 14 findings — it was found only by actually doing the verification the review required, not by reading the code).

1. **HIGH — the checkout-attempt "mint a fresh attempt" branch left a stale session pointer paired with fresh plan metadata.** `claim_checkout_attempt`'s prior body updated `checkout_attempt_interval`/`checkout_attempt_price_id`/`checkout_attempt_mode` when minting a fresh attempt, but left `pending_checkout_session_id` pointing at the SUPERSEDED attempt's own session — a subsequent reader's own fresh claim could observe an internally-inconsistent row (new interval, old session id) and hand back the wrong plan's Checkout URL. Fixed by clearing `pending_checkout_session_id` in the same `UPDATE` that mints the fresh attempt (migration `20260907210000`, `create or replace`d in place — same name/signature, every existing caller keeps working unchanged). `features/billing/actions.ts`'s `reconcileExistingSession` additionally verifies a reused session's own actual Stripe-side line-item price directly, never trusting this row's bookkeeping alone, as defense in depth independent of this fix. See `DATABASE_SCHEMA.md`'s migration-30 entry and `DECISIONS.md`'s "Checkout concurrency" entry.
2. **A superseded but still-`open` Checkout Session was never explicitly closed on Stripe's own side.** A customer holding two tabs/links (an abandoned monthly attempt, then a fresh yearly one) could still complete the OLD session even after a newer attempt superseded it. Fixed: `reconcileExistingSession` now calls `stripe.checkout.sessions.expire()` on a superseded open session before ever returning a different session's URL, with a race-safe re-check (the old session completing WHILE the expire() call is in flight is treated as a real success to reconcile, not swallowed, since Stripe's own `expire()` call fails once a session has already completed).
3. **HIGH — the prior round's own `billing_sync_seq` fencing-token design was invalid in principle, not merely improvable, and its own "proof" never actually tested what it claimed to.** An independent adversarial review demonstrated directly that a request claiming an EARLIER sequence number can still execute — and observe genuinely fresher Stripe state — AFTER a request that claimed a LATER sequence number has already written, and claim-order fencing discards that fresher read purely because of claim order, with nothing left to ever correct it. Worse, this round's own adversarial self-review of the design being replaced found the third round's "barrier-controlled test forcing reverse completion order" (`app/api/webhooks/stripe/route.test.ts`'s then-"Finding 4" test) never actually forced that: it stalled request A inside its mock of `stripe.subscriptions.retrieve()`, which the real code of that era called BEFORE `claim_billing_sync`, not after — so request A never actually held an earlier-claimed sequence number before the barrier released it; the test's assertions were hand-configured mock results matching the narrative it was written to demonstrate, not driven by what the constructed scenario would actually make a real database return. **Replaced entirely** with an exclusive, expiring, durable reconciliation lease — `claim_reconciliation_lease`/`renew_reconciliation_lease`/`write_reconciliation_result`/`write_activation`/`release_reconciliation_lease` (migration `20260907210000`, dropping `billing_sync_seq`/`claim_billing_sync` outright) — claimed BEFORE any Stripe network call, so there is no window where two readers can observe Stripe state concurrently and race to write at all: mutual exclusion, not fencing after the fact. A contender that fails to claim the lease, or loses it before writing, never assumes someone else's write already covers it — `needs_reconciliation`/`reconciliation_dirty_since` are set durably, in the same statement, by the database function itself. See `DECISIONS.md`'s "Stripe webhook reconciliation" entry for the full corrected record, and `e2e/billing-concurrency.spec.ts` (below) for this round's own concurrency proof, run against the real isolated project instead of a hand-configured mock, specifically so this exact mistake cannot repeat.
4. **Duplicate-active-subscription and unapproved-subscription-price detection had nowhere durable to record what it found.** A new table, `private.billing_anomalies` (migration `20260907210000`; `record_billing_anomaly()` RPC, migration `20260907220000`, since `private`-schema tables aren't exposed to PostgREST at all) — RLS enabled, `service_role`-only, the same pattern as `private.alert_email_log`/`private.notification_email_change_log` — now records every anomaly `features/billing/reconcile.ts`'s `pickCurrentSubscription` and the webhook's customer-mismatch checks find, rather than only a `console.error` line nothing keeps.
5. **"Create one shared, idempotent reconciliation service used by: subscription webhooks; the verified Checkout-success return; an authenticated administrator repair path; and a monitored scheduled reconciliation mechanism" — not previously true.** `features/billing/reconcile.ts` (new) — `reconcileOrganizationBilling`/`activateOrganizationBilling` — is now the ONE implementation of "re-derive this organization's entitlement from Stripe," called identically by the webhook route, the billing page's own checkout-success handler (now self-heals immediately on return from Stripe instead of only waiting for a webhook that Finding 5 itself established is not guaranteed to arrive), a new self-service admin resync action (`features/billing/admin-actions.ts`, new — a "Frissítés a Stripe alapján" button, restricted to `canManageBilling` roles), and a new scheduled sweep (`app/api/admin/reconcile-billing-sweep`, new — `POST`, bearer-token authenticated via `RECONCILE_SWEEP_SECRET`, scans `organization_billing` where `needs_reconciliation = true` limit 200, reconciles each, triggered every 15 minutes by a new GitHub Actions workflow at the repo root, `.github/workflows/reconcile-billing-sweep.yml`, sibling to `velemenytap/`). This closes the third round's own "a genuinely missed reconciliation is not auto-corrected by anything today" gap, previously left open pending production volume becoming a concern — it no longer needs volume to become a concern first. `RECONCILE_SWEEP_SECRET` (a new production Vercel env var), a matching GitHub Actions repository secret of the same name, and a `PRODUCTION_RECONCILE_SWEEP_URL` variable are all required for the sweep to actually run against production and are NOT yet configured — owner actions, see "What's needed from the user" below and `DEPLOYMENT.md`.
6. **HIGH — the environment/livemode fail-closed check itself failed open.** `resolveExpectedLivemode()`'s prior design enforced only when `APP_ENV` resolved to exactly `"production"`/`"preview"`, silently skipping enforcement for anything else — including `APP_ENV` genuinely unset, empty, or misspelled in a real deployed runtime, precisely the misconfiguration this check exists to catch. Fixed by inverting the logic to fail closed: only `NODE_ENV === "development"` (the same signal `/api/health` already uses) skips enforcement now; every other runtime must resolve `APP_ENV` to exactly `"production"`/`"preview"` or the function throws, and both the webhook route and `assertStripeConfigurationValid()` refuse to proceed on that thrown error. See `DECISIONS.md`'s "Stripe configuration validated..." entry.
7. **Compare-and-swap results were not checked rigorously, in both the database functions and their callers.** `write_reconciliation_result`/`write_activation`/`renew_checkout_attempt`/`renew_reconciliation_lease` all condition their writes on the caller still owning a live lease/attempt and return a boolean the caller must check — `write_reconciliation_result`/`write_activation` additionally re-mark `needs_reconciliation = true` in the SAME statement on a lost-ownership failure, so a reconciliation that fails to land is never silently dropped. Separately, `features/billing/actions.ts`'s own callers of the checkout-attempt RPCs were found not checking results rigorously either: the release path ignored a false/null result entirely, and a reclaim-after-release never verified the reclaim actually produced a genuinely new, exclusively-owned attempt. Both fixed in application code this round, not schema — see `DECISIONS.md`'s "Checkout concurrency" entry.
8. **A Checkout Session's `payment_status` was not checked before treating `status: "complete"` as a success.** A complete-but-genuinely-unpaid session (still processing, or a delayed payment method) could be treated as paid. Fixed: only `payment_status: "paid"` or `"no_payment_required"` count as success, both in `features/billing/actions.ts`'s own session-reuse path and `app/dashboard/billing/page.tsx`'s `resolveCheckoutSuccessState` — a complete-but-unpaid session is released (checkout path) or reported as `"unpaid"` (billing page), never trapping every future attempt or misreporting success.
9. **Lease/attempt correctness rested on one static duration being provably longer than every possible Stripe round trip.** `renew_checkout_attempt`/`renew_reconciliation_lease` (migration `20260907210000`) let a caller prove continued ownership and extend its own claim right before its slowest remaining step (Checkout Session creation; a slow `stripe.subscriptions.list()`) — correctness no longer depends on any single static claim/lease duration alone. `lib/stripe.ts`'s `createStripeClient()` also now sets an explicit `timeout: 20_000`/`maxNetworkRetries: 2` — the claim window was previously justified only against the Stripe SDK's own *default* 80-second timeout, without accounting for what the SDK's own automatic retry behavior could add on top of a single logical call.
10. **Stripe customer creation's idempotency key was not durable against Stripe's own key-retention window.** Stripe does not guarantee idempotency-key retention as permanent (keys can be pruned after as little as 24 hours) — a retried `customers.create()` call long after an earlier, successful, never-locally-persisted attempt (a crash in between) could create a genuine second Stripe Customer once the original key expired. `getOrCreateStripeCustomerId` now searches Stripe directly (`stripe.customers.search()`, matching the organization's id in Customer metadata) before ever creating, independent of both the local database and the idempotency key's own retention window; the mutable `name` field is set via a separate, ordinary `update()` call afterward, never inside the idempotency-guarded `create()` (idempotency-key retries require every parameter to stay byte-for-byte stable).
11. **A subscription's identity and price were not verified rigorously enough before being trusted to govern entitlement.** Two closed gaps: (a) `customerIdMatches()` is now checked before BOTH `syncSubscription` and `activateOnPayment` ever apply anything, with a mismatch quarantined and recorded as a `"customer_mismatch"` anomaly, not merely logged; (b) `pickCurrentSubscription` now excludes any subscription whose price isn't in `approvedPriceIds()` from the pool ENTIRELY (not merely de-prioritized) and applies a tiered preference (live/trialing always beats recoverable, which always beats terminal, ties broken by most-recently-created) rather than a flat "prefer non-terminal, else newest" rule that couldn't distinguish a payment failure on a second, later subscription from the customer's still-genuinely-active original one. See `DECISIONS.md`'s "Stripe webhook reconciliation" entry.
12. **Organization-ownership verification and the grandfathered-UI condition both had a real gap.** `orgIdentifiersAgree()`'s prior OR-based check let a mismatch between `client_reference_id` and `metadata.organization_id` pass silently as long as EITHER happened to match — fixed to require both to agree when both are present, and either alone to match when only one is. The grandfathered-UI condition now additionally requires `activated_at == null` — an organization grandfathered in the past but that has since genuinely paid (even if later canceled) must never be shown "permanently free" messaging again. See `DECISIONS.md`'s "Checkout success..." entry.
13. **The Stripe Price interval check only verified the unit, not the count.** `price.recurring.interval === "month"/"year"` alone would have let a Price billed every 3 months or every 2 years pass as an ordinary monthly/yearly plan; `price.recurring.interval_count !== 1` is now checked explicitly, since this app's own pricing/copy assume every period is exactly one unit.
14. **`scripts/rollout.mjs`'s `pollHealth` could still wait meaningfully past its own configured deadline.** A THIRD review's own fix bounded each individual health-check request, but `Math.max(1000, remainingMs)` imposed a one-second MINIMUM per request even with far less than that actually left of the overall deadline — so `pollHealth` could return (or throw) later than the `timeoutSeconds` it was actually given. Fixed by removing the floor: with under a second left, the request gets whatever's actually left, however small. `scripts/rollout.test.ts` gained a new test verified both ways (fails against the pre-fix `Math.max(1000, ...)` body, passes against the current no-floor implementation).

**A genuine, previously-undiscovered CRITICAL bug was found and fixed while completing this round's own mandatory real Stripe test-mode verification — not one of the 14 findings above, and not caught by any unit test that existed before this fix.** `features/billing/stripe-config.ts`'s price-amount check compared `price.unit_amount` (Stripe's own minor-unit amount — confirmed directly against Stripe's currently-published documentation, `docs.stripe.com/currencies`: HUF is a normal two-decimal *charging* currency on Stripe, only zero-decimal for payouts, so a genuine 5,990 Ft Price reports `unit_amount: 599000`, not `5990`) against `plan.amountHuf` (a deliberately whole-Forint *display* value) with NO unit conversion between them — a 100x mismatch that made this check FAIL for every correctly-priced real Stripe Price object. Since `validateNow()` fails closed by design, this meant Checkout had been unconditionally, silently BROKEN in any environment where the configured Stripe Price actually matched what the billing page promises to charge, from the moment this check was originally introduced (third independent review, Finding 6). This was found live, mid-verification, when a real Checkout attempt against this project's own configured test-mode Stripe Price failed with exactly this error — the bug had gone completely undetected by every prior unit test because `stripe-config.test.ts`'s own `price()` test helper mocked `unit_amount` using the IDENTICAL wrong convention (`5990` instead of `599000`): the mock mirrored the implementation's own bug rather than catching it, so every test comparing the (broken) implementation against the (equally broken) mock passed. **Fixed** by comparing against `plan.amountHuf * 100`, with 2 new tests added directly reproducing and proving the fix, and the helper's own convention corrected. This is the concrete, citable reason a real (non-mocked) Stripe test-mode round trip was mandatory this round, not optional — see `DECISIONS.md`'s "Stripe configuration validated..." entry for the full incident.

**Two further, genuinely new bugs were found and fixed by this round's own independent adversarial self-audit of its OWN new code** (`features/billing/reconcile.ts`/`admin-actions.ts`, both new this round), not by the external review's 14 findings: (1) once a previously-tracked, approved subscription moves to an unapproved price, `pickCurrentSubscription` correctly excludes it from the pool going forward — but the org's LOCALLY-STORED `status` had no path to ever be revoked, so it would stay frozen at whatever it last was (e.g. `"active"`) forever, rather than reflecting that no approved subscription currently exists; fixed so a reconciliation with zero approved subscriptions in the pool now correctly writes a revoked/no-subscription state instead of leaving stale data untouched. (2) `activateOrganizationBilling` had no `try`/`catch` around its call to `write_activation`, unlike `reconcileOrganizationBilling`'s own equivalent — a thrown error (not merely an `{ error }` result) would silently abandon a claimed reconciliation lease with `needs_reconciliation` never set, the exact "never assume someone else's write covers it" property Finding 3 above exists to guarantee, violated by an oversight in the brand-new code meant to enforce it. Both reproduced directly in `features/billing/reconcile.test.ts` and fixed before this round was considered complete.

**Per the task's own explicit requirement, three fresh independent reviewers — no access to the implementation reasoning above, each given the raw diff and current files, scoped to (a) checkout/database concurrency, (b) webhook/entitlement/tenant identity, (c) migrations/deployment/configuration/documentation — then audited that already-fixed code a SECOND time, in parallel, and found three more genuine, previously-unknown gaps in the very fixes just described:**
1. **(MEDIUM)** `claim_reconciliation_lease`'s SUCCESSFUL claim branch never touched `needs_reconciliation` at all — only its failure branch did. A genuine process-level kill (a function timeout, an OOM — not a catchable JS exception, which the `try`/`catch` above already handled) between a successful claim and any write/release left the organization invisible to the scheduled sweep. Fixed by migration `20260907230000`: a successful claim now also marks the organization dirty in the same statement, so the invariant is "dirty from the moment of claim, clean only once provably complete."
2. **(MEDIUM)** Once claiming itself started marking dirty, `reconcileOrganizationBilling`'s confirmed-clean `no_subscriptions` outcome had no way to say so without ALSO leaving the organization permanently flagged — it called `release_reconciliation_lease`, the error/abandonment path, which always re-marks dirty regardless of why it's called. Every organization that ever reached that branch would be re-swept every 15 minutes forever. Fixed by a new function, `clear_reconciliation_dirty` (migration `20260907240000`), distinct from `release_reconciliation_lease`: same CAS-on-owner discipline, but does not re-mark dirty on success.
3. **(LOW)** `app/api/admin/reconcile-billing-sweep/route.ts` compared its bearer secret with plain `!==`, not timing-safe — fixed with `node:crypto`'s `timingSafeEqual` (length checked first, since it throws on a length mismatch rather than returning `false`).

Plus several LOW/informational findings addressed the same pass: a stale code comment referencing a nonexistent `scripts/reconcile-billing-sweep.mjs` (the real mechanism is the route + GitHub Actions workflow); a migration self-labeled "Second independent review" when every other document consistently calls it "third"; `subscriptionPriceId` documented as only inspecting the first line item (unreachable via this app's own single-item Checkout flow, but no defense if that ever changes). Both MEDIUM findings proven fixed against real Postgres, not mocks (`e2e/billing-concurrency.spec.ts`'s new "crash-gap fix" and "confirmed-clean fix" tests) — see `TEST_PLAN.md`. All three reviewers separately confirmed the ORIGINAL 14 findings' fixes and the two self-audit fixes above were themselves genuinely correct (traced mechanisms directly, ran the real tests, did not just read comments) — see `DECISIONS.md` for the full technical account of all five audit findings.

**Mandatory real Stripe test-mode verification, performed for real, with concrete evidence** (not mocked, not simulated — this environment had real Stripe test-mode credentials, an authenticated Stripe CLI session, and a genuinely isolated Supabase test project available and used): a seeded test organization, a real browser session, a real redirect to Stripe's hosted test-mode Checkout, payment with Stripe's standard test card (4242 4242 4242 4242), and 14 real, signed webhook events (`checkout.session.completed`, `customer.created`/`updated`, `customer.subscription.created`/`updated`, `invoice.created`/`finalized`/`paid`/`payment_succeeded`, `payment_intent.succeeded`, etc., forwarded locally via `stripe listen`) all processed with HTTP 200 and zero errors. Final database state, verified directly against the isolated project's REST API (not just the UI): `status: active`, real `stripe_customer_id`/`stripe_subscription_id`, `activated_at` set, `current_period_end` one month out, and — critically — `reconciliation_lease_owner`/`reconciliation_lease_expires_at` both null and `needs_reconciliation: false`/`reconciliation_dirty_since: null` (the lease was correctly acquired, used, and released, and every `checkout_attempt_*`/`pending_checkout_session_id` column correctly cleared by `write_reconciliation_result`/`write_activation`). The billing page UI correctly showed "Aktív" with the right renewal date. The test subscription was canceled and the seeded test organization/user deleted afterward as cleanup. **This is a one-time manual verification pass, not yet a committed, repeatable, CI-automated e2e spec** — see `TEST_PLAN.md`'s "Known gaps" for that honest distinction, and a suggested follow-up (Stripe CLI as a companion CI process) not built this round given the scope already covered.

**Real, genuine concurrency verified against Postgres, not mocks — 10 new tests, `e2e/billing-concurrency.spec.ts`.** This round's own task instructions were explicit that "unit mocks are... NOT acceptable as sole proof of database locking, concurrency," a standard the third round's own now-discredited barrier-controlled unit test failed to meet without anyone noticing at the time. Every RPC in the new checkout-attempt/reconciliation-lease family is exercised via `adminClient()` against the real isolated Supabase project, with genuine `Promise.all` concurrency (attempt rollover never lets a stale session id coexist with fresher attempt metadata; a genuinely concurrent second claim reuses the same live attempt, never mints two; `record_checkout_session`/`write_reconciliation_result` with the WRONG id/owner are real zero-row CAS failures, not mock-configured ones; a barrier-controlled test proves exactly one of two genuinely concurrent lease claims wins; a sequential claim-write-release-then-reclaim-write scenario proves the row ends up reflecting the LATER write, never the earlier one — reading real, changing database state, not a pre-arranged final result; an expired lease can be reclaimed; `release_reconciliation_lease` always re-marks the organization dirty regardless of whether the release itself matched a row). All 10 passed. See `TEST_PLAN.md`.

**Verification**: `npm run typecheck`/`npm run lint` clean. `npm run test` — **459/459** (18 files, up from 424/15 — three new files: `features/billing/reconcile.test.ts` (24), `features/billing/admin-actions.test.ts` (8), `app/dashboard/billing/page.test.ts` (16); `features/billing/actions.test.ts` **37** (down from 46 — some prior coverage consolidated as the checkout flow was restructured, with new Finding 1/2/7/8/9/10 coverage added), `features/billing/stripe-config.test.ts` **25** (up from 18 — Findings 6/13 plus the 2 new HUF-bug-reproduction tests), `app/api/webhooks/stripe/route.test.ts` **22** (down from 34 — the barrier-controlled concurrency tests this entry's Finding 3 discredited were removed, not merely edited, since real concurrency proof now lives in `e2e/billing-concurrency.spec.ts`; this file's job narrowed to what the route itself is actually responsible for now that it delegates to the shared service), `scripts/rollout.test.ts` **102** (+1, Finding 14's deadline test), the remaining files unchanged. `npm run build` clean, 22 routes (`/api/admin/reconcile-billing-sweep` new). `supabase db advisors --type all` against the isolated project (all 33 migrations applied, including `20260907230000`/`240000` from the three-reviewer audit above): no issues. `supabase migration list` shows all 33 local migrations matching remote, zero drift.

**Full e2e suite, run to completion against a real production build (`npm run build` + `npm run start`, matching CI's exact `workers: 1`/`retries: 1` configuration, `SUPABASE_DB_URL` set so no connection-requiring spec silently skipped): 171/171, one clean run, ~5.7 minutes.** Two genuinely useful, distinct data points from getting there, not glossed over: (1) an EARLIER run against `next dev` (not a production build) with unlimited local parallelism (6 workers) produced 50 failures — traced to a stray leftover dev server from an unrelated manual verification session still running on the target port, confirmed by killing it and re-running clean (148/162 passed on the next attempt, then 168/168 once the two genuinely stale `rpc-privilege-matrix.spec.ts` assertions below were fixed) — this was dev-mode/stray-process contention, not a rate-limit or logic bug, and does not recur against a production build or in CI (which already uses `next start`, see `playwright.config.ts`'s own comment on why). (2) Two real, expected failures on the first clean run were `e2e/rpc-privilege-matrix.spec.ts`'s own role-matrix and completeness tests, still asserting on `claim_billing_sync` (dropped by migration `20260907210000`) and missing the seven new fourth-round functions — fixed to the current 22-function/22-row set (including `clear_reconciliation_dirty` from the adversarial audit above); rerun clean. **Zero rate-limit failures across the entire 171-test run** — see the e2e-login-retry entry above.

**`npm run test:e2e` — the full suite (20 spec files, 169 tests as currently listed; `--list` confirmed) was run to completion against a real production build** (`npm run build` + `npm run start`, matching CI's exact `workers: 1, retries: 1` configuration) with a real direct Postgres connection (`SUPABASE_DB_URL`) so nothing silently skipped: **168 passed, 0 failed, 0 skipped, ~5.6 minutes** — a genuinely clean, complete run, not a collection of manually-recovered fragments. Two tests needed fixing first: `rpc-privilege-matrix.spec.ts`'s role-matrix test and its own completeness test both still referenced the now-dropped `claim_billing_sync` and were missing the 7 new functions this round added — fixed, now 21 functions/21 rows, matching `DATABASE_SCHEMA.md`. **An earlier run against `next dev` (not a production build) with unlimited local parallelism (6 workers) produced 50 failures** — confirmed, by an isolated stray-process-elimination test and by the clean production-build run above, to be pure dev-server compilation-latency contention under Turbopack's on-demand compilation, NOT a rate-limit problem and NOT a real application defect; irrelevant to CI, which already runs against `next start` (see `playwright.config.ts`'s own comment on why). **A standing, previously-undocumented flakiness source is now closed properly, not worked around per-file**: every UI-driven sign-in across the suite now goes through a new shared, rate-limit-aware retry helper (`e2e/support/ui.ts`'s `signInViaUi`/`submitLoginForm`) instead of each file duplicating its own fill-and-click block with no retry, and `e2e/support/seed.ts`'s `userClient()` gained the same retry-with-backoff — this is what let the full-suite run above pass cleanly rather than needing the usual two-pass parallel-then-serial recovery every earlier round in this file records.

**Migration replay — attempted, genuinely not completed, an honest open gate, not a silently skipped step.** Per this round's own explicit instruction ("never destructively reset the shared isolated Supabase project; use a genuinely disposable database; if unavailable, finish everything else and report this exact gate as unresolved"): this environment has no Docker, no local Postgres client tools (`psql`/`pg_ctl`/`initdb`/`postgres` all confirmed absent), no `embedded-postgres`/`@electric-sql/pglite` npm packages installed, and no authenticated Supabase CLI/MCP account access to provision a fresh disposable cloud project (`supabase projects list` fails with `LegacyPlatformAuthRequiredError` — no access token configured). Correctly left undone rather than substituted with a misleading approximation — a WASM/embedded Postgres would not accurately replicate Supabase's own `auth` schema, extensions, and role setup these migrations depend on, so a "pass" there would not actually prove what real migration replay is supposed to. See `TEST_PLAN.md`'s "Known gaps" for the same distinction stated in the test-coverage context.

**Not part of this pass**: merging to `master`, deploying, applying anything to production, altering DNS, enabling Stripe live mode, configuring `RECONCILE_SWEEP_SECRET`/`PRODUCTION_RECONCILE_SWEEP_URL`, or deciding the Hungarian VAT/Stripe Tax invoicing approach (see "What's needed from the user" below).

## Third independent review of the integration round: 9 findings (2026-09-07, on `feature/billing-subscriptions`, not yet merged to `master`)

A third independent review, explicit that the second round's "all fixed" report was not accepted, found 9 further defects — 5 HIGH, the rest MEDIUM — several of which directly contradicted or reopened claims the second round's own report made:

1. **HIGH — the documented rollout deterministically created a `PGRST203` outage.** Production migration 16 already defines `request_notification_email_change(bigint, text, int default 1440)`; the second round's own migration 18 added a NEW function of the SAME name (2-argument). Since a call naming just `(p_organization_id, p_email)` matches both simultaneously, `DEPLOYMENT.md`'s own documented rollout (apply migration 18 during `prepare`, drop the legacy function only during `finalize`) guaranteed an ambiguous-overload failure for every caller across the entire window between those two steps — not a bounded risk, an unconditional one. Fixed by renaming the new function to `reserve_notification_email_change` (migrations 18/22 edited in place, never applied to production) — two different names can never collide as PostgREST overloads, closing the category of bug rather than re-timing around it. Verified executably against the real isolated project's PostgREST layer (`e2e/rollout-compatibility-notification-email-rpc.spec.ts`, all 3 real rollout states + a negative control). The isolated test project's own drifted schema (still holding the old-named function from before the rename) was reconciled — see "Isolated test project reconciliation" below.
2. **HIGH — the Checkout lease was not a durable, owned attempt.** No compare-and-swap owner token, application-clock-based lease decisions, a time-bucketed Stripe idempotency key that changed on every retry, no reconciliation of a completed session once the local lease expired, an open session for one plan reusable for a different one, and a 60-second claim window shorter than the installed Stripe SDK's own 80-second default timeout. Redesigned as a durable checkout-attempt model (`claim_checkout_attempt`/`record_checkout_session`/`release_checkout_attempt`, migration `20260907200000`), all compare-and-swap'd on an opaque attempt id, all lease decisions made with `clock_timestamp()` inside the database. See `DECISIONS.md`'s "Checkout concurrency" entry.
3. **HIGH — an event for an old subscription could overwrite a newer one.** Canonical retrieval (`stripe.subscriptions.retrieve()`) is authoritative only for the ONE subscription it fetches, never for the organization/customer as a whole — a canceled `sub_old`, superseded by a genuinely newer `sub_new`, could still be retrieved and written by a redelivered event, silently reverting the organization's real state. Fixed by listing the Stripe Customer's FULL subscription set and picking whichever should currently govern entitlement, plus a Stripe-Customer-vs-persisted-customer mismatch quarantine. See `DECISIONS.md`'s "Stripe webhook reconciliation" entry.
4. **HIGH — the webhook's retrieve-then-write flow had a real, unfenced write-write race**, previously documented as "self-healing" without actually guaranteeing a correcting event would ever arrive. Fixed with `billing_sync_seq`, a per-organization fencing token claimed before any Stripe network call and checked at write time — proven with a barrier-controlled test forcing reverse completion order. See `DECISIONS.md`.
5. **HIGH/MEDIUM — `activated_at` was set from a subscription status snapshot, not a durable payment signal**, so an organization that paid, went briefly active, and was canceled again before any status webhook happened to process it while active would never have `activated_at` set at all. Fixed by setting it only from a signed `invoice.paid` event, decoupled entirely from subscription state. See `DECISIONS.md`'s "Billing state machine" entry.
6. **MEDIUM — no verification that the live Stripe configuration matches what the billing page promises.** `features/billing/stripe-config.ts`'s `assertStripeConfigurationValid()` (checked before every Checkout attempt) and the webhook's `checkEventEnvironment()` (checked on every delivery) close this — key/event livemode vs `APP_ENV`, Price active/currency/amount/interval/tax_behavior, and both Prices sharing one Product.
7. **MEDIUM — `?checkout=success` was trusted as proof of payment on its own.** Now verified server-side via Stripe's `{CHECKOUT_SESSION_ID}` placeholder, distinguishing confirmed/pending/unpaid/invalid. Grandfathered organizations also got their own explicit UI state instead of being folded into the generic "trial" badge/countdown.
8. **Rollout/CI gaps** — `verify-production-deployment` now also requires `body.environment === "production"`, not just `ok`+`commitSha`; every `/api/health` poll now carries a bounded `AbortSignal.timeout()` capped by the remaining deploy deadline (a hanging health server used to be able to consume the entire timeout on one stuck request); `prepare` no longer moves `--enforce` migration files out of the real `supabase/migrations/` directory at all — it assembles a complete throwaway temporary Supabase project directory and points `supabase db push --workdir` at that copy, so a hard crash mid-rollout leaves the real repository completely untouched instead of missing tracked files; a column rename is no longer described as "purely additive" in `DEPLOYMENT.md`; the rollout script's own header comment corrected to say `APP_ENV` must be set before the MERGE, not merely before `finalize`.
9. **Documentation contradictions** — `DATABASE_SCHEMA.md`/`SECURITY.md`/`DECISIONS.md`/`DEPLOYMENT.md` all described the pre-rename RPC coexistence as safe ("follows the R6-03 principle exactly... safe to apply") when it was the source of Finding 1; corrected throughout, including the RPC grant tables (now 15 rows, not 11) and the "self-healing residual race" framing (removed — the race is now actually fenced, not merely tolerated).

**Isolated test project reconciliation**: migrations 18/22 had already been applied there under their pre-rename names. Reconciled honestly (never touching production, which has never run either version) via `supabase migration repair --status reverted` (bookkeeping only) followed by a real `supabase db push` — which required making migration 18's table/index creation idempotent (`if not exists`, behavior-identical for a genuine first-ever apply) since its already-existing tables would otherwise collide with the reapply. Verified via the RPC privilege-matrix completeness test (`e2e/rpc-privilege-matrix.spec.ts`) failing loudly first (confirming the drift was real, not assumed), then passing after reconciliation — 15/15 functions now match the live catalog exactly, and the real notification-email settings flow was verified end to end against the reconciled project (`e2e/notification-email-verification.spec.ts`, 21/21).

**Independent adversarial review of this round's own diff** (required by the task, performed by a fresh subagent with no access to this session's own reasoning) found 2 confirmed MEDIUM issues, both fixed before this round was considered complete: (1) a blanket find-and-replace rename had swept a test (`e2e/notification-email-verification.spec.ts`'s "the round-3 3-argument function no longer exists" test) into calling the NEW `reserve_notification_email_change` instead of the legacy `request_notification_email_change` it exists to prove is gone — the test still passed, but vacuously, no longer detecting a regression where the legacy function reappears; (2) `runPrepare` (this round's rewrite of the highest-risk part of the rollout tooling) had zero automated coverage. Fixed: the test now deliberately targets the legacy name again (with an explanatory comment on why); `scripts/rollout.test.ts` gained 7 new tests exercising the real filesystem assembly (mocked `execFileSync`, real `mkdtempSync`/`copyFileSync` against the OS temp dir and this repo's own real `supabase/migrations/`, read-only) — workdir contents, real-directory non-interference, cleanup on success and on a thrown push failure, dry-run behavior, and the empty-`--expand` skip path.

**Verification**: `npm run typecheck`/`npm run lint`/`npm run build` clean throughout. Full unit suite 424/424 (15 files, up from 377) — first pass, no reruns needed except the 2 adversarial-review fixes above. `supabase db advisors --type all` against the isolated project (all 29 migrations applied): no issues. See the delivered report for the full initial-vs-recovered e2e breakdown, migration-replay evidence, and remaining risks.

## Second independent review of the integration round: 11 findings (2026-09-07, on `feature/billing-subscriptions`, not yet merged to `master`)

The previous round's own report was reviewed and found materially incorrect: green checks had been presented as proof of production-safety without actually proving it. 11 findings, all confirmed by tracing the actual code (not inferred), all fixed:

1. **`DEPLOYMENT.md` put the `APP_ENV` prerequisite *after* the merge/deploy step it needs to precede.** Vercel environment variable changes never take effect on a deployment that already exists, only the next one — following the steps in the old order could deploy first, then "satisfy" the prerequisite too late to matter. § 7 reordered: `APP_ENV` confirmation is now step 1 (before merging), with an explicit note that a deployment that already happened before noticing needs a fresh redeploy, not just the variable set. Added a concrete, testable step 4 checklist (`curl`+parse) that a human runs before `finalize`, checking `ok`, `environment`, and `commitSha` all at once — not three separate assumptions.
2. **`scripts/rollout.mjs`'s `finalize` returned success without ever polling health when nothing was pending to apply.** An operator running `finalize` with the wrong `--expected-sha`, or against a database where the enforce migrations became applied some other way, saw a green "done" with the live deployment never actually checked. Fixed: the health poll (step 2) now always runs; only the migration-application-specific steps (drain, pre-apply smoke check, the push itself) are skipped when there's nothing to apply. 3 new tests in `scripts/rollout.test.ts` (`runFinalize` exported specifically for this) prove a mismatched SHA or an unreachable `/api/health` still fails even with nothing pending, and that a genuine prior success is still recognized as one.
3. **The documented `prepare` command, and `DATABASE_SCHEMA.md`'s migration-classification reasoning, never mentioned the two billing migrations (24, 25).** Both must be `--expand` (new dashboard code queries `organization_billing` on every request) for the identical reason migration 23 is — `DEPLOYMENT.md` § 7 and `DATABASE_SCHEMA.md`'s "Rollout ordering" both corrected and cross-referenced.
4. **The webhook's out-of-order guard compared `event.created` timestamps with a strict `<`, but Stripe's own `created` resolution is one second** — two genuinely distinct events sharing that second made the second one look "not newer" and its state was silently dropped. Reproduced directly in `app/api/webhooks/stripe/route.test.ts` ("never loses the later state between two events at the identical one-second timestamp"). Fixed by switching to Stripe's own documented pattern for this exact class of problem — **canonical current-state retrieval**: the handler no longer trusts an event's own embedded snapshot at all, it re-fetches the subscription fresh via `stripe.subscriptions.retrieve()` every time and writes whatever that returns. There is no timestamp comparison left to have a precision bug in. `last_synced_event_created_at` (the flawed guard's own column) is renamed to `last_synced_at` (migration `20260907170000`) and repurposed as a plain diagnostic, not a correctness gate.
5. **The billing state machine was incomplete**, keyed entirely on `stripe_subscription_id` being set at all: a grandfathered organization whose first payment attempt merely went `incomplete` lost grandfathering the instant Checkout created the subscription object, and any non-null `stripe_subscription_id` (including `canceled`/`incomplete_expired`) permanently blocked a new Checkout attempt. Fixed with an explicit, documented state machine (`features/billing/status.ts`): a new `activated_at` column (migration `20260907180000`), set once, only on a genuine transition to Stripe status `'active'`, never overwritten afterward — `isBillingActive` checks current status first, falls back to `activated_at` to decide whether grandfathering/trial still apply, and a new `hasLiveSubscription` (trialing/active/past_due/incomplete/unpaid/paused — **not** canceled/incomplete_expired) replaces the bare truthiness check gating both `createCheckoutSessionAction`'s guard and the billing page's own UI. 25 new tests across `status.test.ts`/`actions.test.ts` cover every required transition: grandfathered → incomplete (access preserved) → incomplete_expired → new Checkout (allowed); active → canceled → resubscribe (allowed); past_due recovery; duplicate and delayed webhooks (already covered by finding 4's canonical-retrieval fix, which makes any replay order converge to the same true state).
6. **Checkout's own duplicate-session guard was still partial** — a 30-second, time-bucketed Stripe idempotency key protected against a rapid double-submit in the same bucket, nothing else (a second submission a minute later, or a different interval, shared no key). Added a real, database-backed lease per organization (`pending_checkout_session_id`/`pending_checkout_expires_at`, migration `20260907190000`): a short atomic claim before ever calling Stripe, extended to the session's own real lifetime once created, an existing open session reused (fetched fresh from Stripe, never trusted from the cached row) rather than duplicated, and cleared by the webhook once a subscription genuinely exists. `getOrganizationBilling()` (found in the same pass) used to silently convert a database error into `null`, indistinguishable from "no subscription" — now throws, with the one caller that isn't itself a security boundary (`signInAction`'s single-hop redirect optimization) catching it explicitly rather than the query swallowing it.
7. **`getOrCreateStripeCustomerId` created the Stripe Customer with no idempotency key at all** — the losing side of a race (or a connection failure between a successful `create()` and this app's own persist) had already created a real, orphaned customer before discovering someone else won. Added a stable, organization-scoped (never time-bucketed — an organization should only ever have one customer, ever) idempotency key, so Stripe itself deduplicates a concurrent or retried call for the same organization.
8. **Checkout/Portal authorized on organization membership alone**, ignoring the role `getCurrentOrganization()` already returns — any member, including `manager`/`staff`, could start or cancel a real subscription. Added `canManageBilling` (owner/admin only), enforced server-side in both actions; tests cover an unauthorized manager/staff call being refused before touching billing state or Stripe at all, even though only `owner` memberships are actually created today (no invite flow yet).
9. **The dashboard nav's own overlap regression, reintroduced by billing's seventh link** — confirmed directly: at 1280px, real `getBoundingClientRect()` measurements showed the nav's right edge (x=967.5) overlapping the org name's left edge (x=942.7) by ~25px, while `document.documentElement.scrollWidth` reported zero overflow (flexbox doesn't clip an overflowing sibling — the identical structural reason R5-07 already documented for the *previous* overlap, at a different breakpoint). The nav's breakpoint moved from `xl` (1280px) to `2xl` (1536px) — measured as the first standard Tailwind breakpoint with a comfortable margin (~199px), not the tightest one that happens to pass (1366px, a common laptop width, left only ~29px). Verified live in a real browser before and after (screenshots, real bounding-box measurements) — confirmed the overlap, confirmed the fix. `e2e/dashboard-nav-accessibility.spec.ts` rewritten with a real bounding-box overlap assertion (`assertNoNavOverlap`) run at 1536/1600/1920px, replacing the scrollWidth-only check that structurally could never have caught this. Fixing the breakpoint surfaced two further, genuine (not rate-limiter) e2e failures in `e2e/onboarding-tour.spec.ts` — both relied on the old `xl`/1280px assumption to find the inline nav directly; fixed to pin an explicit `>= 2xl` viewport.
10. **The billing page's own feature copy overclaimed** ("Azonnali e-mail értesítés" — instant email notification) for a system with a per-card cooldown, an hourly budget, and possible provider failures — correded to match the wording already fixed elsewhere (`app/page.tsx`, `features/onboarding-tour/tour-steps.ts`): conditional ("állíthatsz be" — you can set up), never a guaranteed-immediate promise.
11. **This file's own migration-history narrative was self-contradictory** — the "Migration-order reconciliation" paragraph below claimed the renamed billing migration had "never been applied anywhere," while the very next paragraph described discovering its effects already existed, created out-of-band. Corrected in place: the accurate claim is that the migration was never recorded as *applied in the bookkeeping table* (a necessary check before renaming, verified), not that it had never run at all (false, and the entire reason the bookkeeping repair below was needed). See that entry, corrected below, for the precise distinction.

**Test counts, stated precisely rather than as a bare delta from an earlier, less precise report:** `npm run test` — **376/376** (14 files, up from 333). By file: `scripts/rollout.test.ts` **93** (+3, the `runFinalize` health-check coverage above), `app/api/webhooks/stripe/route.test.ts` **19** (rewritten for canonical retrieval + the same-second reproduction + 3 `activated_at` tests), `features/billing/status.test.ts` **32** (the state-machine transition table above), `features/billing/actions.test.ts` **39** (authorization, the checkout lease, the customer-creation idempotency key), the remaining 10 files unchanged. `npm run test:e2e` — **144/144** (18 files, up from 139 — `e2e/dashboard-nav-accessibility.spec.ts` gained 5 tests for finding 9's real overlap check). Initial parallel run: **114 passed, 30 failed**, every failure the same documented Supabase-Auth rate limiter, spanning 7 files this time (`negative-feedback-alert-abuse`, `nfc-card-location-lock`, `notification-email-verification`, `onboarding-tour`, `organization-onboarding`, `redirect-safety`, `tenant-isolation`) — more files than a typical round, still the identical documented cause, confirmed by re-running each failing test in isolation. Serial re-run of those 7 files: **79/79**. Combined recovered total: **144/144**.

`npm run typecheck`/`npm run lint` clean throughout. `npm run build` clean, 21 routes, 28 migrations. `supabase db advisors --type all` against the isolated project (all 28 migrations applied): no issues.

**Migration replay verification — what was and wasn't actually done.** The 3 new migrations this round (`20260907170000`–`20260907190000`) applied cleanly to the isolated project, which already carried the prior 25 (an upgrade-from-a-real-prior-state test, though that prior state already included PR #3's and PR #4's own migrations, not literally production's 1–17 baseline). A genuine clean replay from an empty schema was **attempted and not completed**: this sandboxed environment's own auto-mode classifier blocked the `DROP TABLE`/bookkeeping-reset commands needed to test it, consistent with this project's standing rule that destructive database operations need a human to run them. Static verification instead: migration timestamps are strictly increasing end to end (`ls supabase/migrations/`, confirmed), and every migration's own SQL only ever references objects introduced by an earlier-timestamped migration (read in full across this and prior rounds) — real evidence, but weaker than an executed replay. **If a genuine empty-to-28 replay is wanted, a human needs to run it** (or grant the classifier an exception) — not something this round could complete on its own.

**Not part of this pass**: merging to `master`, deploying, applying anything to production, altering DNS, enabling Stripe live mode, or touching any other external service.

## PR #3/PR #4 integration: 3 billing safety fixes, `rollout.mjs` two-phase redesign, merge (2026-09-07, on `feature/billing-subscriptions`, not yet merged to `master`)

An independent review found the prior assessment of PR #3's merge-readiness materially incorrect on two counts: `scripts/rollout.mjs`'s single-command design had a real production deployment race, and PR #4 (subscription billing, described below under "Subscription billing built") was open, unmerged, with 3 real safety gaps — not "Stripe isn't implemented at all" as previously stated. Fixed all three, then merged PR #4 with PR #3's final state.

**1. `scripts/rollout.mjs` deployment race (PR #3, commit `5a4dd73`, pushed to `fix/round4-review-findings` before the merge).** The single-command design required `--expected-sha` before the merge/deploy it names had even happened — Vercel's auto-deploy runs independently and immediately, with no Deployment Check configured to gate it, so new code needing an expand migration's column could go live before that migration ran; a GitHub squash/merge also produces a SHA the pre-merge branch HEAD can't predict. Split into `prepare` (apply expand only, before merging, no SHA needed, safe to run before the merge exists) and `finalize` (apply enforce only, after merging, using the actual post-merge SHA — refuses outright if anything other than the enforce set is still pending). Both commands are safely resumable after interruption: two new validation functions (`validatePreparePlan`/`validateFinalizePlan`) tolerate a phase's own prior partial progress while still refusing anything genuinely unexpected, alongside the existing `validateMigrationPlan` (left untouched, still used for... no other caller now, kept for its own test coverage and as the strict from-scratch reference). Also corrected a chat-only (never written into any file) claim that branch-protection status was "unknowable" without authenticated GitHub API access — the public `/repos/{owner}/{repo}/branches/{branch}` endpoint reports `protected` directly with no auth needed (confirmed: `false`, for `master`). `DEPLOYMENT.md` § 7 rewritten for the two-command workflow, with `APP_ENV` stated as an explicit prerequisite at the point `finalize` is actually run, not just a general backlog item. **Verification**: `npm run typecheck`/`lint` clean; `npm run test` — `scripts/rollout.test.ts` grew to 90 tests (from the prior round's count), all passing, including new coverage for phase separation (each subcommand rejects the other's flags), manifest errors, unexpected migration state (finalize refusing when an expand migration is still pending), health-check failure diagnostics, wrong SHA/environment, and retry-after-a-completed-prepare/finalize; `npm run build` clean. **Not applied**: no migration was pushed to any database as part of this fix — it is tooling and documentation only. Not merged, not deployed.

**2. Three billing safety findings on PR #4 (`feature/billing-subscriptions`, commit `fa3355c`, pushed before the merge).** PR #4 (below, "Subscription billing built") turned out to have three confirmed gaps, found by tracing the actual code paths, not inferred from the schema alone:

- **A. Every pre-existing organization had no `organization_billing` row at all.** `private.provision_organization_trial()` only fires on `organizations` `INSERT` — an organization created before migration `20260905125856` (renumbered to `20260907150000` during this merge, see below) ran has no row, so `getOrganizationBilling()` returns `null` and `isBillingActive(null)` is `false` — permanently locking it out of the dashboard. Worse, reaching the billing page and attempting checkout made it *worse*: `getOrCreateStripeCustomerId`'s own persisting `UPDATE` matched zero rows for the identical reason, unchecked — a real Stripe customer got created (and, had checkout completed, genuinely charged) that this database could never look up again. Fixed with a new migration (`20260907160000`) backfilling a non-expiring `grandfathered_at` for every pre-existing organization (a deliberate, documented policy — see `DECISIONS.md` — not a normal time-limited trial, since starting a 14-day countdown for real customers mid-use with no warning would be a surprise outage, not a billing launch), and `getOrCreateStripeCustomerId`/the webhook now treat a missing row as a hard error instead of silently proceeding.
- **B. The webhook's idempotency record and its billing-state update were two independent, unchecked Supabase requests.** A crash or transient failure between them permanently marked a never-applied event as done — the event id insert happened *before* the state update, so a retried delivery hit the already-recorded id and never reached the update at all. Fixed by reordering: the update now runs first and its success is checked; the event is recorded as processed only after that succeeds. Also added out-of-order-delivery protection (Stripe does not guarantee delivery order) — each event's own `created` timestamp is compared against the last one actually applied, enforced atomically inside the `UPDATE`'s own `WHERE` clause so it holds under concurrent deliveries too, not just sequentially-reordered ones.
- **C. Checkout had no server-side guard against a second subscription for an already-subscribed organization, and no Stripe idempotency key.** A double-submitted form could create duplicate Stripe customers/Checkout Sessions. Fixed: `createCheckoutSessionAction` now refuses to create a second session when `stripe_subscription_id` is already set, passes a Stripe idempotency key scoped to a short window (organization + interval + time bucket, not the organization permanently — Stripe only remembers a key for 24 hours), and `getOrCreateStripeCustomerId`'s customer-id persist uses an optimistic-concurrency `UPDATE` guard (`.is("stripe_customer_id", null)`), with the losing concurrent call reading back the winner's id instead of orphaning its own.

**Verification (billing fixes, on `feature/billing-subscriptions` before the merge)**: `npm run typecheck`/`lint` clean; `npm run test` — 148/148 passing (9 files), including new coverage in `features/billing/status.test.ts` (grandfathering: active with no subscription/trial, never expires, stops applying once a real subscription exists), a new `features/billing/actions.test.ts` (12 tests: missing-row/read-error hard failures, the concurrency-guard race and its resolution, the already-subscribed guard, the idempotency-key shape), and a rewritten `app/api/webhooks/stripe/route.test.ts` (apply-then-record ordering, stale/out-of-order-event skipping treated as success not failure, a missing billing row asking for a retry). `npm run build` clean, 20 routes. **Not run**: `e2e/billing-paywall.spec.ts` needs no Stripe credentials (it drives the paywall directly through `organization_billing`, not a real Stripe call) but does need the migration applied to the isolated test project first — deferred to the combined post-merge verification below, since running it against this branch's own incomplete migration set (missing PR #3's migrations, which the isolated project already had applied from earlier work this session) would have been a false verification. No real Stripe Checkout/webhook round trip was exercised — this environment has no Stripe test-mode credentials configured; see `TEST_PLAN.md`'s "Known gaps".

**3. Merging PR #3 into PR #4 (user's explicit choice, over a true `git rebase`, to avoid rewriting PR #4's already-pushed history and needing a force-push).** `git merge --no-ff` of `fix/round4-review-findings` into `feature/billing-subscriptions`. 12 files conflicted, all resolved semantically (not by blindly picking one side): `proxy.ts`/`app/dashboard/dashboard-nav.tsx` (both branches' additive changes kept together — the Stripe webhook path and `/api/health` in `proxy.ts`'s allowlist, `CreditCard`+`Menu` icons and the billing nav item without a tour target in the nav); `app/dashboard/layout.tsx` auto-merged cleanly (the billing paywall gate and the onboarding tour/responsive-header restyle don't overlap); `lib/supabase/database.types.ts` (both new type aliases and both new tables kept); `ARCHITECTURE.md`/`DATABASE_SCHEMA.md`/`DECISIONS.md`/`PRODUCT_SPEC.md`/`SECURITY.md`/`TEST_PLAN.md`/`e2e/README.md` (narrative sections combined, renumbered, or cross-referenced as needed — `DATABASE_SCHEMA.md`'s migration list in particular required renumbering, below); `REVIEW_REQUEST.md` (PR #4's own review request archived unchanged to `REVIEW_REQUEST_BILLING.md`, matching the existing `REVIEW_REQUEST_ROUND8.md` precedent, with the live document rewritten to describe the combined branch); `STATUS.md` (this file — PR #4's one entry inserted at its correct chronological position, below, since it was built in parallel with round 4's own pass, both starting right after the same "Merged to production" point).

**Migration-order reconciliation.** PR #4's two billing migrations (originally `20260905125856`, plus a new one for the 3 fixes above) predate PR #3's round-8 migrations (`20260905193325` through `20260906130000`) by timestamp, but land in production *after* them in this integration's actual rollout order. Renamed to `20260907150000`/`20260907160000` — checked via `supabase migration list` against the isolated test project before renaming, which showed `20260905125856` with an empty `remote` field, i.e. **not recorded as applied in the migration bookkeeping table** (`supabase_migrations.schema_migrations`) — a necessary check, but, as the very next entry below found, **not a sufficient one**: the bookkeeping table's own record turned out to be wrong, and the migration's actual effects (the `organization_billing` table) already existed, created out-of-band. Precisely stated, the rename was safe not because the migration had literally never run, but because its **file content had never been edited** — see the next entry for why that specific property, not "never applied" in the fuller sense, is what makes a bookkeeping repair (rather than a rewrite) the correct fix, and why renaming was still fine to do here.

**Verification (combined, after the merge)**: 25 migrations total. `npm run typecheck`/`lint` clean. `npm run test`: **333/333** (14 files — the 148 from `feature/billing-subscriptions` plus the rest of PR #3's own suite). `npm run build`: clean, 21 routes (`/api/health`, `/api/webhooks/stripe`, `/dashboard/billing` all present together for the first time).

**A real schema/bookkeeping drift was hit and fixed while applying this reconciled migration set** — the same class of issue this project has hit before (see `DECISIONS.md`'s `rollout.mjs` entry). `supabase migration list` against the isolated test project initially reported `20260907150000` (the renamed `add_organization_billing` migration) as pending (`"remote":""`), but attempting to push it failed with `relation "organization_billing" already exists` — a direct schema check confirmed the table (and `stripe_webhook_events`, and 2 real rows) already existed, created out-of-band by the user's own earlier `npx supabase db push` run (recorded in this file's "Subscription billing built" entry below) whose effect was never recorded in `supabase_migrations.schema_migrations`. Reconciled with `supabase migration repair --status applied 20260907150000` (bookkeeping-only, does not re-run the SQL — safe here specifically because the migration file's content was never edited, only renamed, so the recorded "applied" migration's SQL genuinely matches what was actually run) before pushing the remaining migration (`20260907160000`, the grandfathering/event-ordering fix) for real. Confirmed post-fix: `organization_billing` has both new columns (`grandfathered_at`, `last_synced_event_created_at`); `supabase db advisors --type all` reports no issues.

`npm run test:e2e`: **139 tests total across 18 files** (up from 135/17 — the new file is `billing-paywall.spec.ts`, 4 tests). Initial parallel run: **116 passed, 23 failed, 0 skipped** (`SUPABASE_DB_URL` exported throughout), every failure `AuthApiError: Request rate limit reached` from the same shared Supabase-Auth rate limiter documented in every previous round, confined to the same 4 files this has affected before (`notification-email-verification`, `onboarding-tour`, `organization-onboarding`, `tenant-isolation`) — **`billing-paywall.spec.ts`'s 4 tests passed cleanly on this first parallel attempt**, not affected. Re-running those 4 files serially (`--workers=1`, `SUPABASE_DB_URL` set): **62/62**, zero failures. Combined recovered total: **139/139** — reported as recovered across the standard two-pass procedure, not as a clean first run.

**Not part of this pass**: merging to `master`, deploying, applying anything to production, altering DNS, enabling Stripe live mode, or touching any other external service.

## Sixth onboarding-tour review response — 2 findings (2026-09-06, not yet merged)

An independent review of `1cbe4a9..066639e` confirmed the three prior findings substantively fixed — no new application, RLS, migration, or security defect in that documentation-only commit — and found 2 more documentation-only issues:

1. **`REVIEW_REQUEST.md` was already stale at its own current head (LOW)** — the history table's row for `1cbe4a9` (the fourth review-response commit) still read "this round's commit — see current head," a placeholder that was accurate when written (a commit can't cite its own SHA) but never got filled in once `066639e` superseded it as the actual current head — exactly the stale-handoff pattern this document exists to prevent. Filled in `1cbe4a9` explicitly, and added the missing row for `066639e` ("Fifth onboarding-tour review response — 3 findings").
2. **`STATUS.md` overstated `/api/health` as "the one thing that could prove the exact live commit" (LOW)** — Vercel's own dashboard and deployment/alias metadata can already identify the deployed commit manually, the same way this repository's own "Vercel deployment gap" incident (above) used exactly that to find the Production alias was stuck on a stale commit, well before `/api/health` existed. Corrected in both `STATUS.md` and `REVIEW_REQUEST.md` (which repeated the same overstatement): `/api/health`'s actual gap is *automated* verification specifically — the CI job that polls it and fails loud on mismatch — not manual verification, which Vercel's dashboard already supports today.

**Verification**: complete diff from `066639e` inspected; `git diff --check` clean; `npm run typecheck`/`npm run lint` clean; `npm run build` clean, 19 routes; **no application code, migration, workflow, dependency, or lockfile changed this round** — only `STATUS.md` and `REVIEW_REQUEST.md` touched; `supabase migration list` shows all 23 migrations still matching remote; `supabase db advisors` reports no issues; `npm run test` (unit) **245/245**, unchanged.

`npm run test:e2e` (135 tests, 17 files, unchanged): the initial default-parallel run genuinely had failures, reported honestly — **90 passed, 23 failed, 22 skipped**, every failure `AuthApiError: Request rate limit reached` from the same shared limiter as every previous round, across the same 5 files. Re-running those 5 serially (`--workers=1`, `SUPABASE_DB_URL` set) still hit the same limiter once more — **68/69** in that pass, one more `AuthApiError` on a different test than any prior round hit (`R6-04: ...finalize_notification_email_change_send directly`) — re-ran that single test alone and it passed cleanly, confirming the same pre-existing flakiness, not a regression. The remaining 12 files, run together separately: **66/66**. Combined recovered total: **135/135** — reported as recovered across two rounds of re-running, not as a clean first or second pass.

**Not part of this pass**: merging, applying anything to production, altering DNS, or touching any external service.

## Fifth onboarding-tour review response — 3 findings (2026-09-06, not yet merged)

An independent review of `cb5ab3b..1cbe4a9` found 3 further issues, all confirmed by independently checking the actual code, git history, and current Supabase documentation before fixing:

1. **The domain checklist's Supabase redirect allowlist value was still wrong (MEDIUM)** — it told the owner to add the bare `https://app.velemenytap.hu` origin. Verified directly: `features/auth/actions.ts` is the only `emailRedirectTo`/`redirectTo` call site in the entire codebase (confirmed by grepping the whole tree), and it sends `` `${NEXT_PUBLIC_SITE_URL}/auth/confirm?next=/onboarding` `` — a full path plus query string, not an origin. Read Supabase's current redirect-URL documentation before choosing a fix: the allowlist matches the full URL, and supports `*`/`**` glob wildcards with `.`/`/` as separators — meaning a single `*` would stop at the `/` embedded in `?next=/onboarding`'s own value, so the pattern has to use `**`. The documentation doesn't explicitly say whether the query string is required in the allowlist entry or ignored during matching; rather than guess, the value now documented (`https://app.velemenytap.hu/auth/confirm**`) is correct under either reading. Step 5 of the checklist corrected in place (this is a targeted fix to one value, not a restructure — no reason to duplicate the whole 13-step list a third time); step 8 strengthened to require an actual signup-confirmation round trip, not just a code read, since that's the one path this specific allowlist entry gates; `REVIEW_REQUEST.md`'s shortened summary of the checklist updated to match, with an explicit note not to shorten it back down to a bare domain.
2. **`REVIEW_REQUEST.md`'s production-state description conflated "not full PR #3" with "still on PR #2" (LOW)** — production's application code is not frozen at PR #2's merge commit (`4f984e6`); `master@443ea98` (this PR's own base) has 6 more real commits after it, confirmed via `git log --oneline 4f984e6..443ea98`: the Vercel Git-integration/Root-Directory recovery work, and a dashboard visual restyle (`443ea98` itself) that shipped directly to `master` outside any review cycle (`STATUS.md`'s own round-4 entry already documents this). Rewrote the production-state summary to distinguish three things explicitly: the **database** (migrations 1–17 applied, 18–23 pending — unchanged, already correct), the **application** (documented as the `master` line through `443ea98`, including the deployment-repair commits and the restyle — explicitly *not* independently re-confirmed as the live deployment today; this repository's own code can't prove that on its own, since `/api/health` doesn't exist in this baseline at all — it's a round-4 addition that only exists on this still-unmerged branch — but that's a gap in *automated* verification specifically, not the only way to check: Vercel's own dashboard/deployment metadata can identify the exact deployed commit manually today, the same way it already did during the "Vercel deployment gap" incident this file documents above), and **what's not in production at all** (every round from 4 onward, in full).
3. **The prior stale-reference audit missed 4 migration-comment citations (LOW)** — `20260904131144`, `20260904194100`, `20260904194500`, and `20260905193325` all have SQL comments citing `REVIEW_REQUEST.md`'s finding table or rollout plan by name. These migrations are **not** rewritten (three are already applied to production; rewriting applied migration history is exactly what this project's standing discipline prohibits — see `DECISIONS.md`). Instead, traced each comment to its actual historical commit by reading that commit's own `REVIEW_REQUEST.md` content directly (not inferred from the finding number alone) — three resolve to commit `68cd929`/`4f984e6` (round 2/3's own handoffs, per the same pattern established last round), one to `799974d` (round 6, which names this exact migration file by filename in its own text). Added a "Historical references from migration comments" section to `REVIEW_REQUEST.md` mapping all four, with a pointer near the top of that document so it's discoverable by anyone who follows a migration comment there. Repo-wide search confirmed these were the only remaining occurrences beyond what last round already fixed — no fifth one found. Also fixed `REVIEW_REQUEST_ROUND8.md`'s archival note, which hardcoded "three further rounds" (now four, and counting) — replaced with wording that doesn't need updating every time this cycle continues.

**Verification**: complete diff from `1cbe4a9` inspected, not just individual files; `git diff --check` clean; `npm run typecheck`/`npm run lint` clean; `npm run build` clean, 19 routes; **no application code, migration, workflow, dependency, or lockfile changed this round** — confirmed via `git status`/`git diff --stat` against the actual diff (only `STATUS.md`, `REVIEW_REQUEST.md`, and `REVIEW_REQUEST_ROUND8.md` touched); `supabase migration list` shows all 23 migrations still matching remote; `supabase db advisors` reports no issues.

`npm run test` (unit): **245/245**, unchanged.

`npm run test:e2e` (135 tests, 17 files, unchanged): the initial default-parallel run genuinely had failures, reported honestly rather than only the recovered result — **86 passed, 27 failed, 22 skipped**, every failure `AuthApiError: Request rate limit reached` from Supabase Auth's shared rate limiter on the isolated project, across the same 5 files this has affected in every previous round (`notification-email-verification`, `onboarding-tour`, `organization-onboarding`, `redirect-safety`, `tenant-isolation`); the 22 skips are the direct-Postgres-connection tests, since that first run didn't have `SUPABASE_DB_URL` exported. Re-running those exact 5 files serially (`--workers=1`, `SUPABASE_DB_URL` set): **69/69**, zero failures. The remaining 12 files, run together separately with the same DB connection available: **66/66**, including `rpc-privilege-matrix.spec.ts`'s full role-matrix + completeness check and a full `review-gating.spec.ts` re-run confirming the Google Review CTA remains unconditionally available at every rating. Combined recovered total: **135/135** — reported as recovered, not as a clean first pass. (These runs verify the *existing* application code is unaffected by this round's doc-only changes — they do not, and cannot, verify anything about the not-yet-implemented domain checklist, which requires real external infrastructure this local suite has no access to.)

**External configuration still required**: the full 13-step checklist in the "Fourth onboarding-tour review response" entry below, with step 5's corrected value — none attempted, none claimed as done. Supabase Auth's *current* Site URL/redirect-URL configuration still hasn't been inspected directly in the dashboard — this round only corrected what the checklist *recommends* adding, not what's already there.

**Not part of this pass**: merging, applying anything to production, altering DNS, or touching any external service.

## Fourth onboarding-tour review response — 3 findings (2026-09-06, not yet merged)

An independent review of `24ed23f..cb5ab3b` found 3 further issues, all confirmed by independently checking the actual code (not the requested wording) before fixing:

1. **The previous round's own domain checklist confused a GitHub Actions repository variable with a Vercel environment variable, and was missing a required redeployment step.** Verified directly: `.github/workflows/ci.yml` reads `PRODUCTION_HEALTH_URL` as `${{ vars.PRODUCTION_HEALTH_URL || ... }}` — a **GitHub Actions repo variable**, set in the repo's own Settings → Secrets and variables → Actions, not anywhere in Vercel. `NEXT_PUBLIC_SITE_URL` genuinely is the Vercel one (confirmed against every call site: `features/auth/actions.ts`'s signup confirmation link, `features/organizations/notification-email-verification.ts`'s confirmation link, `features/notifications/negative-feedback-alert.ts`'s dashboard link, `app/dashboard/nfc-cards/page.tsx`'s public NFC URL, and `lib/safe-redirect.ts`'s trusted-origin default). The previous checklist also placed the Supabase Auth redirect-URL update too late (after the switch, not before — risking generated links Supabase would reject) and never mentioned that changing a Vercel env var doesn't touch an already-built deployment, so `NEXT_PUBLIC_SITE_URL` would silently keep resolving to the old value until a new deploy actually runs. The previous round's own checklist is marked superseded in place (see above); the corrected, 13-step version is below.
2. **`REVIEW_REQUEST.md` claimed "23 migrations, none applied to production," which is false.** Verified directly: `master` at `443ea98` has exactly 17 migration files (confirmed via `git ls-tree`), matching this document's own "Merged to production (2026-09-05)" entry's claim that "All 17 migrations... are now applied to production" after PR #2 merged. The 6 migrations this branch adds on top (18–23) are the ones genuinely pending, applied only to the isolated test project. Corrected to state this exactly.
3. **`STATUS.md` had several references to `REVIEW_REQUEST.md`'s old §5/§6 sections that no longer exist there** — that document has been substantially rewritten twice since those lines were written (once into a consolidated handoff, and its round-8 content moved to `REVIEW_REQUEST_ROUND8.md`). Each stale reference was individually traced to the specific historical commit whose `REVIEW_REQUEST.md` version it actually meant (verified by reading that commit's own file, not guessed): two resolve to content now archived at `REVIEW_REQUEST_ROUND8.md` (still directly readable in this repo); the rest — from rounds that were never separately archived — now link to that exact commit's version on GitHub (commit-pinned, so the link can't drift the way the bare filename did) instead of the current, unrelated file at that path. One of these (the migration-replay citation) turned out to have already been citing the wrong section number (`§ 5` instead of the actual `§ 4`) even before this staleness existed — corrected to the right one while fixing the resolvability. Also fixed `REVIEW_REQUEST.md`'s own history table: it had mapped commit `5c67f9c` to the "Second onboarding-tour review response" entry and `24ed23f` to the same entry — `5c67f9c` actually belongs to the (unprefixed) "Onboarding-tour review response — 6 findings" entry, and `24ed23f` is what "Second onboarding-tour review response — 5 findings" actually describes. Added the missing `cb5ab3b` row.

### Domain map and safe external-configuration checklist (corrected, current)

**What exists right now, independently verified:**
- `velemenytap.hu` (and presumably `www.velemenytap.hu`) — a **Shopify** storefront, password-protected, titled "Vélemény Tap." Almost certainly the store selling the physical NFC cards. **Must stay pointed at Shopify.**
- `veleminytap.vercel.app` — the current, actual production address for this Next.js SaaS app.

  **Correction (2026-09-09).** A previous round recorded that the Vercel project had been renamed `veleminytap` → `velemenytap` and that every code and doc reference had been updated to match. **The rename never happened.** `.vercel/project.json` still reads `"projectName": "veleminytap"`, `https://veleminytap.vercel.app/api/health` answers, and `https://velemenytap.vercel.app` returns **404**. The documentation was written as though an owner action only the owner could perform had already been performed.

  Not merely cosmetic: `scripts/rollout-environments.json` — the real production rollout configuration — had both `allowedOrigin` and `healthUrl` pointing at the 404 host, so `scripts/rollout.mjs` would have health-gated the production migration rollout against a URL that does not exist, and both GitHub Actions workflows fell back to it when their repository variables are unset. All corrected to the address that actually serves. (Both workflows use `curl -fsS`, so they would have failed loudly rather than passing silently — but for a reason that looked nothing like the cause.)

  **Renaming the Vercel project is an open owner decision, not a completed step.** It is a real, breaking change to a live URL. If it is done, `NEXT_PUBLIC_SITE_URL`, the Supabase Auth `site_url` and its `uri_allow_list`, the Stripe webhook endpoint and the two workflow variables all have to move together. Confirmed pre-launch (no physical NFC cards issued to real customers yet, so no already-distributed card could break) before renaming. This closes the "legacy misspelling" every prior round from here down had deliberately left alone — the note in each of those historical entries below ("still correct and working; not changed by this round") describes the state as of ITS OWN round, not the current one.
- **`PRODUCTION_HEALTH_URL` is a GitHub Actions repository variable** (`.github/workflows/ci.yml`'s `verify-production-deployment` job reads `${{ vars.PRODUCTION_HEALTH_URL }}`), configured in this repo's own GitHub Settings, **not** in Vercel.
- **`NEXT_PUBLIC_SITE_URL` is the Vercel environment variable** — read at request time by every generated-link call site listed in finding 1 above, and by `lib/safe-redirect.ts`'s trusted-origin check.

**What's proposed, not yet configured anywhere:**
- `app.velemenytap.hu` — a subdomain for this SaaS, correctly spelled, sitting alongside the Shopify storefront without touching its apex/`www` records. **Nothing in Vercel, DNS, GitHub Actions, or this repo treats this as live yet.**

**Safe ordered checklist (all external, owner-only — dashboard/DNS/GitHub access this repo's code cannot reach):**
1. Confirm with whoever manages the domain that the apex/`www` must stay pointed at Shopify — don't proceed past this on assumption alone.
2. Add `app.velemenytap.hu` as a custom domain in the correct Vercel project (Settings → Domains).
3. Create only the DNS record Vercel asks for to verify `app` — a subdomain record, not touching the existing apex/`www` records Shopify depends on.
4. Verify the existing deployment is actually reachable on `app.velemenytap.hu` (before changing anything that points at it).
5. Add `https://app.velemenytap.hu/auth/confirm**` to Supabase Auth's allowed redirect URLs — **not the bare origin**, and not `**` at the root either (unnecessarily broad; see the reasoning below). **Keep whatever entry currently allows the existing `veleminytap.vercel.app` callback in the list too** during stabilization. Done before the next step, so links generated against the new origin aren't rejected by Supabase the moment they start being generated.
6. Set Vercel's **production** `NEXT_PUBLIC_SITE_URL` to `https://app.velemenytap.hu`.
7. **Trigger a new production deployment.** A Vercel environment variable change does not update an already-built deployment on its own — skipping this step means the app keeps generating links against the old value indefinitely, silently.
8. Verify `/api/health` and every important generated-link path on the new deployment: notification-email confirmation (`features/organizations/notification-email-verification.ts`), negative-feedback alert/dashboard links (`features/notifications/negative-feedback-alert.ts`), and NFC/public feedback URLs (`app/dashboard/nfc-cards/page.tsx`). **Signup confirmation specifically requires a real round trip, not just a code inspection**: actually sign up a test account, receive the real confirmation email, click through it, and confirm it lands back on `app.velemenytap.hu` and reaches `/auth/confirm` successfully — this is the one path step 5's redirect-allowlist entry actually gates, so it's the one that would silently break if that entry is wrong.
9. Update the GitHub Actions repository variable `PRODUCTION_HEALTH_URL` to the full `https://app.velemenytap.hu/api/health` — **this is a GitHub setting** (Settings → Secrets and variables → Actions → Variables), not a Vercel one; setting it in Vercel by mistake does nothing for this workflow.
10. Update `scripts/rollout-environments.json`'s `allowedOrigin`/`healthUrl` to the exact new values (the actual production rollout manifest `scripts/rollout.mjs` reads).
11. Update this repo's own documentation and examples (`DEPLOYMENT.md`, `README.md`) to reference `app.velemenytap.hu` instead of the legacy `.vercel.app` address.
12. Change Supabase Auth's **primary Site URL** only once the new deployment is verified stable — keep the old address in the allowed-redirects list during this stabilization window, don't remove it the moment the primary Site URL switches.
13. Keep `veleminytap.vercel.app` reachable (fallback or redirect) until `app.velemenytap.hu` has been stable for a while and every check (CI, rollout manifest, docs) actually uses the new domain — don't remove the working address the moment the new one first resolves.

None of these 13 steps have been performed or claimed as done. This repo's own text (steps 10–11) must only change *after* the corresponding external step is actually verified, not in anticipation of it. Step 9 in particular is easy to get backwards a third time — say it plainly: **GitHub setting, not Vercel.**

**Why step 5's value is `https://app.velemenytap.hu/auth/confirm**`, not the bare origin (found by an independent review: an earlier draft of this checklist told the owner to allowlist just `https://app.velemenytap.hu`, which Supabase does not treat as covering every path beneath it).** Verified directly against `features/auth/actions.ts`, the only `emailRedirectTo`/`redirectTo` call site anywhere in this codebase (confirmed by grepping the whole tree): it sends `` `${NEXT_PUBLIC_SITE_URL}/auth/confirm?next=/onboarding` ``, which after the domain switch is exactly `https://app.velemenytap.hu/auth/confirm?next=/onboarding` — a full path plus query string, not a bare origin. Read Supabase's current redirect-URL documentation before choosing the pattern: the allowlist matches the full URL (not just the origin), and supports glob wildcards — `*` for "any sequence of non-separator characters," `**` for "any sequence of characters" including separators, with `.` and `/` as the defined separators. That distinction matters here specifically: the query string this app sends is `?next=/onboarding`, which itself contains a `/` — a single `*` would stop at that slash and fail to match the full value, so it has to be `**`. Supabase's own docs don't explicitly state whether the query string is required in the allowlist pattern at all (it's possible the matcher ignores it and matches on path alone) — rather than guess, `/auth/confirm**` is the value documented here because it's correct under *either* reading: if the query string turns out to be ignored, the trailing wildcard is harmless; if it's required, `**` reliably covers it, embedded slash included. This is also why step 8 above requires an actual signup round trip, not just this reasoning, before treating it as confirmed. Narrower than the bare origin, and narrower than a root-level `https://app.velemenytap.hu/**`, which would allow every path on the subdomain rather than just the one callback the application actually generates.

**Verification**: complete diff from `cb5ab3b` inspected, not just individual files; `git diff --check` clean; `npm run typecheck`/`npm run lint` clean; `npm run build` clean, 19 routes; **no migration file, and no production application code, changed this round** (confirmed via `git status`/`git diff --stat` against the actual diff — this was a documentation-only pass); `supabase migration list` shows all 23 migrations still matching remote; `supabase db advisors` reports no issues.

`npm run test` (unit): **245/245**, unchanged — no application code touched this round.

`npm run test:e2e` (135 tests, 17 files, unchanged this round): the initial default-parallel run genuinely had failures, reported honestly rather than only the recovered result — **90 passed, 24 failed, 21 skipped**, every failure `AuthApiError: Request rate limit reached` from Supabase Auth's shared rate limiter on the isolated project, across the same 5 files this has affected in every previous round (`notification-email-verification`, `onboarding-tour`, `organization-onboarding`, `redirect-safety`, `tenant-isolation`); the 21 skips are the direct-Postgres-connection tests, since that first run didn't have `SUPABASE_DB_URL` exported. Re-running those exact 5 files serially (`--workers=1`, `SUPABASE_DB_URL` set): **69/69**, zero failures, including every test that failed in the parallel run. The remaining 12 files, run together separately with the same DB connection available: **66/66**, including `rpc-privilege-matrix.spec.ts`'s full role-matrix + completeness check and a full `review-gating.spec.ts` re-run (all five ratings + the duplicate-submission test) confirming the Google Review CTA remains unconditionally available at every rating. Combined recovered total: **135/135** — reported as recovered, not as a clean first pass.

**External configuration still required**: everything in the checklist above — none attempted, none claimed as done. Stripe: still not applicable, not yet implemented. Resend/Sentry: unchanged, already correct from an earlier round. Supabase Auth Site URL/redirect URLs: still not inspected directly in the dashboard.

**Not part of this pass**: merging, applying anything to production, altering DNS, or touching any external service.

## Third onboarding-tour review response — 3 findings (2026-09-06, not yet merged)

An independent review of `5c67f9c..24ed23f` confirmed the runtime changes sound, with no tenant-isolation/security regression, and found 3 further issues. Fixed:

1. **The previous round's own external-configuration recommendation was actively unsafe**: it suggested pointing `velemenytap.hu` at the Vercel project. An independent live check on 2026-09-06 found `velemenytap.hu` already redirects to a Shopify password-protected storefront ("Vélemény Tap") — presumably selling the physical NFC cards this SaaS's customers tap. Following the previous recommendation would have disconnected a live store. Corrected in place (the previous round's own paragraph now says not to follow it) and replaced with the actual domain map and a safe, ordered checklist — see below.
2. **The "membership removed" e2e test doesn't exercise the zero-row-then-read-back branch it was described as covering** — it removes membership *before* the Server Action runs, so `getCurrentOrganization()` itself returns `null` and the function returns from its earlier, separate guard, never reaching the UPDATE or the follow-up read at all. Kept as genuine, useful membership-loss coverage, but its own comment and this document's description of it were corrected to say what it actually tests. Added `features/onboarding-tour/actions.test.ts` — 8 focused unit tests exercising every branch of the zero-row disambiguation deterministically, via `vi.mock` on `@/lib/supabase/server` and `@/features/organizations/current`, with a chainable mock query builder standing in for the Supabase client (no test-only production hooks, no reimplementation of the logic under test).
3. **`REVIEW_REQUEST.md` was stale and actively misleading**: it still presented round 8 (`a7aa906`, 22 migrations, 108 e2e tests, a specific CI run link) as the current handoff, despite the file's own text claiming to reflect current state — dangerous specifically because every round's own instructions tell the next reviewer to read it first. The round-8 content is preserved at `REVIEW_REQUEST_ROUND8.md`; `REVIEW_REQUEST.md` is now a short, current, consolidated handoff pointing at `STATUS.md` for the round-by-round detail rather than duplicating it, using this round's actual counts (23 migrations, 135 e2e tests going in), and identifying the CI run it links by commit rather than implying it's permanently current.

Also fixed: the "missing the `y`" explanation of the `veleminytap`/`velemenytap` typo from the previous round's own entry was itself wrong — both spellings contain `y` (`...nytap`); the actual difference is the vowel just before it, incorrect `i` vs correct `e`. Corrected at its one occurrence (`STATUS.md`'s previous round entry, below); a repo-wide search confirmed no other file repeated it.

### Domain map and safe external-configuration checklist (supersedes the previous round's recommendation) — **CORRECTED AGAIN by the next round below; the 13-step checklist there is current, this 10-step one is not**

An independent review after this round found two more problems with the checklist below: it told the owner to update Vercel's `PRODUCTION_HEALTH_URL` (step 5) — that variable is actually read by `.github/workflows/ci.yml` as a **GitHub Actions repository variable**, not a Vercel one (confirmed directly in the workflow file: `${{ vars.PRODUCTION_HEALTH_URL || ... }}`); and it never told the owner to trigger a new Vercel deployment after changing `NEXT_PUBLIC_SITE_URL`, even though a Vercel env var change has no effect on an already-built deployment. See "Domain map and safe external-configuration checklist" in the next dated section below for the corrected, 13-step version.

**What exists right now, independently verified:**
- `velemenytap.hu` (and presumably `www.velemenytap.hu`) — a **Shopify** storefront, password-protected, titled "Vélemény Tap." Almost certainly the store selling the physical NFC cards. **Must stay pointed at Shopify.**
- `veleminytap.vercel.app` (legacy misspelling) — the current, actual production address for this Next.js SaaS app, per `DEPLOYMENT.md`/`README.md`/`scripts/rollout-environments.json`. Still correct and working; not changed by this round.

**What's proposed, not yet configured anywhere:**
- `app.velemenytap.hu` — a subdomain for this SaaS, correctly spelled, that would sit alongside the Shopify storefront without touching its apex/`www` records. **Nothing in Vercel, DNS, or this repo treats this as live yet** — it is not referenced by `scripts/rollout-environments.json`, `NEXT_PUBLIC_SITE_URL`, or `PRODUCTION_HEALTH_URL`, and none of those should change until every step below is done and verified.

**Safe ordered checklist (all external, owner-only — dashboard/DNS access this repo's code cannot reach):**
1. Confirm with whoever manages the domain that the apex/`www` must stay pointed at Shopify — don't proceed past this on assumption alone.
2. Add `app.velemenytap.hu` as a custom domain in the Vercel project (Settings → Domains).
3. Create only the DNS record Vercel asks for to verify `app` — a subdomain record, not touching the existing apex/`www` records Shopify depends on.
4. Verify the deployment actually serves through `app.velemenytap.hu`, including `https://app.velemenytap.hu/api/health` reporting `ok: true`.
5. Only then update Vercel's `NEXT_PUBLIC_SITE_URL` and `PRODUCTION_HEALTH_URL` to `app.velemenytap.hu`.
6. Update Supabase Auth's Site URL and allowed redirect URLs to match.
7. Update `scripts/rollout-environments.json`'s `allowedOrigin`/`healthUrl` (production rollout manifest — get this right before the next real rollout, not after).
8. Update this repo's own documentation and examples (`DEPLOYMENT.md`, `README.md`) to reference `app.velemenytap.hu` instead of the legacy `.vercel.app` address.
9. When Stripe Checkout/Customer Portal/webhooks are eventually implemented (not yet — see `PRODUCT_SPEC.md`), point their return/webhook URLs at `app.velemenytap.hu`, not the Shopify domain or the legacy Vercel address.
10. Keep `veleminytap.vercel.app` reachable (fallback or redirect) until `app.velemenytap.hu` has been stable for a while — don't remove the working address the moment the new one first resolves.

None of steps 2–10 have been performed or claimed as done. This repo's own text (steps 7–8) must only change *after* the corresponding external step is actually verified, not in anticipation of it.

**Verification**: complete diff from `24ed23f` inspected, not just individual files; `git diff --check` clean; `npm run typecheck`/`npm run lint` clean; `npm run build` clean, 19 routes; `supabase migration list` shows all 23 migrations still matching remote — **no migration file touched or renamed this round**; `supabase db advisors` reports no issues.

`npm run test` (unit): **245/245** (up from 236 — the 9 new tests are `features/onboarding-tour/actions.test.ts`, 10 files total up from 9).

`npm run test:e2e` (135 tests, 17 files, unchanged this round — no e2e test cases added or removed, only comments/titles corrected): the initial default-parallel run genuinely had failures, reported honestly rather than only the recovered result — **90 passed, 23 failed, 22 skipped**, every failure `AuthApiError: Request rate limit reached` from Supabase Auth's shared rate limiter on the isolated project, across the same 5 files this has affected in every previous round (`notification-email-verification`, `onboarding-tour`, `organization-onboarding`, `redirect-safety`, `tenant-isolation`); the 22 skips are the direct-Postgres-connection tests, since that first run didn't have `SUPABASE_DB_URL` exported. Re-running those exact 5 files serially (`--workers=1`, `SUPABASE_DB_URL` set): **69/69**, zero failures, including every test that failed in the parallel run. The remaining 12 files, run together separately with the same DB connection available (so their connection-requiring tests actually ran instead of skipping): **66/66**, including `rpc-privilege-matrix.spec.ts`'s full role-matrix + completeness check, and a full `review-gating.spec.ts` re-run (all five ratings + the duplicate-submission test) confirming the Google Review CTA is still unconditionally available at every rating. Combined recovered total: **135/135**. Restated per this round's own explicit instruction: the *initial* parallel run was not clean (23 real failures observed, all attributable to the documented shared rate limiter, not a code defect) — the 135/135 figure is the result *after* the required serial re-runs, not a claim that the first pass was already green.

**External configuration still required**: everything in the checklist above (steps 2–10) — none attempted, none claimed as done. Stripe: still not applicable, not yet implemented. Resend/Sentry: unchanged, already correct from an earlier round.

**Not part of this pass**: merging, applying anything to production, altering DNS, or touching any external service.

## Second onboarding-tour review response — 5 findings (2026-09-06, not yet merged)

An independent review of `ba97a9e..5c67f9c` found 5 further low-severity issues. Fixed:

1. **A remaining email-delivery overclaim in `app/page.tsx`'s "Te mindent látsz" step** — "e-mailes értesítést is kapsz" still promised delivery for every low rating regardless of configuration, cooldown, budget, or send failure. Changed to "e-mailes értesítést is beállíthatsz", matching the phrasing already used in the dashboard-card sentence fixed in the previous round. Every other email claim on the homepage and in the tour's own steps was re-read; none promises unconditional or immediate delivery.
2. **The final step's own action button ("Helyszín hozzáadása") lost its navigation intent across a failed-then-retried save** — the first `complete()` call awaited the write and navigated on success, but "Próbáld újra" re-enters through the provider's own `retry()`, not back through the button, so a failed first attempt followed by a successful retry persisted `completed` but never navigated. Fixed by carrying the intended destination inside the pending-close state itself (`PendingClose.navigateTo`), so whichever attempt actually succeeds — first try or a later retry — is the one that calls `router.push`. Moved navigation ownership from `tour-dialog.tsx` into `tour-provider.tsx`'s `attemptClose`, the single choke point every close attempt (and its retries) passes through. Covered by 2 new e2e tests: the action-button path (fails, retries, navigates exactly once) and its mirror (the ordinary Bezárás button, which must never navigate, on the first attempt or after a retry).
3. **`setOnboardingTourStatusAction` treated every zero-affected-row UPDATE result as success**, on the assumption that the WHERE clause's own "already completed" guard was the only way to get zero rows. It isn't: a membership revoked between the organization lookup and the UPDATE (RLS no longer permitting the write) produces the same zero-row result, and is a real failure, not a no-op — previously reported as success regardless, so the client would close the tour having persisted nothing. Fixed: a zero-row result now triggers a follow-up read of the row through the same RLS-bound client; only a confirmed `"completed"` value is treated as success, everything else (unreadable, gone, or readable-but-not-completed) returns the normal error. No new RPC. Covered by 3 new e2e tests: normal single-row success, a concurrent-already-completed zero-row case (success), and a zero-row case with the membership removed first (error) — see the tests' own comments for why the exact single-function-call race (membership vanishing between the two calls, microseconds apart) has no black-box e2e hook, and what was tested instead to cover the same required outcome.
4. **Inaccurate test/doc claims from the previous round's own fixes**: `STATUS.md` claimed "9 new tests" for a round that actually added 7 (now corrected below, and corrected again below for this round's own additions); `e2e/onboarding-tour.spec.ts`'s file header still described the (already-fixed) migration as doing a one-time bulk `UPDATE`, rather than the two-default metadata-only pattern it was rewritten to use; the CHECK-constraint test's own comment claimed a forged POST to the Server Action's endpoint bypasses its validation — false, a raw POST still invokes the real function body, guard included. The test actually bypasses the Server Action entirely, writing straight through the RLS-bound client with no action call at all — corrected the test's name and comment, and the identical claim in `e2e/README.md`.
5. **`veleminytap` (incorrect `i`) vs the correct `velemenytap` (`e`) spelling** — both spellings carry the `y` (`...nytap`); the actual typo is the vowel right before it, `i` instead of `e`. A repo-wide case-insensitive search found this confined entirely to technical identifiers — never in customer-facing text, which was already correct throughout (`VéleményTap`, `vélemény`, etc. all already spelled correctly). Fixed where safe: `package.json`'s `name` (and `package-lock.json`, regenerated via `npm install`, not hand-edited), `supabase/config.toml`'s local `project_id` (a Docker/CLI namespace label, not a real resource reference), and 3 test fixture files (`lib/safe-redirect.test.ts`, `lib/sentry-redact.test.ts`, `scripts/rollout.test.ts` — pure logic tests using an arbitrary-but-realistic domain literal, decoupled from any real external resource). **Deliberately NOT changed** — see "External configuration still required" in the review response for this round for the full, individually-reasoned list: the top-level `veleminytap/` directory name and every doc reference to it or to the matching Vercel "Root Directory" setting (the task's own instruction: don't rename the directory as part of this commit), and the real, currently-live Vercel project domain (`veleminytap.vercel.app`) as it appears in `DEPLOYMENT.md`'s env var documentation, `README.md`, `scripts/rollout-environments.json` (the actual production rollout manifest), and `STATUS.md`'s own historical narrative — changing these without the underlying Vercel project/DNS actually being corrected externally would make the docs describe infrastructure that doesn't exist, and for the rollout manifest specifically, would break the real rollout script's health-check target. The already-correctly-spelled `velemenytap.hu` Resend sending domain (confirmed live in an earlier round, `STATUS.md` below) was not touched.

**Verification**: complete diff from `5c67f9c` inspected, not just individual files; `git diff --check` clean; `npm run typecheck`/`npm run lint` clean; `npm run test` 236/236 (unchanged); `npm run build` clean, 19 routes (confirms `package.json`'s rename didn't break anything); `supabase migration list` shows all 23 migrations still matching remote (this round touched no migration); `supabase db advisors` reports no issues. `npm run test:e2e`: suite grew from 130 to 135 tests (`onboarding-tour.spec.ts` 21→26). A parallel run hit the same documented shared Supabase-Auth rate-limiter flakiness as every previous round, on the same kind of files (`notification-email-verification`, `onboarding-tour`, `organization-onboarding`, `redirect-safety`, `tenant-isolation`); all 5 re-run serially (`--workers=1`, `SUPABASE_DB_URL` set) came back **69/69**, including every new test. The remaining 12 spec files not touched by that flakiness (including `rpc-privilege-matrix.spec.ts`, `public-submission-safety.spec.ts`, and a full re-run of `review-gating.spec.ts` — all five ratings plus the duplicate-submission test, confirming no regression to the Google-review no-gating invariant) were run together separately: **66/66**. Combined: **135/135, zero unexplained failures.** One genuine self-caught issue during this verification: two of the new zero-row-disambiguation tests initially asserted `getByRole("alert")` has count 0 as a "no error" check, which falsely failed against Next.js's own accessibility route-announcer (also `role="alert"`, unrelated to the tour) — fixed by relying on the already-present `dialog` count-0 assertion instead, which structurally proves the same thing without the false positive.

**External configuration still required — CORRECTED by the next round below, do not follow the recommendation this paragraph originally made.** An independent live check on 2026-09-06 found `velemenytap.hu` already redirects to a Shopify password-protected storefront ("Vélemény Tap" — presumably selling the physical NFC cards), which this round did not know when it first recommended pointing `velemenytap.hu` at the Vercel project directly — doing that would have disconnected the Shopify store. **Do not point the apex/`www` domain at Vercel.** See "Domain map and safe external-configuration checklist" in the next dated section below for the corrected plan (a subdomain, `app.velemenytap.hu`, for the SaaS instead) and for what's still genuinely required from Supabase Auth/Stripe/Resend/Sentry.

**Not part of this pass**: merging, applying anything to production, or deploying.

## Onboarding-tour review response — 6 findings (2026-09-06, not yet merged)

An independent review of the onboarding-tour work (below) found 6 confirmed issues, none rejected. Fixed:

1. **`DEPLOYMENT.md`'s rollout example was missing migrations 22 and 23 from `--expand`** — both are genuinely pending against production and neither was in the corrected command. Migration 23 in particular is not optional to include: the new dashboard code queries `organizations.onboarding_tour_status` on every request, so it must exist before that code deploys, or every dashboard page load breaks. Fixed in `DEPLOYMENT.md` § 7, propagated to `REVIEW_REQUEST_ROUND8.md` § 6 (that content has since moved from `REVIEW_REQUEST.md`, which is now a different, current document — see its own top-of-file note) and `DATABASE_SCHEMA.md`'s rollout-ordering section.
2. **The homepage's tour-adjacent copy overclaimed guaranteed-immediate email delivery** ("azonnal e-mailt kap", "e-mailben értesítjük") for a feature that has a cooldown/budget and async delivery — not actually immediate or unconditional. Softened to "e-mailes értesítést is kap(hat)sz" in `app/page.tsx` and the tour's own Settings step (`features/onboarding-tour/tour-steps.ts`).
3. **Migration `20260906130000` (adds `onboarding_tour_status`) ran a bulk `UPDATE` over every existing organization**, bumping `updated_at` on all of them via the existing `organizations_set_updated_at` trigger for a reason unrelated to anything the organization itself changed. Never applied to production, so rewritten in place (same reasoning as migration 18): add the column with `default 'completed'` (Postgres 11+ applies a constant `NOT NULL` default as a metadata-only catalog change, not a per-row rewrite, so `updated_at` is untouched for existing rows), then a second statement changes the default to `'not_started'` for future inserts only. Verified directly against the isolated project — see `DATABASE_SCHEMA.md` § "Onboarding tour state".
4. **The tour's close/skip/complete path fired its persistence call without awaiting it**, closing the dialog immediately regardless of outcome — a failed save was indistinguishable from a successful one, and the Server Action itself trusted its `status` parameter's TypeScript type with no runtime check (a forged direct POST could send any string). Fixed: `features/onboarding-tour/actions.ts` now validates `status` at runtime and applies the state transition atomically in the `UPDATE`'s own `WHERE ... <> 'completed'` clause (correct even under a stale/multi-device client belief); `tour-provider.tsx`/`tour-dialog.tsx` now await the save, show a "Mentés folyamatban…" state while in flight, and surface a real retry/close-anyway choice on failure rather than silently discarding it. **Found during this round's own e2e verification, not the original review**: the first version of that fix only handled the Server Action *resolving* with `{ error }` — a genuine network failure (dropped connection, aborted request) makes the call *reject* instead, which was still an uncaught rejection. `tour-provider.tsx`'s `attemptClose` now wraps the call in `try`/`catch` and routes a thrown failure through the identical retry/close-anyway UI, proven by a new e2e test that aborts the action's own request with Playwright's `page.route`.
5. **Broader copy pass**: fixed the incorrect bare-stacked `Google-értékelés link`/`Google-értékelés lehetőség` compound (needs the adjectival `-i`: `Google-értékelési link`) in `app/page.tsx` and `features/marketing/rating-demo.tsx`, plus a few other homepage strings tightened for the same "don't overclaim, don't read like AI-generated boilerplate" standard applied to the rest of the page.
6. **Test/doc coverage**: `e2e/onboarding-tour.spec.ts` had a mobile-menu test whose title didn't match what it actually verified (it dismissed the tour before opening the menu, so it wasn't testing "while the tour is open" at all) — renamed to describe what it actually checks, not counted as a new test. The suite grew from 123 to 130 tests: **7 net-new test cases** — backdrop-dismissal persistence, reopening a `skipped` tour (both re-dismissing it and finishing it to promote it to `completed`, 2 tests), a network failure during close showing retry/close-anyway rather than silently discarding the attempt (both outcomes, 2 tests), the CHECK constraint rejecting a forged status value (this one bypasses the Server Action entirely via a direct authenticated Supabase update through the RLS-bound client — a raw POST to the action's own endpoint id would still run the action's validation, so that's not what this test does), all in `onboarding-tour.spec.ts` (15→21, 6 new), plus the cross-tenant write case in `tenant-isolation.spec.ts` (1 new). Separately, the existing "reopen after completion" test was **strengthened** (not counted as new) with an explicit status assertion, since it previously only checked that the dialog didn't reappear — which a wrongly-downgraded `skipped` status would also satisfy. `TEST_PLAN.md`/`e2e/README.md` updated to reflect the new file and test count.

**Verification**: `git diff --check` clean; `npm run typecheck`/`npm run lint` clean; `npm run test` 236/236 (unchanged — this round's changes are e2e-covered, not unit-covered); `npm run build` clean, 19 routes; `supabase migration list` shows all 23 local migrations matching remote by timestamp on the isolated project (editing migration 23's contents in place doesn't change its timestamp); `supabase db advisors` reports no issues. `npm run test:e2e`: full suite is now 130 tests across 17 files (`onboarding-tour.spec.ts` grew from 15 to 21 tests this round, `tenant-isolation.spec.ts` gained 1). Confirmed 130/130 passing, across three runs needed to get a clean, non-flaky picture: a parallel run hit the documented shared Supabase-Auth rate-limiter flakiness (`STATUS.md`'s round-5 note) on 22 tests across 5 files under concurrent sign-ins; every one of those files was re-run serially (`--workers=1`) and passed cleanly, including all new tests; `rpc-privilege-matrix.spec.ts`/`public-submission-safety.spec.ts`/`review-gating.spec.ts` (needing a direct Postgres connection or otherwise not touched by the parallel run's failures) were separately confirmed passing with `SUPABASE_DB_URL` present, including a full re-run of `review-gating.spec.ts` (all five ratings plus the duplicate-submission test) confirming no regression to the Google-review no-gating invariant.

**Not part of this pass**: merging, applying anything to production, or deploying.

## First-time dashboard onboarding tour (2026-09-06, not yet merged)

A short, optional, dismissible guided tour that auto-opens the first time a brand-new organization's dashboard loads: a welcome dialog, then 7 steps (Áttekintés, Helyszínek, NFC-kártyák, Vélemények, Elemzés, Beállítások, and a final step nudging toward creating the first location), each highlighting its real nav link via a `data-tour` attribute — never text-matching, and never pointing at a link the responsive nav has actually hidden (below the `xl` breakpoint, the same step renders as a plain centered dialog instead). Built entirely from this project's own existing UI primitives (`Dialog`, `Button`) — no new UI library.

State (`organizations.onboarding_tour_status`: `not_started` | `completed` | `skipped`, migration `20260906130000`) is persisted server-side, scoped to the organization (not the signed-in user — see `DATABASE_SCHEMA.md` § "Onboarding tour state" for why), so it survives a different device or browser and correctly protects every organization that already existed before this shipped (backfilled to `completed` in the same migration) from ever seeing it appear unprompted. A small "Útmutató megnyitása" button in the dashboard header reopens it anytime without touching the persisted state.

Building this surfaced and fixed a real, pre-existing test-fixture gap: `e2e/support/seed.ts`'s `seedOrgWithMember()` — used by dozens of unrelated specs that sign in and interact with the dashboard immediately — now defaults every seeded organization's tour status to `completed` rather than the column's own `not_started` default, specifically so those pre-existing specs don't get a modal dialog they never anticipated sitting on top of the page. Only `e2e/onboarding-tour.spec.ts` itself, and the one real-onboarding-flow test in `organization-onboarding.spec.ts` that goes through the actual signup UI (and therefore gets a genuinely fresh organization from `create_organization_atomic`, not the test helper), opt into or account for the `not_started` case.

**Verification**: `npm run typecheck`/`npm run lint` clean, `npm run test` 236/236, `npm run build` clean (19 routes, unchanged), `supabase db advisors` clean against the isolated project. `npm run test:e2e`: 123/123 across 17 files (up from 122/16 — one new spec, `onboarding-tour.spec.ts`, 15 tests), including a full re-run of `review-gating.spec.ts` (6/6, all five ratings plus the duplicate-submission test) confirming no regression to the Google-review no-gating invariant, and `dashboard-nav-accessibility.spec.ts` (10/10) confirming the pre-existing responsive-nav tests still pass now that the seed helper no longer triggers the tour by accident.

**Not part of this pass**: merging, applying anything to production, or deploying.

## Round-7 review-response pass (2026-09-06, not yet merged)

A seventh-round independent review of PR #3 found 8 further findings (R7-01–R7-08). All 8 verified and fixed; **all 8 confirmed, none rejected** — plus one additional, previously-undiscovered bug found while fixing R7-03 (below). The two most significant:

- **R7-01 (HIGH)** — round 6's Sentry span redaction (R6-02) missed `http.target`, a real OpenTelemetry attribute the installed SDK (10.73.0) attaches to HTTP spans carrying the full raw pathname and query string; `sendDefaultPii: false` was also never actually equivalent to disabling URL/query collection at all. Fixed by extending the redacted attribute set and replacing `sendDefaultPii` with an explicit `dataCollection` block in every runtime config — see `SECURITY.md` for the full attribute inventory and the "setting `dataCollection` resets other defaults" gotcha documented in the config files themselves.
- **R7-03 (MEDIUM), and a more serious bug it led to** — the isolated test project's migration bookkeeping claimed the round-3 3-argument `request_notification_email_change` was applied when the function itself didn't actually exist there (drift from earlier raw-SQL verification work), so a security test had been silently skipping the one function most relevant to round 6's own fix. Recreating it to match production for the first time immediately broke the *entire* notification-email-change feature with `PGRST203` for every caller, old and new application code alike — PostgREST cannot disambiguate a 2-argument call between it and the round-6 2-argument replacement, independent of grants, once both are defined. Fixed by dropping the 3-argument function outright (new migration `20260906100000`) rather than round 6's grant-revocation-only approach — see `DATABASE_SCHEMA.md` § "Rollout ordering" for why this also **changes the production rollout plan for migration 19**: round 6's own handoff had incorrectly claimed migration 19 needed no staged rollout, when in fact it (and now migration 20) must wait for this PR's new application code to deploy first, since production's currently-live code still calls the 3-argument RPC.

Also fixed: R7-02 (a subprocess failure in `scripts/rollout.mjs` could print the production database password through `Error.message`, even though the happy-path log line redacted it), R7-04 (the rollout manifest's `allowedOrigin`/`healthUrl` were recorded but never actually cross-checked, and health polling followed redirects by default), R7-05 (`claim_negative_alert_send` had the identical `now()`-under-concurrency bug round 6 fixed in the notification-email function — found by searching the rest of the schema for the same pattern, fixed in new migration `20260906110000`), R7-06 (a notification-email-change reservation could strand as `'reserved'`, consuming budget for a send that never happened, if token issuance failed after the reservation was created), R7-07 (the health check's "local development" detection was itself fail-open — `NODE_ENV !== "production"` treated an unset, malformed, or `"test"` value the same as genuine local development; flipped to fail-closed on `NODE_ENV === "development"` specifically), R7-08 (several stale documentation claims, listed in `DATABASE_SCHEMA.md`/`SECURITY.md`/`DEPLOYMENT.md`'s own round-7 notes).

**Verification**: see `REVIEW_REQUEST_ROUND8.md` § 4 for exact, fresh results as of this round (typecheck/lint/unit tests/build/e2e/migration replay) — that content has since moved from `REVIEW_REQUEST.md`, which is now a different, current document.

**Not part of this pass**: merging, applying anything to production, or deploying. R4-02 (GitHub branch protection / Vercel Deployment Checks) and R6-06 (`APP_ENV` not yet set in Vercel) both remain open, unconfigurable from this repository — this PR is not merge-ready on that basis alone.

## Round-6 review-response pass (2026-09-06)

A sixth-round independent review of PR #3 found 11 further findings (R6-01–R6-11). All 11 verified and fixed; **all 11 confirmed, none rejected**. The two most significant:

- **R6-01 (HIGH)** — `request_notification_email_change()`, in both its round-3 (already production-deployed) and round-5 forms, returned the raw confirmation token directly to its `authenticated` caller — any org member could call the RPC directly (bypassing the settings form, the Server Action, and Resend entirely), read a live token out of the response, and confirm an arbitrary address without ever proving control of its inbox, defeating the entire point of round 3's confirmation flow. Fixed by splitting token issuance into a new function, `issue_notification_email_change_token()`, granted to `service_role` only; the round-3 3-argument signature's `authenticated` grant is revoked outright (a deliberate, documented exception to a strict compatibility window — see `DECISIONS.md` § "R6-01/R6-03").
- **R6-06 (MEDIUM)** — round 5's own `/api/health` fix (R5-05) rested on a false premise about Vercel: `VERCEL_ENV` is gated by the "Automatically expose System Environment Variables" toggle at build time too, not just runtime, contrary to what R5-05 assumed. Fixed by never trusting any Vercel-managed variable for this decision at all — a new, ordinary (non-system) `APP_ENV` variable, set directly by the owner, is now authoritative. See `DECISIONS.md` § "R6-06" for the full correction, verified against Vercel's own current documentation rather than re-asserted from memory.

Also fixed: R6-02 (Sentry redaction missed `query_string` and never covered transaction/span data at all), R6-03 (the round-5 migration's own signature change wasn't rollout-compatible), R6-04 (rate-limit parameters were caller-suppliable, not server-owned), R6-05 (a TOCTOU race in the confirmation function), R6-07 (the rollout script's origin/health-url trust wasn't bound to the database it authorized), R6-08 (a fork PR's e2e skip could satisfy a required status check), R6-09 (several stale documentation claims), R6-10 (an unguarded `decodeURIComponent` could crash on malformed input), R6-11 (a short commit SHA passed validation but could never match `/api/health`'s response).

**Verification**: exact, fresh results as of this round (typecheck/lint/unit tests/build/e2e) were in `REVIEW_REQUEST.md` § 4 at the time — that content was superseded by later rounds and isn't separately archived; the historical version is still viewable at [github.com/lbenyok/velemenytap/blob/799974d/veleminytap/REVIEW_REQUEST.md](https://github.com/lbenyok/velemenytap/blob/799974d/veleminytap/REVIEW_REQUEST.md) (commit-pinned, not the current file at that path).

**Not part of this pass**: merging, applying anything to production, or deploying. R4-02 (GitHub branch protection / Vercel Deployment Checks) remains open and unconfigurable from this repository — this PR is not merge-ready on that basis alone.

## Round-4 review-response pass (2026-09-05, not yet merged)

A fourth-round independent review found 8 further findings (R4-01–R4-08), directly motivated by the Vercel deployment gap discovered during the round-2/3 rollout (below) and by an unreviewed dashboard restyle that had shipped directly to `master`. All 8 verified and fixed; **all 8 confirmed, none rejected**. In brief:

- **R4-01 (high)** — production had no way to prove which commit it was actually running, which is exactly how the deployment gap went undetected for as long as it did. `/api/health` (new) reports the build's own commit SHA, environment, and expected-migration snapshot, failing loud (503) if release metadata is missing in production/preview rather than silently reporting success. A new CI job polls it after every push to `master` and fails if the live commit doesn't match within 5 minutes — this is what would have caught the actual incident automatically.
- **R4-02** — documented (couldn't configure directly — see `DEPLOYMENT.md`) the GitHub branch-protection and Vercel Deployment Checks settings needed so a deployment can't promote to Production before CI (including Playwright) finishes, plus a Preview-environment configuration pointed at the isolated test project with Resend/Sentry disabled.
- **R4-03** — replaced the manual "move migration files aside, push, restore, eyeball the dashboard" rollout procedure with `scripts/rollout.mjs`: still stages files the same way, but every phase transition is now gated on an automated `/api/health` check, failing closed rather than proceeding on an assumption. Tested end-to-end against the isolated project with real (harmless) pending migrations and a fake passing/failing health server.
- **R4-04** — `SUPABASE_DB_URL` is now mandatory in CI (`e2e/support/db-connection.ts` throws, not skips, when it's missing/unreachable/wrong-project in CI specifically; still optional for local runs), closing a false-green-CI gap for the RPC privilege matrix and location-deactivation concurrency tests.
- **R4-05/R4-06** — the dashboard nav (shipped in the unreviewed restyle) had no accessible name for its six links below `lg`, and could not fit the header row at 320–768px. Rebuilt: every link's icon+label always visible at `lg`+, a labelled menu button (shadcn `Sheet`) below it with the same full-label links, `aria-current="page"` on the active route, keyboard/Escape verified.
- **R4-07** — the same restyle's gradient wordmark used a cyan endpoint with ~1.8:1 contrast against white as small text; replaced with a deeper teal endpoint (~5.4:1, computed, not eyeballed) for the wordmark specifically. The homepage's own hero/nav use the identical original gradient and were **not** touched (out of this round's scope) — see `DECISIONS.md`.
- **R4-08** — corrected stale claims across `STATUS.md`, `TEST_PLAN.md` (hardcoded test counts, references to deleted files), `README.md` (a context-free `vercel deploy --prod`), and added `DEPLOYMENT.md` as the standing deployment runbook this file's own historical entries increasingly needed.

**Verification**: `npm run typecheck` clean, `npm run lint` clean, unit and e2e suites both green (`SUPABASE_DB_URL` exported so the connection-requiring specs actually ran), production build clean. Exact results and what remained manually configured (GitHub branch protection, Vercel Deployment Checks, Preview environment variables, `SUPABASE_DB_URL` as a CI secret if not already added in round 3) were in `REVIEW_REQUEST.md` § 4 at the time — not separately archived; historical version: [github.com/lbenyok/velemenytap/blob/67a8a90/veleminytap/REVIEW_REQUEST.md](https://github.com/lbenyok/velemenytap/blob/67a8a90/veleminytap/REVIEW_REQUEST.md).

**Not part of this pass**: merging, applying anything to production (there are no new migrations this round), or deploying.

## Subscription billing built (2026-09-05, on `feature/billing-subscriptions`, not yet merged or applied to any database)

Explicit user request: a paid subscription, with a real payment page. Built independently of, and in parallel with, `fix/round4-review-findings` (PR #3, the review-response pass immediately above) — both branches started from the same point right after "Merged to production" below. This feature's own review/fix history never went through rounds 5 through 8; it has its own, separate history, archived in full at `REVIEW_REQUEST_BILLING.md`. **Note (added during the PR #3/PR #4 integration round at the top of this file): this entry describes the feature as it was originally built, before the 3 safety fixes (A/B/C) found by that later round's own independent review — see that entry for what changed and why.**

Decisions made with the user before building (`DECISIONS.md` has the full reasoning): one plan, billed monthly (5 990 Ft/hó, ÁFával) or yearly (59 900 Ft/év, ÁFával — roughly 2 months free versus paying monthly), a 14-day no-card trial, and a paywall that gates only the operator dashboard — never the public NFC/feedback pages, since a card already sold and placed on a customer's counter must keep collecting real feedback regardless of its organization's billing status.

Built: `organization_billing`/`stripe_webhook_events` tables plus an auto-provisioning trial trigger (migration `20260905125856`, later renumbered `20260907150000` during the integration round above); `features/billing/` (Stripe Checkout/Billing Portal Server Actions, the `isBillingActive()` gating logic); `app/dashboard/billing/page.tsx` (the payment/plan page); the paywall itself in `app/dashboard/layout.tsx` (protect-by-default, exempting only the billing page); `app/api/webhooks/stripe/route.ts` (signature-verified, idempotent). Full reasoning and security properties in `ARCHITECTURE.md`, `DATABASE_SCHEMA.md`, `SECURITY.md`.

**Verified**: `npm run typecheck`/`lint`/`build` clean; `npm run test` passing (13 new for `isBillingActive`, 9 new for the webhook handler, mocking `stripe`/the admin client — no real Stripe credentials needed for these). The migration (`20260905125856`) has been applied to the isolated test project (the user ran `npx supabase db push`, from their own terminal — this sandboxed environment's connection to the test project's Postgres pooler is intermittently blocked, as noted earlier this session). **Not yet verified at the time**: `e2e/billing-paywall.spec.ts` hadn't actually been run yet. Real Stripe Checkout/Portal/webhook flows were also unverified end-to-end.

**What was needed from the user before this could be considered done (resolved during the integration round above, except where noted):**
1. ~~Apply the migration to the isolated test project.~~ **Done.**
2. ~~Run `npm run test:e2e` to confirm `billing-paywall.spec.ts` actually passes.~~ **Done as part of the integration round above** (deferred until after the merge — see that entry for why).
3. In the Stripe dashboard: on the Product just created, add a second Price for the yearly cadence if only one exists so far (one Product, two Prices — monthly 5 990 Ft, yearly 59 900 Ft). Copy both Price ids into `.env.local` as `STRIPE_PRICE_ID_MONTHLY`/`STRIPE_PRICE_ID_YEARLY`, alongside `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` — see `README.md` § Stripe for the local-dev webhook-forwarding command needed to actually receive webhook events locally. **Still open** — an owner action, not something this integration round could do without real Stripe dashboard access.
4. ~~Once verified, this branch needs its own review cycle before merging.~~ **Done** — the 3 findings in the integration round above, plus the merge into PR #3's final state.

**Not part of this pass**: merging, applying anything to production, or deploying.

## Merged to production (2026-09-05)

[PR #2](https://github.com/lbenyok/velemenytap/pull/2) merged into `master` as squash commit `4f984e6`, following the user's explicit approval after reviewing `REVIEW_REQUEST.md`. All 17 migrations (8 pre-existing + 9 new across rounds 2–3) are now applied to production; `supabase db advisors --type all` reports no issues.

Rollout followed the expand → deploy → enforce sequence laid out in `REVIEW_REQUEST.md` § 6 as it existed when this was approved (commit-pinned, since the current file at that path is a different document: [github.com/lbenyok/velemenytap/blob/4f984e6/veleminytap/REVIEW_REQUEST.md](https://github.com/lbenyok/velemenytap/blob/4f984e6/veleminytap/REVIEW_REQUEST.md)), since this round introduced two column-protecting triggers that would have broken currently-deployed code if applied at the same time as the migrations introducing the RPCs replacing the old direct-write paths:

1. The 7 "expand" migrations (`20260904130921` through `20260904135437`, plus `20260904194200`/`20260904194300`/`20260904194400`) applied to production first — deliberately skipping the two "enforce" migrations (`20260904194100`, `20260904194500`) by temporarily moving those files out of `supabase/migrations/` before running `supabase db push`, then restoring them. Verified with `supabase db push --dry-run` before and after.
2. PR #2 merged into `master`.
3. Vercel's deployment believed live at the time (checked via the dashboard, which showed a "Ready" build) — **this check was wrong, see "Vercel deployment gap" below; the actual application code did not update until several hours and three diagnostic commits later.**
4. The two "enforce" migrations applied via `supabase db push --include-all` (required since their timestamps sort earlier than migrations already applied in step 1 — Postgres/Supabase's own out-of-order-migration safeguard, not a mistake in the migration numbering).

**Note on execution**: the three production `supabase db push` invocations (the two above, plus a later ad-hoc one — see below) were run directly by the user in their own terminal, not by Claude — Claude Code's auto-mode classifier blocks direct production-database write commands regardless of in-conversation approval. Claude prepared and dry-run-verified each command, the user executed them, and Claude independently re-verified the resulting state via `supabase migration list`/`db advisors` (read-only) afterward rather than trusting the terminal output secondhand.

## Vercel deployment gap discovered and fixed (2026-09-05)

**Finding**: after applying the round-2/round-3 migrations and merging PR #2, a live smoke check of the production site (`https://veleminytap.vercel.app`) found that two new public API routes this PR added (`/api/e2e-config-check`, `/api/notification-email/confirm`) were being redirected to `/login` instead of handled — the exact symptom of `proxy.ts` not yet knowing about them. Checking the Vercel dashboard's Deployments list confirmed it: the Production alias was still pointed at commit `a3d5ce1` — **a commit from before round 1's entire review response**, one day old, with no newer deployment (successful, failed, or otherwise) ever having been created. This meant:

- Round 1's fixes (the open redirect close, tenant-isolation RLS work, all 11 findings) were never actually live in production at the application-code level, despite `STATUS.md`'s round-1 entry stating the merge was complete — that entry's Vercel-deploy claim was an unverified assumption (`"not separately verified as part of this exchange"`), and the assumption was wrong.
- Round 2/3's fixes were likewise never live.
- **The two "enforce" migrations just applied in this same session were, for a window, actively rejecting the still-live old code's direct writes** to `nfc_cards.last_negative_alert_at` and `organizations.notification_email` — meaning negative-feedback alerts and notification-email settings saves were failing in production during that window. No customers exist yet, so no real user was affected, but this was a genuine, live correctness bug for as long as it lasted.

**Root cause, found by process of elimination** (each step confirmed empirically with a real test push, not assumed):
1. Pushing an empty test commit to `master` produced **no new deployment at all** — the GitHub↔Vercel Git integration itself was broken (webhook not firing), unrelated to anything in this review cycle. Disconnecting and reconnecting the Git repository in Vercel's project settings fixed the webhook (confirmed: the next push did trigger a build).
2. That build then failed with `Couldn't find any pages or app directory` — Vercel's **Root Directory** project setting was blank/wrong, building from the git repo root (which holds unrelated files — design assets, Claude config) instead of the `veleminytap/` subdirectory where the actual Next.js app lives. Likely reset to blank by the disconnect/reconnect in step 1. Setting Root Directory to `veleminytap` fixed this — confirmed: the next push built successfully and the previously-broken routes now respond correctly on the live production URL.

**This means the Vercel project's Git integration and Root Directory setting were never correctly configured for continuous deployment from this repository, going back to before this entire review cycle began** — not something introduced by rounds 1–3's work. `ARCHITECTURE.md`/`DECISIONS.md` should be treated as accurate for application code from this point forward; anything they said was "deployed to production" before 2026-09-05 (afternoon) needs re-confirming, since it's now established that Vercel deployment status can silently lag the actual `master` HEAD by an unknown amount.

**Verified after the fix**: `/api/e2e-config-check` returns the correct production Supabase project URL; `/api/notification-email/confirm` with an invalid token now correctly reaches the route handler and redirects to `/dashboard/settings?notification_email=invalid` (rather than bouncing to `/login` before ever running); homepage loads with no console errors. Not independently re-verified: the full authenticated dashboard flow (no test credentials used against production, consistent with this project's standing discipline of never writing test data to production).

## Round-3 review-response pass (2026-09-04, merged — see above)

A third-party review of PR #2 (branch `fix/round2-review-findings`, then at commit `68cd929`) raised 7 further findings (R3-01–R3-07). All 7 verified independently — reproduced the CI failure R3-01 predicted, reproduced R3-02's race with real concurrent requests, confirmed R3-04/R3-07's gaps by direct RPC calls and catalog introspection — and fixed; **all 7 confirmed, none rejected**. In brief:

- **R3-01 (high)** — `e2e/support/env.ts`'s credential resolution was broken in CI specifically (the case it was meant to protect): a per-key presence check could pass while the function still returned an object missing every key, which is exactly what broke Playwright's `globalSetup` and would have failed the entire CI e2e job before a single test ran. Rewritten to resolve from exactly one complete source (file or process environment, never a mix), validate the resolved project against an explicit allowlist (credential presence isn't proof of safety), and overwrite `process.env` so the test process and the spawned server always agree.
- **R3-02** — `claim_negative_alert_send`'s org-wide budget check had no lock scoped to the organization, so concurrent claims for *different* cards in the same org could all pass the same "under budget" snapshot. Fixed with a `pg_advisory_xact_lock` keyed on the org id; verified with `Promise.all`-driven genuinely concurrent requests, not a sequential loop.
- **R3-03** — built the recipient-confirmation flow round 2 had explicitly deferred: a candidate `notification_email` is now pending until a confirmation link (sent to the candidate address itself) is clicked, with the active address untouched until then and a trigger blocking any attempt to bypass verification via a direct write.
- **R3-04** — `get_feedback_period_analytics`'s caller-controlled `p_days` fed straight into `generate_series` with no validation; now allowlisted to 7/30/90, rejected before any table scan.
- **R3-05** — the alert-cooldown and (new) notification-email-change triggers now ship as a separate, later "enforce" migration from the RPCs/columns that introduce them, closing a real production rollout-ordering hazard where applying both together would break currently-deployed code mid-deploy.
- **R3-06** — `claim_negative_alert_send`'s log row used to record an alert as sent the instant the cooldown/budget checks passed, before any recipient lookup or Resend call; rows are now attempts with an explicit status, finalized by a new `finalize_negative_alert_send()` called from a `finally` block so every reservation resolves.
- **R3-07** — `get_feedback_overview_snapshot` had retained an unintended `service_role` grant (the same default-privileges gap R2-07 fixed for two other functions, missed for this one); fixed, and the full 5-function role matrix is now a committed automated test (`e2e/rpc-privilege-matrix.spec.ts`) instead of a one-time manual check.

**New migrations** (5 new files, plus migration 12 revised in place since it had not yet been applied anywhere but the isolated test project): `20260904194100_enforce_alert_cooldown_trigger.sql`, `20260904194200_validate_analytics_period_days.sql`, `20260904194300_restrict_service_role_and_enable_alert_log_rls.sql`, `20260904194400_notification_email_verification.sql`, `20260904194500_enforce_notification_email_change_trigger.sql` — none applied to production; `supabase db advisors` reports no issues against the isolated project.

**New/extended test coverage**, all against the isolated project: `e2e/notification-email-verification.spec.ts` (10 tests, including a full real-browser round trip through the actual confirmation URL), `e2e/negative-feedback-alert-finalize.spec.ts` (4), `e2e/analytics-period-validation.spec.ts` (10), `e2e/rpc-privilege-matrix.spec.ts` (6, direct Postgres catalog introspection), plus a new real-concurrency test in `e2e/negative-feedback-alert-abuse.spec.ts` and 5 new `e2e/support/env.test.ts` unit tests (11 total).

**Verification**: `npm run typecheck` clean, `npm run lint` clean, `npm run test` 88/88 (Vitest), `npm run test:e2e` 80/80 (Playwright, real browser, isolated project, `SUPABASE_DB_URL` exported so the two connection-requiring spec files actually ran rather than skipped), `npm run build` clean (18 routes, including the new `/api/notification-email/confirm`). Test-project database confirmed clean of leftover orgs/users after the full run. **A full "migration replay from an empty database" was deliberately not performed** — the reasoning (a raw schema reset risks silently losing the project's own `ALTER DEFAULT PRIVILEGES` configuration, which is itself exactly what R2-07/R3-07 are about) and what was done instead were written up in `REVIEW_REQUEST.md` § 4 at the time (that document's own § 7 cross-references it as "§ 4"; an earlier version of this line cited "§ 5," which was already wrong before this correction). Historical version, commit-pinned since the current file at that path is a different document: [github.com/lbenyok/velemenytap/blob/4f984e6/veleminytap/REVIEW_REQUEST.md](https://github.com/lbenyok/velemenytap/blob/4f984e6/veleminytap/REVIEW_REQUEST.md). **None of this constitutes production verification** — every test ran against the isolated Supabase test project, never production; no production migration has been applied.

**Not part of this pass**: merging the branch, applying the 5 new migrations to production, or deploying.

## Round-2 review-response pass (2026-09-04)

A second independent review of `master` (post round-1 merge) raised 9 further findings (R2-01–R2-09). All 9 verified independently against actual code/database state (empirical reproductions, real concurrent database connections, `pg_proc.proacl`/`has_function_privilege()` introspection — not accepted on faith) and fixed; **all 9 confirmed, none rejected**. In brief:

- **R2-01** — the round-1 open-redirect fix's origin check was necessary but not sufficient: a dot-segment payload (`/a/..//evil.example/path`) and a same-origin absolute URL with a double-slash pathname both produced a same-origin-*validated* result that was itself a protocol-relative URL. Fixed by re-validating the exact output string, not just the input candidate (`lib/safe-redirect.ts`).
- **R2-02/R2-03/R2-04** — round 1's analytics fix (page-fetching past PostgREST's 1000-row cap up to a 5000-row ceiling) still had a silent ceiling at 5000, plus `OFFSET`-pagination inconsistency under concurrent writes. Rebuilt as two tenant-scoped SQL aggregation functions (`get_feedback_overview_snapshot`, `get_feedback_period_analytics`) computing every statistic in one snapshot-consistent call, with an explicit unavailable/retry UI state on query failure instead of an apparently-valid empty result.
- **R2-05** — `submit_feedback_atomic` locked only the card row, not the location row it also reads status from; a location deactivated mid-transaction could still receive a feedback insert. Fixed by locking both rows in the same `SELECT`, reproduced with two real concurrent database connections.
- **R2-06** — e2e test config could silently fall back to production credentials and reuse a wrongly-configured already-running dev server. Fixed: fails closed on missing isolated-project config, force-disables `RESEND_API_KEY`/`NEXT_PUBLIC_SENTRY_DSN` for test runs, and a Playwright `globalSetup` verifies the actually-running server's configured Supabase URL before any test executes.
- **R2-07** — `revoke ... from public` alone did not remove `anon`/`authenticated`'s ability to call either round-1 RPC, due to this project's `ALTER DEFAULT PRIVILEGES` configuration. Not a demonstrated RLS bypass in practice, but fixed with an explicit role allowlist on every RPC (5 functions total).
- **R2-08** — the negative-feedback alert cooldown was a plain, tenant-writable column; an org's own authenticated session could reset it directly, and the per-card rate limit didn't bound total email volume across an org's cards. Fixed: the cooldown column is now trigger-protected against any caller but one dedicated function, plus a new organization-wide hourly send budget. Recipient-address verification deliberately not built this pass — see `DECISIONS.md` § "R2-08."
- **R2-09** — the card editor offered a location-change dropdown the database has rejected since round 1; fixed at both the UI (read-only location text when editing) and server-action (no `location_id` field in the update schema at all) layers.

**New migrations** (4, none applied to production): `20260904130921_tenant_scoped_analytics_aggregation.sql`, `20260904131144_enforce_rpc_role_allowlists.sql`, `20260904132712_lock_location_in_feedback_submission.sql`, `20260904135437_server_owned_alert_cooldown_and_budget.sql` — all validated against the same isolated test project as round 1, never against production.

**New/extended test coverage**, all against the isolated project: `e2e/nfc-card-location-lock.spec.ts`, `e2e/analytics-aggregation.spec.ts`, `e2e/location-deactivation-race.spec.ts` (real two-connection concurrency, using the `pg` driver directly since PostgREST/supabase-js has no cross-request transaction control), `e2e/negative-feedback-alert-abuse.spec.ts`, `e2e/support/env.test.ts`; extended `lib/safe-redirect.test.ts` (34, up from 29), `e2e/redirect-safety.spec.ts` (7, up from 4); new `features/analytics/parse-snapshots.ts`/`.test.ts`. Deleted (confirmed dead first): `features/analytics/aggregate.ts`/`.test.ts`, `fetch-all-rows.ts`/`.test.ts`, `e2e/analytics-row-cap.spec.ts` (all superseded by SQL aggregation).

**Verification**: `npm run typecheck` clean, `npm run lint` clean, `npm run test` 83/83 (Vitest), `npm run test:e2e` 49/49 (Playwright, real browser, isolated test project), `npm run build` clean (all 16 routes compile). Test-project database confirmed clean of leftover orgs/users after the full run. **None of this constitutes production verification** — every test ran against the isolated Supabase test project, never against the linked production project; no production migration has been applied and no production data was read or written as part of this pass.

**Not part of this pass**: merging the branch, applying the 4 new migrations to production, or deploying — this task's explicit constraint. The reviewable PR and rollout plan were in `REVIEW_REQUEST.md` § 6 at the time — commit-pinned, since the current file at that path is a different document: [github.com/lbenyok/velemenytap/blob/68cd929/veleminytap/REVIEW_REQUEST.md](https://github.com/lbenyok/velemenytap/blob/68cd929/veleminytap/REVIEW_REQUEST.md).

## Merged to production (2026-09-04)

[PR #1](https://github.com/lbenyok/velemenytap/pull/1) merged into `master` as squash commit `e2bbb7b`, CI green. Rollout order was deliberately migrations-before-merge: CI's e2e secrets were updated to the isolated test project's values first (confirmed green), then all 5 new migrations were applied to **production** (`supabase db push` via the transaction pooler — the direct/IPv6 connection was unreachable from the network in use at the time; `supabase db advisors` against production reported no issues afterward, migration history confirmed in sync, 8/8), then the PR was merged. Vercel's deploy from `master` follows its normal auto-deploy flow — not separately verified as part of this exchange; confirm the live deployment reflects `e2bbb7b` before treating this as fully rolled out. `REVIEW_REQUEST.md` § 6 had the exact sequence and the rollback plan at the time — commit-pinned, since the current file at that path is a different document: [github.com/lbenyok/velemenytap/blob/8f05205/veleminytap/REVIEW_REQUEST.md](https://github.com/lbenyok/velemenytap/blob/8f05205/veleminytap/REVIEW_REQUEST.md).

## Review-response pass (2026-09-04)

All 11 findings from the independent review verified against the actual code (not accepted on faith) and fixed where confirmed; see the finding-by-finding verdict handed back to the reviewer for the full detail. In brief:

- **Confirmed and fixed, all with new migrations against a newly-provisioned isolated Supabase test project (not the shared dev/production one) and new automated tests:** Sentry redaction (4 real bugs — see `SECURITY.md`), the open-redirect in both `next`-param redirect call sites, the `feedback_update` RLS policy's missing column-level restriction, `nfc_cards.location_id` relocation, public-submission abuse/email-amplification (a real database-backed rate limit and alert cooldown, not Redis), the public submission path's active-status check-then-insert race, the analytics row-cap silent truncation (confirmed empirically: 1200 seeded rows, `.limit(5000)` requested, 1000 returned), organization-onboarding's non-atomicity/non-idempotency, and the feedback inbox's cursor pagination correctness under tied `created_at` values.
- **New test coverage**, all run against the isolated project: `e2e/feedback-and-card-integrity.spec.ts`, `e2e/public-submission-safety.spec.ts`, `e2e/feedback-pagination.spec.ts`, `e2e/analytics-row-cap.spec.ts`, `e2e/organization-onboarding.spec.ts`, `e2e/tenant-isolation.spec.ts`, `e2e/redirect-safety.spec.ts`, plus 29 new `lib/safe-redirect.test.ts` unit tests, 22 rewritten `lib/sentry-redact.test.ts` unit tests, and 7 new `features/analytics/fetch-all-rows.test.ts` unit tests. 35 e2e tests and 82 unit tests, all green.
- **Test infrastructure**: e2e/db tests now run against a dedicated, isolated Supabase project (`.env.test.local`, gitignored) instead of the shared dev/production one flagged in the prior entry's "Deliberate tradeoff" — see `e2e/README.md`. ~~CI's e2e job secrets still need updating~~ — done in the merge that followed this pass, see "Merged to production" above.
- **Documentation corrected**, not just code: `SECURITY.md`'s admin-client-consumer count was wrong (claimed two, actually four at review time); now lists all three current admin-client consumers plus the two `SECURITY DEFINER`/`SECURITY INVOKER` database functions that are the other RLS-bypass category, and the "no rate limiting" / "no tenant-isolation test" claims in this file's own prior "Not done" section (below) are corrected rather than left stale.
- **Not part of this pass** (production migration/deployment): done in the merge that followed, once explicitly approved — see "Merged to production" above.

## Review-prep pass (2026-09-04, no new functionality)

Full verification re-run from a clean state, all green: `npm run typecheck`, `npm run lint`, `npm run test` (30/30 Vitest), `npm run test:e2e` (6/6 Playwright against the real linked Supabase project, test data confirmed cleaned up), `npm run build` (production build succeeds). The full external-review handoff written for this pass — covering architecture, every implemented feature, repo structure, database tables, RLS policies, auth/authz, the public submission flow, Google Review link logic, security-sensitive files, all Server Actions/routes, migrations, tests, and known bugs/risks/shortcuts, addressed to an independent senior reviewer (ChatGPT) — was `REVIEW_REQUEST.md` at the time; it has since been rewritten many times over by later rounds, so the current file at that path is a different document. Historical version: [github.com/lbenyok/velemenytap/blob/5f8436c/veleminytap/REVIEW_REQUEST.md](https://github.com/lbenyok/velemenytap/blob/5f8436c/veleminytap/REVIEW_REQUEST.md).

**One real bug found during this pass, not introduced in the reviewed diff:** `app/auth/confirm/route.ts` redirects to the raw `next` query parameter after a successful email-confirmation OTP verify, with no same-origin validation — an **open redirect**. Contrast with `features/auth/actions.ts`'s `signInAction`, which validates `next` (`typeof next === "string" && next.startsWith("/") && !next.startsWith("//")`) before redirecting. This file hasn't been touched since it was first written (`ea63f03`, the original auth-flow commit) and was not part of this session's diff — left unfixed deliberately per this pass's explicit "no new functionality" instruction, flagged here and in that same `REVIEW_REQUEST.md` handoff (see the commit-pinned link just above) for the reviewer and for a follow-up fix.

## Done

**The entire product is now in Hungarian, and rebranded to match the real logo.** Every user-facing surface — the new homepage, the public feedback flow (`/r/[publicId]`), auth (login/signup/check-email/link-expired), onboarding, the full dashboard (nav, Overview, Locations, NFC Cards, Feedback inbox, Analytics, Settings), the 404 page, the global error boundary, and the negative-feedback alert email — was translated in place (see `DECISIONS.md` for why in-place rather than an i18n framework). `<html lang="hu">`. Supabase Auth's own English error messages get a small translation lookup (`translateAuthError` in `features/auth/actions.ts`) rather than leaking English on an otherwise-Hungarian sign-in form. Verified for real: logged into a real QA account, walked the Hungarian dashboard nav, and re-ran the 1-star public submission — the Google Review CTA ("Google-értékelés írása") still fires correctly, proving the core no-gating invariant survived the relocalization. `e2e/review-gating.spec.ts` was updated to assert on the new Hungarian button/link text and passes 6/6 against the real app.

The shared `--pf-*` color system (used by both the public feedback page and the homepage) was rebranded from an invented wine/gold palette to navy ink + a blue-to-cyan gradient, matching the user-supplied logo and app icon exactly — variables renamed `--pf-accent`/`--pf-accent-hover`/`--pf-accent-2` (previously `--pf-wine`/`--pf-wine-hover`/`--pf-gold`) since the old names became misleading once they held blue/cyan values.

**A real public homepage now exists at `/`**, replacing the unbuilt Next.js scaffold placeholder — see the homepage-build entry below for what it contains (all copy is now Hungarian as of this pass, superseding the "English, matching the dashboard" choice made when it was first built).

## Done (earlier in this session)

Everything through the product skill's MVP priority list (auth → orgs → locations → NFC cards → public landing page → feedback submission → inbox → statuses/resolution → negative-feedback notifications → basic analytics → Google Review CTA → production deployment) was built and browser-verified in earlier sessions, then deployed to Vercel production (https://veleminytap.vercel.app).

**Resend is fully live, including a verified sending domain.** `velemenytap.hu` is verified in Resend, and `RESEND_FROM_EMAIL` is `alerts@velemenytap.hu` in both `.env.local` and Vercel production (updated from the shared `onboarding@resend.dev` sender). Confirmed via direct API calls: a send to a third-party address (not the Resend account's own email) succeeded once the domain was verified, where it had previously been rejected with `403 validation_error` under the shared sender's restriction — the hard blocker noted below is resolved. The sender display name is `Velemenytap` (ASCII, no diacritics) per explicit request, not `VéleményTap`. The full `submitFeedbackAction` → `after()` → `sendNegativeFeedbackAlert` code path was exercised end-to-end (real signup, real org/location/card, a real 1-star submission through `/r/{publicId}`) with no error logged, prior to the domain switch.

**Sentry is now live.** `@sentry/nextjs` is wired up for client, server, and edge runtimes (`instrumentation.ts`, `instrumentation-client.ts`, `sentry.server.config.ts`, `sentry.edge.config.ts`), plus `app/global-error.tsx` to catch root-layout rendering errors. `NEXT_PUBLIC_SENTRY_DSN` is set in both `.env.local` and Vercel production (as a `config`-type var — a DSN is meant to be public, not a secret; Vercel's credential-detection prompt was overridden deliberately, not by accident). A deliberately-thrown test error was triggered locally through a temporary route (removed afterward) and confirmed reaching Next.js's error handling correctly. **Redaction is enforced**: `lib/sentry-redact.ts`'s `beforeSend` hook strips `feedback_text`/`internal_note` (and their camelCase equivalents) from request data, extra context, and breadcrumbs on every event before it leaves the app — covered by 6 Vitest unit tests including a circular-reference case. Source map upload (via `authToken`/`org`/`project` in `withSentryConfig`) was deliberately skipped for now — see `DECISIONS.md`.

**Playwright e2e and CI are now live and fully green, including in CI itself.** `e2e/review-gating.spec.ts` automates the product skill's Review-Gating Regression Test for real: for each rating 1–5, a real browser loads the public feedback page, submits, and asserts the Google Review CTA is visible with the correct URL — plus a duplicate-submission test. `.github/workflows/ci.yml` (repo root) runs typecheck/lint/Vitest on every push/PR, then Playwright against a production build (`npm run build` + `next start`, not `next dev` — Turbopack's dev cold-start was too slow for a CI runner). All three GitHub Actions repo secrets are now configured; the most recent run (`14f5b34`, [run #5](https://github.com/lbenyok/velemenytap/actions/runs/33800333904)) passed end-to-end: 45s checks + 4s secrets gate + 1m43s e2e, **6/6 Playwright tests passed**, test data confirmed cleaned up afterward. **Deliberate tradeoff:** e2e tests run against the same Supabase project as dev/production, not an isolated Docker-based local stack — see `DECISIONS.md` for the full reasoning and when to revisit it.

Getting here took four CI iterations, each a real bug found and fixed via the actual failing run rather than guessed away: (1) a workflow-wide `working-directory` default broke a job with no checkout step, (2) `next dev`'s Turbopack cold-start exceeded Playwright's webServer timeout on a GitHub-hosted runner, (3) the secrets gate only checked one of the three required secrets so a bad value slipped through silently, (4) the actual bad value — the `NEXT_PUBLIC_SUPABASE_URL` secret needed re-pasting. All four are separate commits with the failing-run URL in each message.

This session closed the gap between that MVP and the master build prompt's fuller spec:

- **`feedback.priority`** — generated column (`high`/`medium`/`normal` from rating), surfaced as a badge in the inbox table and detail dialog, with unresolved-high-priority rows visually highlighted.
- **NFC card filter** on the feedback inbox (alongside the existing status/rating/location/date filters).
- **Overview page** — added Today / This week / Unresolved-negative stat tiles and a rating-distribution bar, on top of the existing Total/Average tiles and recent-feedback list.
- **Settings page** — business name, notification email, logo URL (pasted URL, not an upload — see `DECISIONS.md`). `notification_email`, when set, overrides the default owner/admin/manager email fan-out for negative-feedback alerts.
- **`profiles` table** — one row per auth user, auto-created on signup via trigger. Not yet read anywhere in the app (see `DATABASE_SCHEMA.md`).
- **Cookie-based duplicate-submission protection** on the public feedback endpoint (`SECURITY.md` has the full reasoning for why this instead of IP/Redis rate limiting).
- **Vitest unit tests** for `features/analytics/aggregate.ts` and the extracted `features/feedback/schema.ts`, including a schema-level review-gating regression test (`it.each([1,2,3,4,5])`).
- **Full documentation suite** — this file plus `README.md`, `PRODUCT_SPEC.md`, `ARCHITECTURE.md`, `DATABASE_SCHEMA.md`, `SECURITY.md`, `DECISIONS.md`, `TEST_PLAN.md`, `REVIEW_REQUEST.md`.

Everything above was typechecked, linted, and — for the UI-facing pieces — manually verified in a real browser against the real linked Supabase project (`TEST_PLAN.md` has the specifics), with the QA account and data cleaned up afterward.

## Not done / explicitly deferred

- **Sentry source map upload.** Error capture is fully live; source maps (readable stack traces in the dashboard instead of minified ones) need a `SENTRY_AUTH_TOKEN` plus org/project slugs, deliberately skipped for now — see `DECISIONS.md`.
- ~~A genuinely isolated e2e environment.~~ **Done this pass** — see the review-response entry above and `e2e/README.md`.
- ~~Tenant-isolation automated test.~~ **Done this pass** — `e2e/tenant-isolation.spec.ts`.
- **QR code generation for NFC cards.** Explicitly lower priority per the product skill ("do not allow QR work to delay core functionality") — not started, and nothing currently blocks a business from printing/encoding the public URL onto a physical NFC tag by other means in the meantime.
- ~~Redis-backed rate limiting.~~ **Superseded, not simply done** — a database-backed rate limit was built instead (`submit_feedback_atomic`), which closes the actual gap (an unbounded scripted flood) without Redis; see `SECURITY.md` for why Redis specifically remained unnecessary rather than merely deferred.
- **Org switcher, role-gated authorization, `feedback_notes` table, logo upload UI.** All deliberate MVP-scope decisions — see `DECISIONS.md` for the reasoning behind each.
- ~~Tenant-scoped SQL aggregate functions/views for analytics.~~ **Done in the round-2 pass** — `get_feedback_overview_snapshot`/`get_feedback_period_analytics`, see `DATABASE_SCHEMA.md`.
- **Verified `notification_email` recipients for negative-feedback alerts.** An organization-wide send budget bounds the blast radius (round-2, R2-08); actual address verification needs its own email-confirmation flow, deliberately out of scope this pass — see `DECISIONS.md`.

## What's needed from the user now

- ~~Review and approve PR #2, apply migrations, merge.~~ **Done 2026-09-05** — see "Merged to production" above.
- Add `SUPABASE_DB_URL` as a CI repo secret (isolated **test** project's connection string, not production's) so `e2e/rpc-privilege-matrix.spec.ts` and `e2e/location-deactivation-race.spec.ts` actually run in CI instead of silently skipping — already wired into `ci.yml`'s `e2e` job env, just needs the secret added under GitHub repo Settings → Secrets and variables → Actions.
- **(Fourth independent review, Finding 5) Configure the scheduled billing-reconciliation sweep's three pieces — none set yet:** `RECONCILE_SWEEP_SECRET` as a new Vercel Production environment variable (a long random secret); a matching `RECONCILE_SWEEP_SECRET` GitHub Actions repository secret (same value); and `PRODUCTION_RECONCILE_SWEEP_URL` as a GitHub Actions repository **variable**. Until all three are set, `.github/workflows/reconcile-billing-sweep.yml` (already committed, runs every 15 minutes) will keep failing loudly against the unconfigured secret rather than silently doing nothing — see `DEPLOYMENT.md`'s "Scheduled billing reconciliation sweep" section for the exact steps.
- **A Hungarian VAT/Stripe-Tax invoicing decision — a business/accounting decision, not a technical one, and not yet made by the owner.** `assertStripeConfigurationValid()`'s `tax_behavior: "inclusive"` check only proves the Stripe Price's own field agrees with the billing page's "az árak az ÁFát tartalmazzák" copy — it is NOT proof that a compliant Hungarian VAT invoice is actually calculated, collected, and issued for a real purchase. Three options, described neutrally (none recommended here — this needs the owner and/or an accountant, not this codebase, to decide): Stripe Tax (Stripe's own automated tax product), a fixed manually-configured tax rate, or an external Hungarian invoicing provider integrated alongside Stripe. See `DEPLOYMENT.md`'s "Stripe Tax / Hungarian VAT invoicing" section. This app's paid-launch readiness is explicitly blocked on this decision.

## What was needed from the user before round 1 shipped

- ~~Confirmation that the Supabase Auth Site URL/Redirect URLs were added for production.~~ **Confirmed done.**
- ~~Verify a sending domain in Resend.~~ **Confirmed done — `velemenytap.hu`, sends to arbitrary recipients working.**
- ~~A decision on whether/when to prioritize Sentry vs. Playwright/CI vs. further product features.~~ **Both are done.**
- ~~Add three GitHub Actions repo secrets.~~ **Confirmed done.** ~~Update them to the isolated test project's values.~~ **Done** — see "Merged to production" above.
- ~~Apply the five new migrations to the production database.~~ **Done** — see "Merged to production" above.
- ~~Confirm the Vercel deployment triggered by the `master` merge actually reflects `e2bbb7b`.~~ **This was never actually confirmed, and the honest answer turned out to be no** — see "Vercel deployment gap discovered and fixed (2026-09-05)" above. The deployment stayed on a pre-round-1 commit (`a3d5ce1`) for an extended period; this specific unresolved item is what that section is the resolution of, applying to the current commit rather than retroactively to `e2bbb7b` specifically (there's no way to confirm a now-superseded historical deployment state after the fact).
- Optional: a `SENTRY_AUTH_TOKEN` + org/project slugs, if/when readable (non-minified) stack traces in the Sentry dashboard become worth the extra setup.

## Deployed

Phase 14 (feat/test/docs, commits `47ec557`/`b9baf5f`/`1daf267`), the Resend wiring (`6b859e0`), and Sentry wiring are all committed, pushed to GitHub, and live on Vercel production. Playwright/CI is committed, pushed, and passing in CI as of `14f5b34`.
